# Cuubz — Bug Ledger

**This is the single list of known defects. There is no other.** `DEPLOY.md` §8 used to
hold a second copy; it is now a pointer to this file.

**Every row has an owner PR. No row may say "Unowned".** That rule exists because
"documented and unowned" is how this codebase reached the state Phase 0 is unpicking:
`DEPLOY.md` §8 shipped with six unowned rows, one of which (**H-1**) was live data
corruption that destroyed ~90% of a world's saved chunks per cross-world visit and sat
unowned through two PRs. A defect with a severity and no owner is a defect nobody is
going to fix.

**The process, which applies to every session from PR 6c onward:**

1. Every bug found gets a row here — ID, what, severity, owner PR, status — at the time
   it is found, not at the time someone decides what to do about it.
2. Every row gets **either a fix in the current PR or an explicit PR slot**. If no
   suitable PR exists, create the slot in `refactor.md` and cite it here.
3. A bug found mid-task that is out of scope is **logged with an owner and left**, not
   silently absorbed and not merely mentioned in prose. Prose is not a ledger.
4. IDs are never reused and never renumbered — they are cited from `refactor.md`,
   `DEPLOY.md`, source comments and test messages.

Severity means:

| | |
|---|---|
| **critical** | destroys player data, or takes production down, if triggered |
| **high** | player-visible breakage, silent stale deploys, or unrecoverable data loss in a narrow case |
| **medium** | real cost or real risk, with a workaround or a bounded blast radius |
| **low** | waste, confusion, or a latent trap with no live consequence |

---

## Open

| ID | What | Severity | Owner PR | Status |
|---|---|---|---|---|
| **D-122** | **A failed save left the worldgen version upgraded in memory and not on disk.** `WorldManager.upgradeWorldgen` set `world.worldgenVersion = 2` and *then* called `storage.saveWorld`, returning `{success:false}` from the catch with the cache already mutated. The caller sees a failure and the world screen says so; the `WorldManager` the running process holds says the world is on v2. Every consumer reads the cache — `getWorldgenVersion` feeds `genParams` (`ChunkGenerator.js:44-51`) and the worker branches on it — so the session that followed would have **generated and saved v2 chunks into a world that reloads as v1**. That is precisely the mixed terrain the version gate exists to prevent (§3.1), arrived at without the confirmation, and it is not recoverable: the chunks are on disk with the terrain they were written with. `deleteWorld`, two methods above, has always mutated the cache only *after* the storage call returns | low — a storage write has to fail to reach it, and IndexedDB writes of a ~1 kB world record do not fail often. The severity is about the trap rather than the rate: the one field in this object whose value decides what terrain gets written permanently was also the one mutated before it was durable, and nothing downstream can tell a stale cache from a fresh one | **S9** | **Fixed in S9.** The catch restores the previous value, and restores an *absent* key as absent rather than as `undefined` — a pre-S4 save has no `worldgenVersion` field at all and `serialize()` writes whatever is on the object. `test/unit/ui/worldUpgrade.test.js` drives it through the real screen and a store whose `saveWorld` throws, and asserts the version, the key's absence and that the upgrade badge is still offered afterwards; the assertion was confirmed red against the pre-fix method (`expected 2 to be 1`). **Found by S9**, while writing the save-failure case for the upgrade button — the failure path had no caller before this commit, because the button did not exist |
| **D-121** | **The third copy of D-63's wrong block id.** `src/engine/world/workerGeneration.js`'s `BLOCK` table declared `YELLOW_POPLAR_LEAVES: 115`. Id 115 is `white_concrete`; the real `yellow_poplar_leaves` is **192**. This is the same literal mistake **D-63** fixed in `src/engine/renderer/meshWorker.js`, where it made the mesh worker apply humidity-based green tint to white concrete, and which `src/game/data/BlockCategories.js` documents at length under the heading that no consumer should keep a hand-written id table at all. The terrain generator kept one, and nobody checked it. **Latent, not live:** no entry in the worker's `TREE_TYPES` names yellow poplar — the poplar rows all use `ORANGE_POPLAR_LEAVES` — so nothing has ever planted it. The first person to add an autumn-poplar variant would have planted concrete | low — no live consequence today, and the exact shape of a defect that has already shipped twice. The severity is about the trap, not the symptom | **S5** | **Fixed in S5.** Id corrected to 192. More usefully, `test/unit/engine/biomeMasks.test.js` now sweeps **every** `NAME: id` pair in the worker's `BLOCK` table against `BLOCK_BY_ID` and `BLOCK_TYPES`, so the whole table is checked rather than the one entry someone happened to look at. That guard found this on its first run. `BlockCategories.js`'s ruling — compute from the registry, never transcribe — still cannot be applied here, because `workerGeneration.js` is a classic script with no imports (decision 14); a sweep over the source text is the strongest available substitute. **Found by S5** |
| **D-120** | **A reconnecting player was ignored by the host for the rest of the session.** `HostManager._handlePlayerLeft` sets `player.connected = false` and **nothing ever set it back**. Every host-side handler guards on that flag — `_handlePlayerMove`, `_handleRemoteBlockBreak`, `_handleRemoteBlockPlace`, `_handleInventorySync` and now `handleQuestContribute` all return early for a player that is not `connected`. The relay cannot supply the missing signal by accident either: `server/session.js:_handleJoin` recognises a returning `playerId` as a *reconnection* and deliberately does **not** re-broadcast `PLAYER_JOINED` (it replies `WELCOME` to the reconnector alone, so the other clients do not re-add an avatar they already have), which is correct for avatars and leaves the host with no way to learn the player is back. A player whose wifi blipped therefore rejoined, saw the world, could walk around locally — and had every block change, inventory sync and quest contribution silently discarded by the authority | high — player-visible and silent. The symptom is "my blocks stopped saving after I reconnected", with no error on either side. The 30 s reconnect grace D-103 added is what makes it reachable: the socket comes back inside the window, so the session survives and the player does not | **S2** | **Fixed in S2.** `HostManager._noteActivity(playerId)` flips the flag back the moment any message arrives from a known player, and the inbound handlers call it. This is not a weakened guard: **the relay only forwards from live sockets**, so a message arriving from a player is itself proof of connection — the guard was never wrong, it was starved of information. Reconnecting also re-fires `onPlayerJoined` with `reconnected: true` and re-sends `QUEST_SYNC`, since the world moved on while they were gone. `test/integration/questSync.test.js` drives it through the real relay: a guest contributes 8, drops, rejoins on a fresh socket, and contributes again — the last step was a no-op before the fix. **Found by S2**, while writing the disconnect-and-rejoin case §11 asks for |
| **D-118** | **Three Act 3 quests asked the player to collect an unbreakable block.** `BLOCK_TYPES.OBSIDIAN` was a *legacy alias* — `BLOCK_BY_NAME['crying_obsidian'].id` — and `crying_obsidian` has `hardness: -1`, which `getBlockDrop` short-circuits to `null` and which `BlockInteractionSystem.unbreakableBlocks` lists by that same alias. So "collect 5 obsidian" (Q13), "15 obsidian" (Q14) and "deliver 10 obsidian" (Q16) all named a block that cannot be broken and yields nothing when it is. `obsidian.png` has been on disk the whole time, unreferenced by any registry entry | medium — three of Act 3's five quests unfinishable, and the failure is silent: the block simply refuses to break, which reads as "needs a better pickaxe" rather than as a defect. Latent until S1, since no quest system existed to ask | **S1** | **Fixed in S1.** `obsidian` is a real registry entry at id 196 (hardness 10, pickaxe), pointing at the PNG that was already there; `OBSIDIAN` now resolves to it and the unbreakable list names `CRYING_OBSIDIAN`, which is what that entry always meant. `questSystem.test.js` asserts, over **every** `contribute_item` objective in all 28 quests, that the id resolves to a real block with `hardness !== -1` and a non-null `getBlockDrop` — so the next quest to ask for something unobtainable fails a test rather than a playthrough. Found by S1 |
| **D-119** | **`sandstone` had no registry entry at all**, though `sandstone.png`, `sandstone_top.png` and `sandstone_bottom.png` all ship. Q22 asks for 15 of it and `quest_implementation.md` §7.3 builds the Buried Hall out of it; `BLOCK_TYPES.SANDSTONE` was `undefined`, so the quest definition would have been `undefined` in an `items` array and counted nothing forever | low — one quest, and unlike D-118 it would have failed loudly the moment anything read the id. Notable mainly because `quest_implementation.md` §2.5 audited the storyline for missing content and listed two gaps (`bed`, `bread`); this and D-118 are two more the audit did not catch | **S1** | **Fixed in S1.** Registry id 197 with the per-face top/side/bottom split the textures were drawn for. §2.5's own two are closed in the same stage: `bed` is a new block (198, wool over planks — no bed texture ships and drawing one was not worth it) with a crafting-table recipe, and `bread` has a hand recipe from hay. Found by S1 |
| **D-117** | **`character.id` was stripped on the way to the wire, and pooled quest contributions key on it.** `initPlayer.js:159` called `sm.client.joinGame({ name: charData.name, color: charData.color }, …)` — the character's `id` was in scope, in `charData`, and was not included. `HostRemotePlayer` therefore stored a character with no `id`, and every consumer that needed a *stable* identity for a player had only `playerId`, which the relay assigns **per connection**. Pooled objectives (§4.5) credit a positive delta against a per-contributor high-water mark, so a player who dropped and reconnected mid-objective would have arrived with a fresh `playerId`, a high-water mark of 0, and been credited a second time for every item still in their inventory. A player carrying 20 of the 20 obsidian an objective wanted could have finished it twice by toggling their wifi | medium — no crash and no visible error; a pooled objective would simply complete early, which reads as a generous target rather than a defect. Latent until S0, because nothing had ever read the field | **S0** | **Fixed in S0.** `joinGame` sends `{ id, name, color }`, and `Host.handleQuestContribute` checks the `contributorId` on a contribution against the character id it recorded for that player on join — so the identity is both present and enforced. `test/unit/multiplayer/questRouting.test.js` asserts the field is sent at all, and `questState.test.js` drives the double-credit directly: the same character re-observing the same 15 obsidian after a reconnect credits 0, and the same observation under a fresh per-connection id credits 15 — the bug, pinned beside the fix. **Found by S0**, which named it as a verification item (§4.5, *"confirm `character.id` is present on the wire and not stripped"*); it was not |
| D-12 | `StrictHostKeyChecking=no` on both `scp` and `ssh` — any host key is accepted | low (LAN IP), by design | PR 10 | **Improved, not closed, in PR 10.** `accept-new`: the first key is trusted and pinned, so a *changed* key is now an error rather than a shrug. Closing it properly means provisioning a `known_hosts` entry out of band, which needs one verified connection to the host — and no session in this project has had SSH access. Revisit after the first real deploy |
| D-68 | **Two mob types can never spawn, and six of the ten biomes have no mobs at all.** `mobManager` selects a mob with `selectMobForBiome(biome)` where `biome` is `BiomeSystem.getBiomeAtWorldPos(...).id`, and `selectMobForBiome` matches on `def.biomes.includes(biome)`. **`corrupt_wolf` and `corrupt_wisp` declare `biomes: ['corrupt']`** — an id `BiomeSystem` cannot produce (its `NAME_TO_ID` has exactly ten keys and `corrupt` is not one) — so neither definition can ever match. `stone_golem` lists `deepslate_caves`, also not producible, but survives on its second entry `mountains`. Separately, **`deep_ocean`, `ocean`, `beach`, `badlands`, `desert` and `frozen_peaks` are matched by no mob definition**, so `selectMobForBiome` returns `null` and those chunks spawn nothing. `QuestSystem` has the same drift in a third place: 7 quests set `markerBiome: 'corrupt'` and 5 set `markerBiome: 'lava'` | medium — player-visible. Two of the game's mob types have never appeared, and six of ten biomes are empty of mobs, which reads as a spawn-rate problem rather than a defect. **This is the fourth artefact of one drift**: **D-39** (world previews advertising "Lava" and "Corrupt" as biomes), **D-59** (the render loop's unreachable lava/corrupt particle branches, closed in PR 20), the `AmbientAudio` config keyed on the same eight-name list (deleted with the module), and now the spawn tables. The eight-name list `Plains Forest Desert Tundra Mountains Ocean Lava Corrupt` predates `BiomeSystem`'s ten and nothing regenerates any consumer from it | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. **The names half is closed in PR 23 — decision 48**; the content half is what remains. A definition naming a biome `BiomeSystem` cannot produce is a defect, so `corrupt_wolf` and `corrupt_wisp` were repointed `'corrupt'` → `'badlands'` and `stone_golem`'s unproducible `'deepslate_caves'` was dropped (zero behaviour change, verified over 500 draws). `BIOME_NAME_TO_ID` is **derived from `BIOME_DEFS`** now — a second hand-written table inside the `BiomeSystem` IIFE went with it — and `test_mobBiomes.js` fails if any mob definition names an id `BiomeSystem` cannot produce, which is the deliverable **D-64** demanded in the biome dimension. **What PR 23 deliberately did not do:** decide whether an ocean should have mobs. **Five biomes still have none — `deep_ocean`, `ocean`, `beach`, `desert`, `frozen_peaks`** (badlands stopped being one), pinned as an assertion so the day someone adds an ocean mob that line says to update this row. The quest half — 7 quests with `markerBiome: 'corrupt'` and 5 with `'lava'` — was left untouched with `QuestSystem.js`. Both are content calls of the kind decision 42 removed from PR 20, so **PR 34** owns them. Found by PR 20 |
| D-33 | `npm run lint` reports **172 `no-unused-vars` warnings**, and warnings do not fail the build. *(178 until PR 14 deleted the two `Browser*` manager classes; PR 15 moved ~700 lines out of `main.js` and it stayed at 176, because the extraction removed six dead delegates rather than leaving them behind; PR 16 took it to **172** — the `MultiplayerClient` and `HostManager` imports and the `sessionUI` alias were orphaned by the session move and deleted rather than left, and the rewritten `test_relayUrl.js` took a fourth, against three new `catch (e)` warnings in `StorageHelper.js`)* | low — noise, but the kind that decays into "green means nothing" | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. Open, and logged rather than tolerated for exactly that reason. `refactor.md` §6 PR 11 specifies `'no-unused-vars': 'warn'` and that is what shipped; making it an error today would fail CI on 178 pre-existing findings, and `--max-warnings 178` would freeze a number nobody chose. Most of the test half is destructured imports a test stopped using — PR 32 restructures the tests and owns it. The `src/` half falls out of Phase 3 and 4 as each file is split. Found by PR 11. **CI runs `lint:ci` (`eslint . --quiet`, errors only)** — GitHub renders every ESLint warning as a check annotation, so the plain script turned one green run into 178 warning annotations and buried anything that mattered. `npm run lint` locally still shows all of them **PR 31 delivered the test half and the count is 149, not the 172 this row's title says.** The migration hoisted every `require` into a real `import`, so a destructured import a test stopped using is now a lint warning in a file the linter parses as a module; the D-47 rewrite took two more with it. **Reassigned PR 31 → PR 33 → PR 34**, because what is left is almost entirely the `src/` half and PR 33 is the last sweep-up slot. It stays a row rather than being closed with a `--max-warnings 149` ratchet: freezing a number is what this row objects to, and a ratchet set mid-refactor would fail CI on PR 33's and PR 34's own churn. |
| D-70 | **Mobs have no buoyancy, no swim and no drown, and PR 23 made that visible.** Fixing **D-56** correctly made water (46) and lava (47) passable; the old test counted them solid, so land mobs stood on the surface of oceans and lava lakes. Nothing replaces that behaviour — `_findSpawnPosition` now scans through the water column and returns seabed+1, and `_resolveAxis` no longer stops a mob at the waterline, so mobs sink and accumulate on lake and ocean floors | medium — player-visible, and it trades one wrong behaviour for another rather than regressing from a correct one. There is no drowning path and no lava-damage path either (`mobMovement.js:259` is a `// For now, just snap` TODO), so nothing removes them once they are down there | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. Open. Found by PR 23's adversarial pass, which traced the D-56 fix through the whole mob subsystem instead of stopping at the four changed sites. **Deliberately not fixed in PR 23 — decision 51:** reverting means hand-writing the exception list the PR exists to delete, and buoyancy/swim/drown is a gameplay feature, not a table fix. **PR 34** already owns D-68's other content half (the five biomes with no mob) under decision 48 |
| D-86 | **PR 31 migration residue — four inert findings the runner swap created.** (1) `process.exit(0)` is a throw now, so it unwinds through six files' own error tails and each prints a crash banner on every GREEN run (`chunkStorage`, `createEntity`, and the four relay suites) — and `integration/server.test.js:574` / `sessionDiscovery.test.js:521` therefore run `cleanup()` **twice** on success. (2) Seven files' `src/` imports were hoisted above the `global.*` stubs they were deliberately written after; checked module-by-module, no target reads those globals at evaluation time, so the ordering guarantee is now unasserted rather than broken. (3) `test/unit/ui/pageLoad.test.js:77-80` scrapes `#id` tokens out of `test/e2e/saveLoad.js`'s raw **text, comments included** — a `#foo` placeholder written in prose turns a unit test red. (4) `saveLoad.js`'s `await import()` needs Node ≥ 22.7 module auto-detection; below that the harness dies on "Cannot use import statement outside a module" | low — every one is inert today. One row rather than four silent tidies, because (1) is real code executing on the success path that never executed before and (3) is a trap with no warning anywhere | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. Open. Found by PR 31's adversarial pass. **Deliberately carried:** fixing (1) means editing six migrated bodies in the PR whose whole safety argument is that the bodies are **verbatim** (decision 60), and the verdict is unaffected — a genuine crash leaves no exit signal at all, which `legacy()` reports as a failure |
| D-89 | **`WorldStep.js:219-221` swallows every per-frame mob exception after frame 10.** The mob update is wrapped in a `try/catch` that only `console.error`s while `state.frameCount < 10`; after that a throw is silent and mobs simply stop updating for the rest of the session | low — no live fault, and the throttle exists for a good reason (a per-frame log is a per-frame log). It is a row because **D-77 just put a renderer inside that call**, so the one subsystem whose first execution is untested by definition is also the one whose failures are invisible | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. Open. Found by PR 33's D-77 trace. **Not changed in the D-77 commit** — widening what a catch reports is an observability change, and making it in the commit that turns the subsystem on would mean the run that proves the fix is also the run that changed how failures surface. The shape that fixes it without a per-frame log is a one-shot latch: report the first throw after frame 10 and then go quiet |
| D-90 | **Three inert findings PR 33's own changes made visible.** (1) `src/ui/css/responsive.css:163-167` declares a **3-column** mobile crafting grid that has never rendered — `screens/crafting.css` is import #28 to responsive's #21 at equal specificity, so its 9-column rule wins, and its *non-media* `gap` beats responsive's *media* `gap`. Worse, `crafting.css:237`'s "348px" arithmetic is only correct **because** responsive's `gap` is dead, so reviving the rule silently invalidates **D-81** (2)'s rationale. (2) `Bootstrap.js:178-181` clears `chunkManager._uvLookupCache` on a texture-resolution change but not `_meshTablesCache`, and `ChunkMeshCoordinator.js:174` returns early once `uvFallbackSize > 0` — so the worker keeps the **old** atlas's tile size forever after a resolution swap (≤5%, and only for blocks with no atlas entry). That coupling is new: **D-74** made the payload atlas-derived. (3) `ChunkMeshBuilder.js:325-338`'s mixin collision guard has **no regression test** — it was proved by hand in three colliding configurations and its own comment points at `test/unit/meta/globalCollisions.test.js`, which has never mentioned mixins | low — all three inert. One row rather than three silent tidies because (1) is a **second** live-CSS-order defect on the very grid D-81 (2) is about, and it is a content call: somebody designed a 3-column phone layout and somebody else designed a 9-column one | **PR 35** | **Reassigned PR 34 → PR 35 when PR 34 landed without reaching it** — a row pointing at a closed PR is the "documented and unowned" state the rule at the top of this file forbids, and `refactor.md` §15 is the slot that now owns the mob subsystem's content and the refactor's residue. Open. Found by PR 33's adversarial pass. **Not fixed in PR 33:** (1) means choosing between two mobile layouts, which is a UI decision with no test that can tell right from wrong — the same line **decision 58** drew; (2) and (3) are in files PR 33 had just split and proved equivalent, and adding a cache-invalidation path to that diff forfeits the proof |
| D-91 | **`./sync.sh --dry-run` cannot exercise the texture branch, so the one path a first deploy needs is the one the dry run never reaches.** Under `--dry-run` the `remote` helper returns 0 unconditionally, so `! remote "test -f …/manifest.json"` is always false and the run always prints "Textures unchanged — skipped". The 118 MB / 3,370-file upload onto a clean box is therefore **unverified by the only executable check this project has for `sync.sh`** | low today, and the severity is entirely about *when*: it is exactly the branch the **first** real deploy takes, and by **decision 20** that deploy happens once, after the whole rewrite, against a box nothing here has ever reached | **PR 10** (post-deploy) | Open. Found by PR 33 while running `--dry-run` as its final gate. **Not fixed:** making `remote` return a *scriptable* result under `--dry-run` means the dry run starts simulating remote state, and a simulation that disagrees with the box is worse than an honest gap. Sits with **D-12**, the other row that says "revisit after the first real deploy" |
| D-93 | **Three mob animations move the group and nothing survives the frame.** `mobRenderer.js:115` sets `entry.group.position` from `mob.position` every frame **immediately before** `entry.animator.update()`, so `dissolveDeath`'s upward float, `hurtReaction`'s recoil push and `crumbleDeath`'s sink are overwritten on the next frame and render as a constant sub-centimetre offset instead of motion. Separately, one-shot animations are truncated one frame early for the mirror-image ordering reason — `mobAI.js:170-173` flips ATTACK → CHASE before the renderer runs, so `lungeAttack`'s final recovery frame never renders | low — pre-existing and cosmetic; `_resetToInitialPose()` runs every frame so nothing leaks, which is exactly why it went unnoticed while mobs were invisible | **PR 35** | Open. Found by PR 34's adversarial pass while proving **D-92**. **Not fixed in PR 34:** the cure is to let an animation own the group transform for the frame it is playing, which is a change to the render/update ordering contract between `mobManager`, `mobRenderer` and `mobAI` — three files, in the PR that also deleted 3,788 lines. The comments in `deathAnim.js` and `mobAI.js` were corrected to say what is actually true rather than what the animation intends |
| D-94 | **`sync.sh` runs `npm ci` in `server/` BEFORE the step that resolves node, so a first deploy can fail on a box where `npm` is not on a non-interactive PATH.** The "Server dependencies" step is `remote "cd /var/www/html/server && npm ci --omit=dev"`; the "Resolving node" step that creates the stable `~/.local/node` symlink runs **after** it. A non-interactive `ssh host cmd` does not source Debian's default `~/.bashrc` (it is guarded by the not-interactive early return), so if node lives only under `~/.local/node-v*-linux-x64` and is added to PATH by an rc file, `npm` is not found and `set -e` aborts the deploy **after the web root has already been replaced** — site up, relay dependencies missing | medium as a first-deploy risk, and unverifiable from here: it depends entirely on how `dadmin`'s PATH is set on the box. If node is installed system-wide or via a profile that non-interactive shells read, it never fires | **PR 10** (post-deploy) | Open. Found by reading the deploy path end to end when the owner asked what a first deploy needs, 2026-07-31. **Not fixed blind:** the correct fix is to move the node-resolution step above `npm ci` and have it export the PATH for the dependency install too, but reordering a deploy script that has never been executed, against a box whose shell configuration nobody here has seen, is guesswork of exactly the kind **DEPLOY.md** §3.1 warns about. It sits with **D-12** and **D-91** as things the first real deploy settles. **CONFIRMED BY THE FIRST DEPLOY, 2026-07-31 14:17 UTC, and fixed the same day.** It fired exactly as written: `journalctl -u cuubz` showed `ERR_MODULE_NOT_FOUND: Cannot find package 'ws' imported from /var/www/html/server/index.js`, restart counter 68, `Restart=on-failure` retrying every 5 s. The prediction "unverifiable from here — it depends entirely on how `dadmin`'s PATH is set" is now verified: node on that box lives only under `~/.local/node-v*-linux-x64` and is not on a non-interactive PATH. The fix is the one this row specified: "Resolving node" is **step 4** and "Server dependencies" is **step 5**, the install `export PATH="$HOME/.local/node/bin:$HOME/.local/bin:$PATH"` — the same path the unit file's `Environment=PATH` names, so dependencies are installed by the interpreter that will run them — and a `command -v npm` guard turns exit 127 into a message naming the PATH it searched. Step 8 now asserts `server/node_modules/ws` exists, because the previous verification block passed cleanly over a relay that could not boot. **Verified by a real deploy the same day**: `Resolving node` printed `~/.local/node -> node-v22.22.0-linux-x64 (v22.22.0)` and `Server dependencies` printed `added 1 package … npm 10.9.4 / node v22.22.0`, and the relay's next boot got past the `ws` import. Node on that box is exactly the case this row predicted — `~/.local/node-v22.22.0-linux-x64` only, and the non-interactive PATH is `/usr/local/bin:/usr/bin:/bin:/usr/games` |
| D-95 | **The relay on 10.0.30.160 is installed as `cuubz.service`; every line in this repo says `cuubz-relay`.** `journalctl` from the first deploy reads `cuubz.service: Scheduled restart job` with `Description=Cuubz Multiplayer Relay Server` and `SyslogIdentifier=cuubz-relay` — so the unit was installed by hand under a different name before `sync.sh` existed, and **`DEPLOY.md` §7 line 399 recorded "Service unit name: `cuubz-relay`" from the repo filename without ever having looked at the box.** `sync.sh` step 7 `cmp`s against `/etc/systemd/system/cuubz-relay.service`, finds nothing, installs it, `daemon-reload`s and starts it — leaving **two enabled units with the same `WorkingDirectory` and the same `MATCHMAKING_PORT=8765`** | medium. Not a crash so much as a coin flip: whichever unit loses the race to bind 8765 gets `EADDRINUSE` and crash-loops on `Restart=on-failure` forever, and which one loses depends on boot order. The static site is unaffected, so the symptom is "multiplayer works, sometimes" | **PR 10** (post-deploy) | **Closed 2026-07-31 — owner ruling (decision 69): the REPO moves to `cuubz.service`, the host is not touched.** `diff` of `/etc/systemd/system/cuubz.service` against the repo's `cuubz-relay.service` was **empty** — byte-identical content, the filename the entire difference — so this was a naming choice, not a drift to reconcile, and the cheaper direction wins: renaming the repo needs no root, no `daemon-reload` and no downtime, while renaming a running enabled unit needs all three. `git mv cuubz-relay.service cuubz.service`, and the unit-name references in `sync.sh`, `DEPLOY.md` and `multiplayer.md` followed. **Three things deliberately NOT renamed**, because they only look like the unit name: the relay hostname `cuubz-relay.thehomelabguy.com`, `SyslogIdentifier=cuubz-relay` (the journal tag — changing it would break continuity with every log line written before today), and `cuubz-relay[<pid>]` inside quoted journal excerpts, which are historical records. `refactor.md` and `PR34_HANDOFF.md` keep the old name for the same reason: they describe what a past PR did. **The step 7 guard is inverted rather than deleted** — it now refuses (exit 91) if a `cuubz-relay.service` is found on a host, since that is the pre-rename unit declaring the same port, and `cuubz-relay.service` stays in the pre-extract `rm` list so the stale copy is cleared from any web root deployed before today. Found by reading the first deploy's journal |
| D-98 | **`sudo` is not installed on 10.0.30.160, so `sync.sh` can never restart the relay there.** `dadmin` is `uid=1000 … groups=1000(dadmin),100(users)` — no `sudo` group — and `dpkg-query` finds no `sudo` package at all; `/usr/bin/sudo`, `/usr/sbin/sudo` and `/bin/sudo` do not exist. Only `/usr/bin/su` does. Every `sudo -n` in step 7 therefore fails with `command not found`, the step exits 90, and the deploy takes the warning path **on every single run** — which is D-2 (the relay silently running old code) restored as a permanent condition, just with a warning printed over it | medium. The deploy is otherwise complete and correct, and the failure is loud rather than silent — but "loud every time" decays into "ignored", which is the exact mechanism this file exists to prevent | **PR 10** (post-deploy) | **Accepted, not fixed — owner ruling 2026-07-31 (decision 70): leave the host root-only and restart by hand.** Installing `sudo` plus a NOPASSWD sudoers rule was the alternative and was declined; a security posture change on a production host is not a bug fix. So this row stays as a **standing operational condition** rather than a defect to close. What changed instead is that the script and the runbook stop lying about it: step 7 distinguishes "sudo is not installed" (exit 92) from "sudo is installed but not passwordless" (exit 90) — the old single line sent the reader off to run four `sudo` commands that cannot exist here — and prints the `ssh -t "su -c '…'"` form that does work. `DEPLOY.md` §3.1.1 and §5 say the same. **The mitigation that makes this survivable** is worth stating plainly, because it is what actually recovered the relay on the day: `Restart=on-failure` + `RestartSec=5` means a relay that is **down** picks up newly deployed code within five seconds with no root at all. The manual restart is only needed when the relay is **healthy**, and therefore has no reason to restart itself. Found during the first deploy |
| D-99 | **An orphaned relay held port 8765 from 14 July until the first deploy, and it was invisible behind D-94.** `pid 560` — `node index.js`, started `Tue Jul 14 02:59:35`, `cwd -> /var/www/html/server (deleted)`, cgroup `user.slice/…/session-c14.scope` — was a hand-started process in a login session, **not** owned by `cuubz.service`. It was serving pre-PR-33 CommonJS code from a directory that no longer existed. While the unit was dying on `ERR_MODULE_NOT_FOUND` it never got as far as `listen()`, so the conflict was masked; the moment **D-94** was fixed the unit's failure changed to `EADDRINUSE: address already in use :::8765` and it kept crash-looping. One `kill 560` (the process was `dadmin`-owned, so no root needed) freed the port and `Restart=on-failure` brought the unit up unaided | medium at the time, closed now. It is logged because of the **shape**: a fix that "works" can be fully masked by an unrelated squatter, and the symptom simply changes to another `Restart=on-failure` loop | **PR 10** (post-deploy) | **Closed 2026-07-31** by killing the orphan; `systemctl is-active cuubz.service` → `active`, journal `[RELAY] Listening on port 8765`, and a live smoke test returned `{"status":"ok","activeSessions":0}` from `/health` plus a `WELCOME` frame from `/matchmaking`. **The standing risk this leaves:** nothing prevents another hand-started relay. `sync.sh` does not check for a stray listener on 8765, and adding that check is the natural follow-up — a deploy that cannot bind its own port should say so rather than let systemd retry forever |
| D-100 | **A Windows checkout ships CRLF text to a Linux host, and it defeats step 7's `daemon-reload` guard.** This repo has `core.autocrlf=true` and had **no `.gitattributes`**, so git stores LF and checks out CRLF; `sync.sh` packs from the working tree. `/var/www/html/cuubz.service` therefore arrived with CRLF while `/etc/systemd/system/cuubz.service` had LF, and `diff` reported **all 56 lines changed on byte-identical text** (`diff <(tr -d '\r' …)` is empty). Step 7's `cmp -s` exists precisely so a normal deploy does **not** reinstall the unit and reload systemd — with CRLF it can never match, so it would do both on **every run**. Second order, and worse: a CRLF unit installed into `/etc` parses only because systemd's `strstrip()` happens to drop the trailing `\r` from each value; that is a property of the parser, not a guarantee, and an `ExecStart` carrying a stray `\r` is a miserable thing to debug | medium, and **entirely invisible on 10.0.30.160** — D-98 means step 7 exits before it ever reaches the `cmp`, so this would have surfaced for the first time on whichever host got sudo working. Found only because the post-deploy `diff` was run by hand | **PR 10** (post-deploy) | **Fixed 2026-07-31.** New `.gitattributes` pins `*.sh` and `*.service` to `eol=lf`, and the working tree was renormalised. Verified end to end: `file` reports the deployed unit as plain UTF-8 text and `cmp` against `/etc` is now **identical**. **Scope is deliberate** — `.sh` and `.service` are the files whose bytes a shell interpreter and a systemd config parser read as structured text. JS/JSON/HTML are left alone: Node, browsers and Vite are CRLF-tolerant, and `dist/` is generated LF by the build regardless. **The general lesson is the one worth keeping:** every check `sync.sh` makes about "has this changed?" compares bytes across an OS boundary, and a checkout setting nobody thinks of as part of the deploy can silently invert the answer |
| D-96 | **The first deploy served the unbuilt source tree, and all four post-deploy checks passed on it.** The browser reported `Uncaught TypeError: Failed to resolve module specifier "three"` — the signature of the **repo-root** `index.html`, whose `<script type="module" src="/src/index.js">` reaches `import * as THREE from 'three'` with no import map and no bundler. That is `sync-legacy.sh`'s output, not `sync.sh`'s: it carries `--exclude='dist'` (**D-4**) and ships `src/` into the web root (**D-13**). **The verification block could not tell the difference**: `index.html` existed, it named a `.js`, and `/src/index.js` was on disk, so all three assertions passed over a site that cannot execute a single line | medium — total client-side failure, and the deploy that caused it would have reported success | **PR 10** (post-deploy) | **Fixed 2026-07-31.** Step 8 asserts the entry is a content-hashed `/assets/` path (what Vite rewrites it to, and the one thing an unbuilt tree can never produce) and that `/var/www/html/src` does not exist. Both are hard failures, not warnings. **The script was never the defect — running the wrong script *last* was.** The recovered sequence, from `${BACKUP_DIR}` timestamps: `sync.sh` ran **four times** between 14:05 and 14:07 UTC (four backups, the first 4.9 MB for a full source web root, the next three 308 kB for a correct `dist`-only one), aborting each time on **D-94**; `sync-legacy.sh` was then run as a fallback and re-extracted the whole repo over the top. `assets/` survived from the `sync.sh` runs because `sync-legacy.sh` excludes `dist`, which is how a hashed bundle came to sit next to an unbuilt `index.html`. **Reading mtimes to reconstruct this is a trap and cost a wrong conclusion in the diagnosis:** `tar` preserves mtimes, so every extracted file carries the time it was written on the *source* machine, not the time it landed — the web root was full of 14:04 files that arrived after 14:07. `${BACKUP_DIR}` timestamps are the reliable record because `sync.sh` stamps those itself. `sync-legacy.sh` has now outlived the one release cycle its header granted it and should go with **PR 35**; until then this check is what stops it silently winning. Found by the first deploy |
| D-97 | **`sync.sh`'s pre-extract `rm` list missed six repo files, so a public web root kept serving them after a "converging" deploy.** The list was written from what the repo looked like at PR 10 and never reconciled against what `sync-legacy.sh` actually leaves. Verified against the real web root: `.github/` (CI workflows), `.prettierignore`, `vitest.config.js`, `sync.sh` and `sync-legacy.sh` — **both of which contain the host IP, the deploy user and every remote path** — and `eslint.config.mjs`, which the list named `eslint.config.js`, an extension this repo has never used. Apache also has `Indexes` on (`http://10.0.30.160/textures/` returns `Index of /textures`), so these were not merely reachable at a guessable URL, they were **browsable** | medium. Not a code defect — a **D-13** regression through the gap between "the list of things we delete" and "the list of things we ship" | **PR 10** (post-deploy) | **Fixed 2026-07-31.** All six added. `docs/`, `sounds/` and `test_worldgen.js` were deliberately **left out**: they pre-date every deploy (May–June), they are not in this repo, and the list removes only what this script puts there — same argument as `textures/`. Post-fix the web root is `assets/ index.html server/ shared/ textures/ cuubz-relay.service`, and `/BUGS.md` now 404s. **The directory-index finding is separate and open** — `Indexes` should come off the vhost, which is a root change on the box, not a repo change |

## Fixed

| **D-116** | **A chunk the host believed it had delivered could never be sent again, and a joining client's spawn area is precisely where that assumption fails.** `ChunkStreamEntry.markClean` records a chunk as delivered when the payload is *queued*, and `buildStreamQueue` will not look at that chunk for that player again — delivery was assumed, never confirmed, and no path existed to ask for a second copy. The trigger is the host's LOADING timeout: a joining client's spawn area is usually terrain the host has **not** generated (`memoryCache` covers a radius around the *host's own* player), so the streamer's generator returns `null` for it, and after `loadingTimeout` (8 s) `tick()` forced the entry to LOADED **with `data` still `null`**, sent that empty payload — which the client drops on `initChunkStreaming.js`'s `if (!data.data) return` — and marked it delivered to every player in `playerRefs`. The blocks, generated seconds later, were never sent. Walking far enough created *fresh* entries, whose generation had long since caught up, which is why the symptom read as "nothing loads unless you move outside the initial spawn range". Measured against a real relay socket before the fix: **113 CHUNK_DATA messages, 113 of them empty, client left with 0 of 49 chunks in its render radius.** Three more losses shared the same dead end and are closed by the same reply path — `onChunkStreamed` drops the send outright when the game socket is not connected; the client evicts chunks beyond `renderDistance + 2` in `ChunkGenerator._updateVoxelRegion` and walking back into them asked nobody; and the host's stream radius (6) is narrower than the client's render distance (8), so the outer ring was never streamed at all | high — a joining client stands in a void at spawn, which is every multiplayer session's first thirty seconds | **PR 37** | **Fixed 2026-08-05.** The timeout retries instead of fabricating: the entry drops back to UNLOADED so the next tick re-runs `loadChunk` and the `_ensureChunkInMemory` behind it (`_queueGeneration` already dedupes, so this does not storm). `tick()` also refuses to send — and so to mark delivered — an entry with no blocks. **The assumption is now answerable:** `CHUNK_REQUEST` (28th message type) carries the chunks a client is missing, `server/session.js` forwards it to the host with the asking `playerId` attached and a 512-chunk cap, and `ChunkStreamer.requestChunks` clears `streamedTo` so the ordinary queue re-sends. A request also overrides the load radius, which is what closes the 6-vs-8 ring. The client half is `src/multiplayer/ChunkResync.js` — nearest-first, 64 per ask, a 5 s per-chunk cooldown, and it forgets a chunk once it arrives so a later eviction can be asked for again. `test/integration/chunkResend.test.js` drives both failures through a real relay with the real `initChunkStreaming` on both ends and is red on the pre-fix tree (113 empty payloads, 0/49 chunks); `test/unit/multiplayer/chunkResend.test.js` holds the invariants against a stubbed clock |
| **D-114** | **Seven items shipped a texture that was not an item icon, and a file-existence audit cannot tell the difference.** `coal`, `bone`, `rotten_flesh`, `rabbit_hide`, `rabbit_meat`, `raw_venison` and `corrupt_fang` were full-bleed 128×128 squares — a diagonal-stripe gradient with a crude blob centred on it — so the hotbar drew a coal-black square, a cream square, a pink square. **These are the same "coloured boxes" as D-111 and they survived that fix**, because D-111 removed the atlas's *invented* colours while these are real files that load correctly: every one of the four seams `textureCoverage.test.js` checked (registry→disk, manifest→face, `NAMED_ITEMS`↔manifest, manifest→disk) passes on a placeholder. The audit asked *"does a texture exist and resolve"* and never *"is the thing on the other end an icon"*. Two independent tells, neither of which the gate looked at: they are **100.0% opaque** where all 97 genuine icons are 13–57% (widest is `leather` at 56.6% — a gap, not a gradient), and they are the **only 7 of 224 item files with no `_n`/`_s` companion maps**, i.e. they never came from the texture pack at all. Found only because a question about the resolution of the D-112 icons prompted measuring the directory instead of trusting it | medium — the user-visible symptom is identical to D-111, so it reads as "the fix did not work" rather than as a second cause | **PR 36** | **Fixed 2026-08-02.** All seven redrawn by `scripts/generate-item-textures.js`, which grew five shapes for them (`lump`, `bone`, `ragged`, `pelt`, `fang`) and reuses `steak` for `raw_venison` so it reads as a set with `cooked_meat`. Coverage is now 13–56.6% across all 104. **The gate gained the seam that was missing:** `no item texture fills its whole tile` rejects anything over 90% opaque — deliberately not a tuned threshold but a wide gap in a bimodal distribution. Confirmed to fail against the original `coal.png` restored from git |
| **D-115** | **The D-112 icons were authored at 16×16 against a 128×128 pack, so the atlas upscaled them 4× and they read as placeholder art.** No malfunction — `_loadItemTexture` draws every source into the 64×64 cell and the UV maths never sees the source size, so a 16px, a 128px and a 256px file all land correctly. The cost was purely visual and purely comparative: beside the pack's per-pixel noise and directional shading, flat 4px-block colour fills look like something nobody finished. Measuring the directory settled what guessing would not have — 217 files at 128×128, 7 at 256×256, and only the 14 new ones at 16. **Raising the file size alone would have fixed nothing**: the same design in bigger blocks is pixel-identical once the atlas samples down to 64. The gap was detail density, not dimensions | low — cosmetic, and invisible until an item sits next to pack art in the same hotbar row | **PR 36** | **Fixed 2026-08-02.** The 16×16 grid stays the authoring format — it is what keeps these hand-editable — and the generator now renders it at 8× into 128×128, adding three shading layers at render time: a directional gradient over the sprite, per-design-cell brightness jitter so flat fills stop reading as flat, and per-pixel grain. All variation comes from `hash2(x, y, seed)` seeded off the item name, never `Math.random()`, so re-running is byte-identical and stays out of `git status` — verified by md5. Alpha stays hard 0/255, which is what keeps D-114's coverage assertion meaningful. **Shape fidelity is still 16×16-derived and the silhouettes remain coarser than the pack's; detail was the larger half of the gap, not all of it.** Gate: `every item texture is square and at least the atlas tile size` — phrased against `ATLAS_TILE` rather than `== 128` so it states the rule that matters (never upscale) instead of encoding today's pack. Confirmed to fail against a 16×16 `iron_ingot.png` |
| **D-111** | **Twenty-nine block ids drew a flat coloured square with the wrong block's initial on it, and they were the only ids that *couldn't* self-correct.** `ItemTextureAtlas._buildItemRegistry` carried a hardcoded list of 29 block ids and painted each one via `_drawBlockPlaceholder` — `fillRect` in a colour from an inline table, plus `fillText` of `name.charAt(0)`. The comment above it said "the actual block texture will be drawn from the block atlas at render time"; **nothing did that.** Compounding it, the ids were from a registry generation that no longer exists: against today's 193-entry `BLOCK_REGISTRY`, id 4 was labelled "Grass Block" but is `andesite`, 32 "Wood Log" but is `deepslate_gold_ore`, 33 "Leaves" but is `deepslate_diamond_ore`, 34 "Planks" but is `deepslate_copper_ore`, 45 "Glowstone" but is `raw_iron_block`. The inversion is the part worth remembering: `Hotbar.renderItemIcon` tries the item atlas **first** and only then falls back to the block atlas, so the 29 blocks *with* a placeholder rendered wrong and the other 163 *without* one rendered correctly — **the bug was carried entirely by the code written to prevent it.** Reported as *"items in my inventory that don't have textures, only colored boxes"* | high — cobblestone, andesite, deepslate, stone bricks and every ore in that id range are among the first blocks a player picks up | **PR 36** | **Fixed 2026-08-02.** `_drawBlockPlaceholder` and the id table are deleted; there is no longer any code path that invents a colour for a block. Block entries are derived from the block atlas's own `tileMap` (so the two atlases cannot disagree about which blocks exist) and `_blitBlockTile` copies the real pixels canvas-to-canvas out of `PBRTextureAtlas.diffuseCanvas` — which means colour multipliers and grass-style overlays are already baked in and the icon matches the placed block. `ItemTextureAtlas` takes the block atlas as a constructor arg, making the `initScene.js` step-4 ordering (block atlas awaited before item atlas) load-bearing rather than incidental. `renderItemIcon`'s two byte-identical string/number branches collapse to one lookup; its block-atlas fallback stays, now genuinely reachable only when no block atlas was supplied. Gates: `test/unit/engine/itemTextureAtlas.test.js` (12 assertions, **9 confirmed to fail against the pre-fix file**) — including that the atlas canvas receives *zero* `fillText` and `fillRect` calls, which is the most direct available statement that the lettered box is gone |
| **D-112** | **Fourteen of the 104 `NAMED_ITEMS` had no atlas slot and no PNG anywhere under `textures/`, so their inventory slot rendered empty.** `ItemTextureAtlas` held a hand-maintained 92-entry array against `ItemDefinitions.js`'s 104, and the two had drifted. Missing: `copper_ingot`, `iron_ingot`, `gold_ingot`, `netherite_ingot`, `diamond`, `leather`, `corrupt_crystal`, `iron_ore`, `gold_ore`, `apple`, `golden_apple`, `bread`, `berry`, `cooked_meat`. Not decorative — `iron_ingot` alone has nine call sites, and `getBlockDrop` hands out `apple`, `leather` and `corrupt_crystal`. With no slot, `renderItemIcon` fell through **all three** of its branches and left the canvas untouched; even with a slot the 404 path drew a silent flat grey square, indistinguishable from a dark item texture. Two entries (`blue_egg`, `brown_egg`) existed in the atlas but in no other file in the repo | high — a blank hotbar slot reads as an inventory bug, not a texture bug, and sends the search to the wrong system | **PR 36** | **Fixed 2026-08-02.** The array is gone. Named items come from `textures/items/manifest.json`, generated from `NAMED_ITEMS` by `scripts/generate-manifest.js`, so adding an item to `ItemDefinitions.js` now adds it to the atlas and a missing PNG is reported at generate time instead of 404-ing at runtime. `ITEM_TEXTURE_OVERRIDES` holds the only two entries whose file is not named after their key (`quest_key`→`disc_fragment_5`, `compass`→`compass_00`). The 14 PNGs are generated by `scripts/generate-item-textures.js` (idempotent; `--force` to overwrite), which shares one silhouette across each tier set so the four ingots, both apples and both raw ores read as sets. **They shipped at 16×16 and were re-cut to 128×128 the same day — see D-115.** The 404 path now draws a visible "?", logs, and records the key in `atlas.missingTextures`. Gates: `test/unit/meta/textureCoverage.test.js` walks all four seams a texture crosses (registry→disk, manifest→icon-able face, `NAMED_ITEMS`↔manifest set-equality **both directions**, manifest→disk), and `itemTextureAtlas.test.js` asserts `missingTextures` is empty after a real `buildAtlas()` |
| **D-113** | **`sync.sh` would never have shipped the new item manifest to an already-provisioned host.** Step 6 skips the 118 MB texture upload unless `--textures` is passed *or* the host lacks `textures/blocks/manifest.json`. That probe was complete while `blocks/` was the only manifest — the item atlas built its list from an array inside the client bundle, so items needed nothing on disk. D-112 made `items/manifest.json` a build artifact, at which point a host provisioned before it existed passes the blocks-only probe, skips the upload, and 404s the item manifest. The failure is asymmetric and so reads as something else entirely: **every block icon renders correctly and every named item becomes a "?"** | medium — found while fixing D-111/D-112, not in the field; it would have turned a fix into a fresh visual regression on the first deploy after it | **PR 36** | **Fixed 2026-08-02.** The probe asks for both manifests, so the next deploy is self-correcting rather than needing someone to remember `--textures`. Step 8's post-deploy checks gained the matching assertion, naming the "?"-placeholder symptom so the log says what a reader would otherwise have to infer |
| **D-110** | **Pack aggro was an unbounded chain reaction, and nothing anywhere capped how many mobs could fight the player at once.** `triggerPackAggro` is called every tick from `_stateChase`, and it flipped every idle/wander same-type mob inside `packRadius` straight to CHASE — **no line-of-sight test and no distance-to-player test**. Those recruits ran `_stateChase` on the next tick and recruited *their* neighbours, so aggro propagated hop by hop across the entire same-type population: `packRadius` bounded a single hop and nothing bounded the chain. Both `packAggro` mobs (`corrupt_wolf` w:25, `corrupt_wisp` w:20) are high-weight spawns in `badlands`, so in practice a single sighting pulled in wolves that were tens of blocks away, through terrain, with the player never in their senses. Two multipliers on top: (1) `loseInterestRange` was **2x or worse** every mob's `aggroRange` (wolf 20→40, wisp 16→32, golem 15→32), so pursuers were gained at half the distance it took to shed one and **retreating did not work** — they accumulated; (2) spawning had `mobCap: 60` / `mobsPerChunk: 8`, **no hostile-specific budget**, and no minimum spawn distance, so hostiles could materialise inside their own aggro range. Reported as *"too many enemy mobs with crazy distance spotting … attacked by every enemy on the screen at the same time"* | high — it is the headline complaint about how the game plays, and the three causes compound rather than merely coexist | **PR 35** | **Fixed 2026-08-02.** Four guards on the cascade, the load-bearing one being that **recruits do not recruit** (`Mob.recruitedByPack`, cleared on disengage) — propagation now radiates one hop from a mob that aggroed on its own senses and stops. The others: a recruit must be within `PACK_RECRUIT_RANGE_FACTOR` (1.25) of its **own** `aggroRange` of the player, `MAX_PACK_RECRUITS` (2) bounds one trigger, and `MAX_ENGAGED_MOBS` (**2**) is a hard ceiling on concurrent CHASE/ATTACK, re-counted as recruits are added. Over-budget mobs stay in WANDER and re-ask every tick, so a freed slot is taken immediately — **the fight stays continuous, it just stops being simultaneous.** Ranges: aggro 20/16/15 → **10/9/8**, lose-interest → ~1.6x aggro (16/14/13), sense 24/20 → 14/12, packRadius 10/8 → 6/5. Spawning: `mobCap` 28, `mobsPerChunk` 3, new `hostileCap` 12 and `minHostileSpawnDistance` 24 — passives are untouched, deer and rabbits are ambience. **The aggro gate now lives in one place:** `mobAI.js` had inlined the range + LOS test while the exported `shouldAggro` sat with **zero callers**; the AI routes through it, so the two cannot drift. Gate: `test/unit/game/mobAggro.test.js`, 20 assertions; 7 of them confirmed to fail against a build with the guards removed |
| **D-109** | **Exit-to-menu kept the rejoin record, so the next page load re-*hosted* the session it should have rejoined — and that is where the duplicate rows came from.** `SessionManager.leaveSession()` never touched `cuubz_last_session`, so a deliberate exit left a record behind and `attemptAutoRejoin` fired on the following load. For `isHost: true` that branch called `client.hostSession(...)`, and `HOST` has exactly one meaning to the relay: *create a session*. It does not look for an existing one. So every reload after an exit minted a fresh session carrying the old one's name, standing beside whatever was already listed, with no host behind it — reload twice and there were three. `SessionRejoin.js`'s manual button had the same branch. Two more, on the same seam: neither rejoin path ever built a `HostManager`, so a re-hosted session ran with **no validation, no rate limiting and no speed check** at all; and a joiner's rejoin arrived under a fresh id, leaving its previous id in the relay's `players` map holding a dead socket until its 120 s heartbeat lapsed — one of four seats, rendering as a frozen twin | high — player-visible, cumulative, and the direct cause of the reported duplicate sessions | **PR 35** | **Fixed.** `leaveSession()` calls `clearLastSession()` and repaints the rejoin panel. The record now carries `playerId` (`normaliseSessionRecord`, `getSessionRecord`), and both rejoin paths call the new `SessionManager.reclaimHostedSession()`, which asserts that id over an ordinary `JOIN` — `server/session.js._handleJoin` matches it against `hostId` and the client is the host of the *original* session again, chunk-streaming rights included. `HOST` survives as a fallback for a record written before `playerId` was in the shape. `attachHostManager()` is extracted from `SessionHosting.js` and called on both reclaim paths. Joiner rejoins call `setPlayerId()` too |
| **D-108** | **`MultiplayerClient.leaveSession()` was an alias for `disconnect()`, which closes the matchmaking socket — so the first exit-to-menu killed the lobby for the rest of the page, silently.** `disconnect()` sets `_matchmakingConn = null`, and `connectMatchmaking()` runs exactly once per page load (`Bootstrap.js:226`). `browseSessions()`, `hostSession()` and `joinSession()` are each guarded on that field, so after one exit all three were no-ops with no error anywhere. **This is all three reported symptoms at once:** Refresh drew nothing, so `#session-list` kept the rows painted before the game started ("duplicates … stale"); clicking one sent no `JOIN` ("cannot actually be entered"); hosting again sent no `HOST`, so the new session never reached the relay ("another one doesn't show up if you create a new one"). Second half, same method: `_connectToGameSession`'s guard was `if (this._disposed \|\| this._gameSessionConn) return` — *any* existing game socket, for any session — so had the `HOST` gone out, the client would have stayed bolted to the first session's socket and the new session would have had no host join it | high — the lobby is unusable after one exit, and the leaked sessions it produces are what a guest browses | **PR 35** | **Fixed.** Leaving a session and leaving the lobby are now different things: `leaveSession()` keeps the matchmaking socket, sends `LEAVE` on it (`sendLeaveSession`, which `server/matchmaking.js` already handled), drops only the game socket, and re-arms matchmaking if it died mid-game. `disconnect()` is unchanged and is still the full teardown. `_connectToGameSession` tracks `_connectedGameSessionId`: same session is a no-op (the reconnect path), a different one replaces the socket. Gate: `test/unit/multiplayer/clientSessionLifecycle.test.js` |
| **D-107** | **A heartbeat timeout announced a deliberate `LEAVE` before reconnecting.** `_setHeartbeatTimeout`'s handler called `disconnect()`, which sends `LEAVE`. A socket that has merely gone quiet — a backgrounded tab, a 30 s stall — is not a departing player, and with D-103 now (correctly) treating a host's explicit `LEAVE` as "destroy this session, do not wait", the mislabelling would tear down live games on a hiccup. It was already wrong before that: `disconnect()` also calls `_cancelReconnect()`, which zeroes `_reconnectAttempts`, so every heartbeat timeout restarted the backoff at 1 s no matter how many had already failed | medium — latent before D-103, load-bearing after it | **PR 35** | **Fixed.** `_dropForReconnect()` tears the socket down without `LEAVE` and preserves the attempt count; `disconnect()` keeps its `LEAVE`. Both share `_closeSocket()` |
| **D-106** | **`maxReconnectAttempts` had no reader, and the one place that tried to disable reconnection produced a 100 ms retry storm instead.** `DEFAULT_CONFIG.maxReconnectAttempts = 10` was declared, documented and consulted nowhere, so every connection retried forever. `connectMatchmaking()` set `_options.reconnectBaseDelay = 0` under the comment "Effectively disables reconnect" — it disables nothing: `_calculateReconnectDelay()` floors its result at `Math.max(100, …)`, so a base of 0 yields **100 ms** and a dead lobby socket reconnected ten times a second, indefinitely. Each reconnect drew a fresh `WELCOME` with a fresh `playerId`, which is what made D-105 fire in practice rather than in theory | medium — wasted sockets and log noise on its own; the delivery mechanism for D-105 in combination | **PR 35** | **Fixed.** The cap is enforced in `_scheduleReconnect()`, `autoReconnect: false` is what "do not reconnect" is spelled as, and the matchmaking socket keeps the ordinary 1 s → 30 s backoff — the lobby genuinely does want to come back after a blip |
| **D-105** | **Every matchmaking `WELCOME` overwrote `_playerId`, so a reconnect silently renamed the player — and a host stopped being the host of its own session.** The relay mints a new `playerId` per matchmaking *connection*, and `_setupMatchmakingHandlers` adopted each one. `_playerId` is the identity the **game** socket joins under (`joinGame` → `sendJoin(this._playerId, …)`) and the identity recorded as `hostId` when the session was created. After a rename, `session.js._handleChunkData` compared the sender against `hostId`, did not match, and rejected the host's own chunk streams as `Non-host … sent CHUNK_DATA` — joiners saw an empty world with no error on either side. Under D-106's 100 ms reconnect storm this was not a rare race | high — silent, and it presents as "the session cannot be entered", which is exactly what was reported | **PR 35** | **Fixed.** The first `WELCOME` wins and later ones are ignored; `setPlayerId()` is the only other writer, used by a rejoin reclaiming its session (D-109) |
| **D-103** | **A hosted session that nobody ever joins is never collected, and `onHostLeave` is a no-op that contradicts its own caller.** (1) `matchmaking.js:105` logged `Host … disconnected — destroying session` and called `onHostLeave`; the implementation at `server/index.js:180-182` only logged `session … stays alive` and destroyed nothing. Two adjacent log lines asserting opposite outcomes. (2) The one real collection path was `_removePlayer` observing the ≥1 → 0 transition, which cannot fire for a session created by `HOST` whose `/session/:id` socket nobody ever opened — and `listSessions()` asked only "is it in the map", so such a session was advertised forever as a row that accepts a click and then does nothing. Reproduced against the live relay: `HOST`, host socket closed, still advertised with `players: 0` ten minutes later. *(The original row also claimed the join→leave path failed to collect; that was wrong — `session.js` schedules a deliberate 30 s grace and the re-check was made at 12 s.)* | medium — player-visible and cumulative; the relay is in-memory, so every abandoned "Host" click added a permanent browse-list entry surviving until a manual restart (**D-98**) | **PR 35** | **Fixed**, along the shape the original row argued for: a grace period, not an unconditional destroy on host-leave. Three timers now end a session — `_claimTimer` (60 s, host never arrived), `_hostGraceTimer` (30 s, host left while guests remain — a hostless relay streams nothing, so it is a countdown, not a steady state) and the pre-existing `_emptyTimer`, which `_removePlayer` skips when the **host** sent an explicit `LEAVE`. `isListable()` / `isJoinable()` replace "is it in the map" for `listSessions()` and `onJoinRequest`; `getSessionInfo()` carries `hasHost` so the lobby can grey out a row in its host's reconnect window. `onHostLeave` destroys a never-claimed session and the two misleading log lines are gone. Gates: `test/unit/multiplayer/sessionLifecycle.test.js`, and `relayMaxPlayers.test.js` now claims each session before reading `/sessions` |
| **D-104** | **The hurt flash gave every mob's eye meshes an `emissive` property their shader has no uniform for, and the first draw call after any hit took down the render loop.** `hurtAnim.js:64` assigned `material.emissive = new THREE.Color(0xff0000)` to any material that lacked one, on the stated theory that it was harmless because `MeshBasicMaterial`'s shader never reads it. The shader does not; **the uniform refresh does.** Three r134's `refreshUniformsCommon` (`three.module.js:24534`) branches on the *material* and then dereferences the *uniform* — `if ( material.emissive ) uniforms.emissive.value.copy(...)` — and `ShaderLib.basic` has no `emissive` uniform. Every mob carries at least one `MeshBasicMaterial`: the eye spheres from `mobModelBuilder.js:52`. So the frame after the first hit on any mob threw `Cannot read properties of undefined (reading 'value')` out of `renderBufferDirect` | **high — the game stops.** This is not a cosmetic glitch degrading one mob; the throw is inside `renderer.render()`, so the whole frame dies and the loop with it. It fires on the second frame of the first hit on the first mob of a session. **It was masked until now by D-101**, which threw in `CombatStep` *before* the flash could ever be applied — fixing the knockback shadow is what let this reach the renderer for the first time | **PR 35** | **Fixed 2026-08-01.** `hurtReaction` now returns early for any material without an `emissive`, so the property is only ever written where the shader has a uniform for it; the matching `_origEmissiveAbsent` branch in `clearHurtReaction` is deleted with the thing it undid. The eyes simply do not flash — they are 0x000000 spheres a few pixels across. **Why the 22 existing D-88 assertions were green through all of this:** they compare emissive state *before* a hit to *after* it, and `emissiveState` deliberately records `'ABSENT'` so an added-then-restored property could not pass — but the crash lives on the frames *during* the window, and the scene is a stub, so no material ever reached a uniform refresh. The new suite closes exactly that: it runs the real `ShaderLib` uniform set through the real failing expression on **every frame of every mob type's hurt window**, with non-vacuity guards that the flash rendered and that the mob owns a basic material at all. Confirmed to fail on the pre-fix file with the production error text |
| **D-101** | **`Mob.knockback()` was shadowed by `this.knockback` and had never executed — every player attack on every mob threw.** The constructor assigns `this.knockback = def.knockback || 0` (`mob.js:31`), an own data property; the class also defines a prototype method `knockback(dx, dz, force)` (`mob.js:170`). An own property shadows a prototype method, so `mob.knockback` was the **number** `0` for deer and rabbit and `1.5` for stone_golem, and `CombatStep.js:63`'s `hit.mob.knockback(...)` threw `TypeError: mob.knockback is not a function` on **every hit**. The method's own body is the proof it was accidental: `force !== undefined ? force : this.knockback` reads the property as its default, so the author expected both to coexist under one name | medium — **three player-visible effects, all silent.** No knockback ever applied; and because the throw happens *before* the two lines after it, `state.firstPersonHand.swing()` never ran (no hand-swing animation on a mob hit) and `blockInteraction._attackOverride = true` was never set, so **attacking a mob also broke the block behind it**. `CombatStep.js:73` catches it behind `frameCount < 10`, so after the tenth frame of a session it is completely invisible | **PR 35** | **Fixed 2026-07-31.** The **method** was renamed to `applyKnockback`, not the property: `def.knockback` → `this.knockback` is the stat, the method reads it as a default, and renaming a field that comes straight off the definition table is how **D-68** and **D-39** happened. One call site changed. A sweep for the same collision class over every `class` in `src/` — own `this.X =` assignment against a prototype `X()` — found **this one and no others**, and the detector was validated by running it against the pre-fix file, where it flags exactly this row. Found by driving the real `MobManager` + `MobRenderer` through a damage cycle in Node |
| **D-102** | **`getRelayUrl()` ignored the host the page was served from, so multiplayer could not work off the public domain.** It returned `${protocol}://cuubz-relay.thehomelabguy.com` unconditionally — no port, correct only behind the reverse proxy that terminates TLS and forwards to 8765. Loading the game from the production server directly, `http://10.0.30.160/`, therefore made every client open `ws://cuubz-relay.thehomelabguy.com` — a **different machine, on port 80**, where no relay listens. The host's `HOST` never reached the relay and the guest's `BROWSE` never saw it | medium — reads as "hosting works but sessions never show up to join", which is exactly how it was reported. **Neither test suite could catch it:** `npm run test:e2e:mp` passes 70 assertions across two browser contexts and `relayUrl.test.js` had 24 — but the e2e harness passes `?relayUrl=` explicitly and the unit test asserted, in as many words, that *"the page hostname is NOT consulted"*. Both exercised everything except the default this defect lives in | **PR 35** | **Fixed 2026-07-31.** If the page hostname is not on `thehomelabguy.com`, the relay is `${protocol}://${location.hostname}:8765` — the relay on *this* machine, port named explicitly because nothing proxies 8765 off the public domain. The public hostname keeps byte-identical behaviour, so **the only inputs whose result changes are ones that were already broken**. `relayUrl.test.js` gained a Group 1b for the new arm and every pre-existing case was given an explicit public `hostname`, since omitting it used to be equivalent and now selects the other branch. **What this is NOT:** the per-game relay subdomain **D-45** deleted from that test — the hostname is consulted only to ask "is this the public domain?". Verified against the live relay by hand first: `HOST` → `HOST_CREATED` and a second socket's `BROWSE` returns that session, so the relay was never at fault |

| ID | What | Severity | Fixed in | Notes |
|---|---|---|---|---|
| **D-41** | **Five code paths created a character or a world and none of them agreed.** `CharacterScreen`'s modal, the browse-panel and host-panel inline forms and the host world form (`LobbyForms.js`) plus `WorldScreen`'s modal — the row said three. They disagreed on ten things, including whether they checked `canCreateMore()` at all, **opposite null-manager polarity** for the same guard, one empty-name string missing its trailing period, one Enter handler missing `preventDefault()`, and two byte-identical seed validators | low — no data risk; every path reached the same validated manager method. The cost was that a change to character creation had five places to make it and one would be missed | **PR 26** | Unified into `src/ui/forms/createEntity.js` — `canOpen` / `submitCreate` / `syncCreateButton`, with `LobbyForms`'s already-null-safe `setBanner` **promoted rather than reimplemented**, which fixed the two unguarded `showError`s for free. **Decision 55** rules the greyed-out button, on the two modals; **decision 59** keeps the three lobby toggles enabled and banner-ing, because they also *close* their form and a disabled button dispatches no click. There was **zero** coverage of any path before this; `test_createEntity.js` is 78 assertions, every one proved non-vacuous by hand because the e2e harness never reaches three characters. Found by PR 15 |
| **D-52** | **The inventory's equipment-slot click handler was unreachable.** `_invClickStart` was captured into `fromIdx` and set to `null`, and 112 lines later the click branch read `if (_invClickStart && _invClickStart.equipSlot)`. Clicking an equipment slot to unequip did nothing; only dragging worked | low — no data risk and there was a working path. ~20 lines that read like a feature and were not one | **PR 26** | Fixed in three lines by capturing `fromEquip` **before** the null. It failed silently rather than throwing, which is why nobody noticed: `getSlot(undefined)` tests `index < 0` and `index >= totalSlots`, both false for `undefined`, so it returned `undefined` and the handler fell through as a no-op. `test_inventoryEquipClick.js` is 15 assertions on the one UI surface with no automated coverage; the adversarial pass caught that its safety assertion ("a full inventory re-equips rather than destroying the item") **passed under the broken code** — nothing-happens and unequip-then-re-equip have the same end state — so it now traces the interaction instead. Found by PR 17 |
| **D-58** | **Seven pause-menu control listeners accumulated one set per session**, and the leak was worse than this row said. The two chunk sliders, four performance controls and the day/night checkbox attach to elements that outlive the session, and `setupPauseMenu`'s cleanup removed only three of ten | medium — **and the real cost is retention, not CPU.** All seven close over a per-session `GameState`, so every exited session's renderer, chunkManager, skybox and loaded meshes were **pinned by the DOM forever**; a stale chunks-per-tick handler called `startFlushTimer` on a **disposed** manager (which guarded `_flushIntervalId` but not `_disposed`); and stale day/night handlers held previous sessions' WebSocket connections and would `TIME_SYNC` a dead session's clock onto them | **PR 26** | Fixed per **decision 56** — session lifetime with a complete cleanup, which is what `Bootstrap.js:134`'s existing comment already claimed. All seven are named `const`s with matching removers; `ChunkStorage.startFlushTimer` gained the `_disposed` guard. Naming them pushed `PauseMenu.js` to 443, so the settings half split into `PauseMenuSettings.js` (decisions 30/33 again). `test_pauseMenuListeners.js` asserts 10 registrations and 10 removals and that an **exited** session's handler no longer answers — proved non-vacuous by dropping one remover (25/32). Found by PR 18 |
| **D-61** | **Two test files referenced `main.js`, which no longer exists.** `test_pageLoad.js` asserted `js/main.js` was on disk and that `index.html` referenced it — both false since PR 9, not PR 18 | low — nothing went red, because `test_pageLoad.js` was quarantined | **PR 26** | Closed with the un-quarantine (**decision 54**). `test_pageLoad.js` is 486 → **136 lines** and asserts the *assembled* DOM rather than `index.html`'s text, so it cannot rot against markup that now lives in 16 template modules; its e2e id list is **scraped from `saveLoad.js`** so the two cannot drift. Sixty-seven assertions were deleted as already-false, including 33 `js/*.js` files and 26 root textures under names the generator has never produced. The nine `main.js:NNNN` comment citations in `saveLoad.js` remain — a sweep for whoever next edits that harness. Found by PR 18 |
| **D-63** | **The mesh worker's block-category tables had drifted from `BLOCK_REGISTRY`, so a chunk rendered differently depending on whether the worker spawned.** `ChunkMeshBuilder.js` derived `cutoutIds`/`transparentIds`/`tintableIds` from the registry; `meshWorker.js` hard-coded them, tinting `white_concrete` (115) green and rendering `yellow_poplar_leaves` (192) opaque | **high** — player-visible and *inconsistently* so, which is the worst shape for a rendering defect: it cannot be reproduced reliably and no screenshot baseline can pin it | **PR 23** | Fixed by deleting the tables, not correcting them: the worker now receives the registry-derived sets in its build message, so a stale copy is impossible rather than merely detectable, and `meshWorker.js` contains **zero block-id literals**. **A third instance was found in the same file** — the block loop skipped `blockType === 12` as `CAVE_AIR` (which is 0), so polished granite emitted no geometry at all while still counting solid for neighbour culling. `test_meshTables.js` runs the real worker source in a `vm` sandbox against the real payload; proved non-vacuous by restoring both wrong entries (267 → 257). Found by PR 18's PR 23 inventory |
| **D-64** | **Hand-maintained block-id tables predating the block renumbering — this row was the census.** Live: `mobMovement.js` (**D-56**), `meshWorker.js` (**D-63**), `InventorySync.js`'s `VALID_BLOCK_IDS` (`1, // Grass` where 1 is `bedrock`), `BlockColors.js` (**D-51**). Dead, each pinning its wrong values with a passing test: `DamageSystem.js`'s `LAVA_ID = 15` | medium as a class — the shared cause is that `BLOCK_REGISTRY` is the only thing that knows the ids and nothing protected its consumers | **PR 23** | **All four live tables now derived; two more were found while deriving.** `VALID_BLOCK_IDS` went **27 → 193** (it had been rejecting 166 real blocks) and `VALID_NAMED_ITEMS` 10 → 104; `NAMED_ITEM_META` was deleted after measuring that **5 of its 10 entries disagreed with canonical `NAMED_ITEMS`**; and `SINGLE_STACK_BLOCKS` was `{22,25,26}` — three ordinary ores capped at 1 over the wire — where the two real quest items (189, 191) were uncapped. Every rewritten assertion is equal or stronger (two spot checks became two `every()` checks over all 104 items). **`DamageSystem.js`'s dead pair stays open under D-25 and travels with PR 34.** Assembled by PR 18 |
| **D-56** | **Mobs walked through `polished_granite`.** `mobMovement.js` tested solidity as `block !== 0 && block !== 12` at four sites; id 12 is `polished_granite`, and the test was wrong in both directions — water (46) and lava (47) counted as **solid**, so mobs stood on the surface of oceans and lava lakes | medium — player-visible. The third occurrence of the defect class PR 4 fixed twice | **PR 23** | Fixed with one derived predicate — `!BLOCK_PROPERTIES[id].solid`, the registry's own field, the same one `Player._isSolidAt` reads (**decision 50**) — rather than the three-id set the row named, which would have been a sixth hand-written table. **Three sites beyond the four survived the row:** `mobManager._findSpawnPosition` treated `tuff` and `deepslate_tiles` as non-ground, its head-space check, and `mob.canSee`, which let mobs see through granite one line under a comment saying otherwise. There were no mob tests at all; `test_mobMovement.js` is 228 assertions, proved non-vacuous by reverting the literals (7 red). **The consequence is D-70** — decision 51. Found by PR 18's PR 20 inventory |
| **D-57** | **The main-thread terrain fallback decision 9 deliberately kept did not exist.** `ChunkManager.js` falls back to `window._voxelgenGenerateChunk`, assigned only at the bottom of `workerGeneration.js` — a file it imported `?url` **only**, and a URL import does not evaluate the module. The branch threw `'No worker pool and no inline generation available'` instead of generating terrain | medium — decision 9's stated reason for keeping the triple contract was that losing the fallback means a browser that cannot spawn a worker gets no terrain, and that was the state | **PR 23** | Fixed with one plain side-effect import alongside the `?url` one. **Verified in the built bundle, not reasoned about:** `dist/assets/index-*.js` went from **one** occurrence of `_voxelgenGenerateChunk` (the read) to **three** (read, assignment, comment), and the `?url` copy is still emitted separately with the same hash. The adversarial pass AST-enumerated the IIFE's top-level statements to confirm evaluating a worker script on the main thread has no other effect — the worker branch is gated on `!globalScope.document`, so no `onmessage` is installed on the window. Two stale comments claiming the opposite are gone. **D-71** is the residue. Found by PR 18's PR 20 inventory |
| **D-60** | **`BiomeSystem.js` contained its Perlin implementation twice** — `_mulberry32`, `_hashString`, `_createPerlin`, `_fbm2`, `_applySpline` and `_createSharedPerlin` as module-level exports and again, verbatim, inside the `BiomeSystem` IIFE, ~66 lines in a 441-line file | low — no live divergence, but a function defined twice in one file is one a fix silently misses | **PR 23** | Both copies collapsed onto `Noise.js`'s exports, which gives that module its live consumer for the first time and settles **D-25**'s answer for it (decision 42). **This was the one task in PR 23 that could have changed terrain silently, so it was proved rather than read:** 202,163 `Object.is` comparisons across all four implementations — including `workerGeneration.js`'s, which had never been checked against the main thread's — and 273,818 more of `BiomeSystem`'s public surface before and after, at seed 424242 among others. **Zero differences**, and the adversarial pass reproduced 42,793 of them independently. A second hand-written table, the IIFE's `NAME_TO_ID`, is derived from `BIOME_DEFS` now. `BiomeSystem.js` 441 → 360. Found by PR 18's PR 20 inventory |
| **D-65** | **`Inventory.splitStack(from, to)` did not bounds-check its destination and `swapSlots` did.** `splitStack(0, 999)` wrote `this.slots[999]`, growing the fixed-size array past `totalSlots`; `serialize()` iterates `i < totalSlots`, so the split-off items silently disappeared on the next save | medium — item loss, silent, on the one UI surface with no automated coverage | **PR 23** | Fixed by copying `swapSlots`'s guard verbatim rather than writing a new one — the row's complaint is that the two disagree. The no-op half returns `false` when nothing moves, guarded on `actualMove <= 0` rather than `=== 0` (**decision 49**): an over-full destination stack makes `space` negative, and a negative `actualMove` ran the transfer **backwards**, growing the source. **The row's reach claim was wrong** — `splitStack` has no call site in `src/` at all; `InventoryDrag.js` calls `swapSlots`. Fixed anyway, four lines, public API. 15 new assertions, proved non-vacuous by reverting each half separately (8 red, then 2). **D-76** is the layer above it. Found by PR 18's PR 23 inventory |
| **D-66** | **`ChunkManager.onRenderDistanceChange` was fired and could never be set, and `_voxelRegionRadius` went stale on every render-distance change.** The callback field was never initialised and nothing in `src/` assigned it (the one thing that did, `PerformanceOptimizer.js`, assigned it to *itself* and was deleted in PR 20); `setRenderDistance` did not recompute `_voxelRegionRadius`, so `_updateVoxelRegion` used the startup radius for the rest of the session | low-medium — the slider itself worked; the cost was a callback that reads as a wiring point and one genuinely stale derived field | **PR 23** | Both fixed **before** the split, deliberately: `_voxelRegionRadius` and `renderDistance` land in different target files, and a cross-file version of this bug is materially harder to see. The dead callback is deleted (**D-42**'s shape). `test_chunkStorage.js` §23 is the regression test — 10 assertions including an end-to-end one counting the chunks `_updateVoxelRegion` actually scans — **proved non-vacuous by reverting the fix: 139/139 → 136/139, with the scan count reading 1089 (33²) where it should read 1369 (37²)**. Found by PR 18's PR 23 inventory |
| **D-67** | **`SkyRenderer.js` defined dawn and dusk three times and the three disagreed** — `getSkyPhase()` on integer hours, the fog and ambient curves on day fractions (`DAWN_START 0.20` = 4.8 h), and `getSkyColorForTime()` on a third set of hour-based smoothsteps. At hour 7.1 the label read `'day'` and the sky had finished its dawn blend while fog and ambient were still 4% short | low-medium — a visible seam at both transitions every in-game day, and `getStateSummary()` returns all three together so a debug overlay displays them contradicting each other | **PR 23** | **The sky-colour schedule wins — decision 46**: the sky is what the player sees, `getSkyPhase()`'s hours already matched it, so only the fraction constants moved and by at most 12 minutes of in-game time. One set of `HOUR` constants in `DayNightCurves.js` with the fractions derived from them, so the two representations cannot drift again. `test_skybox.js` 100 → 139 assertions, including a sweep over 240 tenth-hour samples asserting every `'day'` hour has day fog *and* day ambient; **proved non-vacuous by reverting each of the four constants individually (6 red each) and by a label-only drift (3 red)**. Found by PR 18's PR 23 inventory |
| **D-51** | **The dropped-item colour table predated the block renumbering.** 34 hand-written ids against a **193**-entry registry (not the 162 the row and `refactor.md` said): id 3 is `cobblestone` and the table painted it dirt-brown, id 4 is `andesite` and it painted it grass-green, 148 registry ids fell through to `#888888`, and 21 of the table's 34 ids did not exist in the registry at all | low — cosmetic, on the spinning cube a broken block leaves behind. Same defect PR 4 fixed twice before this ledger existed | **PR 23** | **Deleted rather than corrected — decision 47.** The registry has no colour field to generate from (`color:` is on 2 of 193 entries), so the colour is now the **alpha-weighted mean RGB of the block's diffuse atlas tile**, cached per id, following `Hotbar.renderItemIcon`'s existing access pattern. Alpha weighting is load-bearing: cutout tiles are mostly transparent and a straight mean drags leaves, flowers and torches toward black. `#888888` remains the fallback and is **never cached**, so a block asked for before the atlas loads does not stay grey all session; the two registry `color:` triples multiply rather than replace, because `red_flower` and `yellow_flower` share one texture. Found by PR 17 |
| **D-55** | **The hotbar advanced two slots per scroll notch, and scrolling with the inventory open cycled it anyway.** Two independent `wheel` paths both called `inventory.cycleSelection()`: a `document`-level listener in `initHud.js`, and `Mouse.js`'s canvas handler accumulating `scrollDelta` for the render loop to consume. A wheel event over the canvas bubbles to `document`, so both fired, and `Mouse.update()` never cleared `scrollDelta` — only the loop path did — so neither was a no-op. The two also disagreed: the `initHud` listener guarded `if (state.inventoryOpen) return;` and the loop path did not | medium — player-visible in every session, solo included. Selecting a block by scrolling overshot by exactly 2×, which reads as a sensitivity problem rather than a defect | **PR 20** | Fixed by keeping the loop path and deleting the `initHud.js` listener — the loop is where §9 PR 22 wants this logic and `Mouse.scrollDelta` is the input abstraction — and **carrying across the guard the survivor lacked**. The guard's shape is the part worth recording: it wraps only the `cycleSelection` call, **not** the `scrollDelta = 0` reset, so a scroll swallowed while the inventory is open is discarded rather than replayed on the frame it closes. `test/test_hotbarScroll.js` is the regression test, driving the **real** `worldStep` export rather than a copy (the D-45/D-47 defect), and it was **proved non-vacuous by reverting the guard** — 13/13 drops to 11/13 with exactly the two expected failures. Found by PR 18 while lifting the scroll consumption into `WorldStep.js`; the registrar and the consumer had been 600 lines apart in `main.js` |
| **D-59** | **The render loop's `lava` and `corrupt` biome-particle branches were unreachable.** `WorldStep` spawned lava bubbles on `biomeData.id === 'lava'` and toxic bubbles on `'corrupt'`; `BiomeSystem` produces only `deep_ocean, ocean, beach, plains, forest, badlands, tundra, desert, mountains, frozen_peaks` | low — no runtime cost beyond two string comparisons per frame, and the effects were never seen so nothing regressed. ~14 lines in the hottest function in the program that read as a feature and were not one | **PR 20** | Deleted, after **verifying unreachability rather than assuming it**: `NAME_TO_ID` has exactly ten keys, the ten `name:` literals are the only names the module defines, and the `toLowerCase()` fallback is itself unreachable because every one of those ten already has an entry. `spawnLavaBubbles` / `spawnToxicBubbles` were **left on `BiomeEffects`** — they are that class's API, and deleting a method because its only caller went is a different decision. **Closing this row does not close the drift**, and the adversarial pass insisted the distinction be written down: **D-68** is the same wrong biome list in the mob spawn tables and the quest markers, and it is live |
| **D-21** | **`SurvivalSystem`'s default spawn was `{x:0, y:20, z:0}` while `SpawnManager` used `SEA_LEVEL + 4` (68) — y=20 is 44 blocks underground** | low — latent, because `onDeath`/`onRespawn` were never wired to anything | **PR 20** | Fixed per **decision 5** (the owner's, and four PRs old). **There was a second hard-coded `20` the row never mentioned**: `_finishSleeping`'s `bedY` fallback, which flows into `setSpawnPoint(bedX, bedY+1, bedZ)` — so a null bed position wrote y=21 into the spawn record. Both sites now read `SEA_LEVEL + 4`, imported from `ChunkData.js`. **The old test asserted `spawnPoint.y === 20`, i.e. it pinned the bug**; it is rewritten into what the fix makes true and the file's assertion count went *up*, 352 → 360. Ironically the reference value's own module (`SpawnManager.js`) was deleted in the same PR as unwired duplication — the constant survives where it always lived, in `ChunkData.js` |
| **D-50** | **Every session added listeners that nothing removed — and the count in this row was wrong.** It said *eight `document`-level listeners added on every `startGame()`*: the camera-look `mousemove`, the three inventory-drag handlers, two `keydown`, a `wheel`, and a canvas `click`. **There are eleven, because the row counted the ones written inline in `startGame()` and not the ones a *constructor* registers.** `KeyboardInput` adds `keydown` + `keyup` on `document`, `MouseInput` adds `pointerlockchange`, and `TouchInput` binds **thirteen** handlers to the touch-control buttons in `index.html` — all three sets outlive the session, and the touch ones outlive the canvas | low as written, **but the correction is not low.** The eight were inert by luck (each guards on a `GameState` whose `running` is already `false`). The keyboard pair is **not**: `_onKeyDown` calls `e.preventDefault()` on `Space` with no focus or pointer-lock guard, so **one exit to the menu left a stale handler eating the space bar in the character-name and world-name inputs** — player-visible, in a text field, after any exit-to-menu | **PR 18** | Fixed with a session teardown registry: `teardowns` is a **declared** field on the `GameState` constructor (decision 23 — declared, not grown) with `addTeardown()` / `runTeardowns()`, and `Game.stop()` drains it. **Every one of the eight was a named function *expression* or an inline arrow**, and the name of a function expression is bound only inside its own body — so `removeEventListener` had nothing to name and a teardown written the obvious way would have removed nothing. They are `const`s now and the remover gets the same reference. The three input classes each already carried an **idempotent `dispose()` with no call site**; three `addTeardown` lines close the other half. The pause menu's own `keydown` is deliberately outside the registry — it has to keep working while the game is paused, and `setupPauseMenu`'s returned cleanup already owns it. **The undercount was found by PR 18's adversarial verification pass, not by the implementation** — the row, the plan and three source comments all said eight. `D-58` is the residue this fix does not cover |
| **D-54** | **`game.stop()` had no call site, so the periodic-save teardown had never run.** `startGame()` step 14 monkey-patched it and the pause menu's Exit-to-Menu handler set `state.game.running = false` **directly** instead, so every session's 30 s `setInterval` was left live. `DEPLOY.md` §7's timing table said player state saves "on `game.stop()`"; it never had | low — the leaked intervals were inert (each checked its own session's `running`) and the Escape handler still saved before the pause menu could be reached, so **no save was ever actually lost.** What accumulated was one live 30 s timer per session, each pinning a disposed `GameState` | **PR 18** | Fixed by making `onExit` call `state.game.stop()` first, in a `try/catch`. **This is a deliberate behaviour change and it is the ruling in decision 39:** exiting to the menu now saves player position, inventory and equipment. `DEPLOY.md` §7 already claimed it did, so the fix makes the document true rather than requiring it to be edited. **The first implementation of this row's fix was wrong in a way worth recording:** the `try/catch` wrapped the whole of `stop()`, and `runTeardowns()` — D-50's only drain — was the *last* statement inside it, so a throw in the newly-added save path would have skipped both the `clearInterval` and the listener removal, silently reinstating **both** bugs behind one `console.warn`. Caught by the adversarial pass; `Game.stop()` now guards only the fallible save, so the two teardowns always run |
| **D-42** | **`applyPerfSettings()` was defined and never called.** A 14-line function pushing render distance and shadow quality onto a live renderer and chunk manager, documented as *"Apply performance settings to the live game engine"*, with no call site anywhere in the tree — and its third parameter, `textureAtlas`, was in the signature and the JSDoc with **no line of the body reading it** (**D-46**'s shape, in the same file, found the same way) | low — **not** a behaviour bug. Both settings were applied inline at their own construction sites, so saved settings *were* honoured on game start. The cost was a second implementation of the same thing | **PR 18** | **Deleted — decision 38.** The row said "either the inline sites call it or it goes", and they cannot call it: `initScene.js` step 5 has a renderer but **no chunk manager yet**, and `initWorld.js` step 7 sets render distance through the `ChunkManager` **constructor's options** rather than by a setter afterwards. Making either call it would mean reordering construction to serve a function whose third parameter is dead. The pause menu already applies both settings live, per control. `grep -rn applyPerfSettings src/ test/` now returns exactly one hit — the comment in `src/core/Bootstrap.js` recording this ruling. Deleting it also took one warning off `no-unused-vars` (171 → 170), which is where it had been hiding since PR 15 |
| **D-49** | **Pressing E did not open the inventory. It threw.** `renderInventoryCraftingUI()` called `checkNearCraftingTable(player, game.chunkManager)`, and **`game.chunkManager` has been `undefined` since PR 12** folded the ad-hoc `game.*` props onto `GameState`. The proximity scan's first statement is `chunkManager.getVoxel(...)`, so it threw `TypeError: Cannot read properties of undefined (reading 'getVoxel')` — and it threw from inside `toggleInventoryScreen()` **before** `craftingScreen.classList.remove('hidden')`. So `E` exited pointer lock, hid the hotbar, set `inventoryOpen = true` and showed nothing. Pressing `E` again restored the hotbar, so it looked like the key did nothing | **high — player-visible, in every session, solo included.** The inventory, the crafting recipe list, the equipment panel and the mobile craft button were **all unreachable**, from PR 12 until PR 17. The 4×9 grid rendered zero slots because the function died four lines earlier | **PR 17** | Found by PR 17 while lifting step 13's DOM knot into `src/ui/hud/Hotbar.js` and `src/ui/overlays/*` — the read is 160 lines from the `new CuubzGame()` that stopped supplying it, and PR 12's own comment lists `chunkManager` among the props it moved. **Introduced by PR 12 and it survived four PRs and five fully green `test:e2e` runs**, because no assertion had ever pressed `E`. That is the fourth time in this refactor that a green harness proved less than it looked like (D-32, D-34, D-43) and the fourth found by structural change rather than by reading. Fixed by reading `state.chunkManager` — the same object, from the place PR 12 put it. **`test/e2e/saveLoad.js` presses `E` now**, on both hosts, and **the six new assertions were proved non-vacuous by reintroducing the defect**: `.hidden` stays `true`, the grid renders **0 slots instead of 36**, and the recipe list stays empty. 183 → 189 assertions |
| **D-43** | **Two `beforeunload` handlers wrote the rejoin record, with different payloads, and the loser's fields were silently discarded.** `src/main.js` registered one at IIFE top level (evaluated when the module loads) and a second inside `initMenuNavigation` (called from `init()`), so the top-level one was registered **first**, ran **first**, and was then overwritten by the second's `setItem` on the same `cuubz_last_session` key. They did not agree: the top-level handler took `mode` from `sessionManager._gameMode` — the mode the session is actually running in — while the `initMenuNavigation` one read it from `#host-mode-select` for a host and **hard-coded `'survival'` for a joiner.** The winner was the one that hard-coded it | medium — player-visible. **Refresh while joined to a creative session and the rejoin put you into survival**, because the record that survived said `mode: 'survival'`. The host path was unaffected (both read a real mode) and no data was lost either way | **PR 16** | Found by PR 15's PR 16 investigation, while counting the five `cuubz_last_session` write sites §8.3 says to consolidate — there are **six**, and two of them fire on the same event. Fixed as §8.3 specified rather than by deleting a handler: `src/util/StorageHelper.js` is the only writer of the key in `src/`, `normaliseSessionRecord` gives every record all eight fields with explicit nulls, and `SessionManager.getSessionRecord()` is the only thing that decides what one contains. **Deleting a handler would not have been enough:** `JOIN_ACCEPTED` carries no `mode` either (`server/matchmaking.js:174`), so the surviving handler's `data.mode \|\| 'survival'` could only ever have produced `'survival'` too — the mode now arrives through `joinSession(sessionId, {mode, name, seed})`, which `LobbyScreen` calls with the values it already rendered the browse list from. **Two changes ride on the fix and are stated in `refactor.md` §8.3 rather than left to be found:** a joiner's record now carries the session's real name instead of the literal `'Joined Session'`, and it carries the host's seed — which no write site produced before, so `SessionRejoin`'s temp-world branch was **unreachable for a joiner** and a rejoin silently loaded the player's own world with a different seed and different terrain. `test/test_sessionRecord.js` is the regression test, inside `npm test` so it runs on every push, and it was **proved non-vacuous three ways**: reverting the `joinSession` call gives *expected "creative", got "survival"*; a second `setItem` on the key names both files; a second `beforeunload` gives *expected 2, got 3* |
| **D-44** | **`startHosting()` wired the block-validation callbacks twice.** It took an `options` argument and registered `BLOCK_BREAK` / `BLOCK_PLACE` from `options.onBlockBreakValidated` / `onBlockPlaceValidated`, and `registerHostCallbacks()` registers the **same two events on the same connection** from `startGame()`. `startHosting` is called from exactly one place — `LobbyScreen`'s start-hosting button — **with no arguments** | low — latent. The `options` half had never executed, so nothing was doubled in production. Had anything ever passed it, every remote block break would have been handled twice on the host, once persisting to IndexedDB and once not | **PR 16** | **Deleted rather than carried across, which is a decision and not tidying.** `PR15_HANDOFF.md` §4.2 flagged it as *"one `client.onGame` registration away from being a double-handler bug"* and said not to delete it silently. Carrying a dead double-registration through the extraction would have preserved the trap inside the file whose whole point is that the session layer is legible now; the live registrar is unchanged and still does the job. The two surviving registrars share one `_wireBlockCallbacks` helper, so "wrap a handler in a try/catch and attach it" has one implementation instead of three. Found by PR 15's PR 16 investigation |
| **D-45** | **`test/test_relayUrl.js` asserted a copy of `getRelayUrl` that had drifted into asserting behaviour the game does not have.** The file defined its own `getRelayUrl(pageOrigin, queryParam)` under a comment reading *"Pure implementation of getRelayUrl logic (extracted from main.js)"* and asserted, 24 times, that `https://webgame-cuubz.thehomelabguy.com` resolves to `wss://relay.webgame-cuubz.thehomelabguy.com` and that everything else falls back to `ws://localhost:8765`. **`src/main.js` does neither**: it returns the fixed host `cuubz-relay.thehomelabguy.com`, chooses `ws`/`wss` from the page protocol, and takes no second argument — its only override is a `?relayUrl=` query parameter | low — no runtime risk, and the shipped function is correct. The cost is 24 green assertions per CI run against logic no browser executes, in the one file a reader would open to find out how the relay is addressed. **The third vacuous test this refactor has found** | **PR 16** | Found while looking for existing coverage of the session layer. Fixed by making the real function importable — `src/multiplayer/RelayUrl.js`, no DOM and no imports — and rewriting the file against it: 13 assertions, all of them what the real code makes true. **Proved non-vacuous rather than assumed:** deleting the `?relayUrl=` branch turns 3 red, inverting the protocol rule turns 8 red. The `?relayUrl=` override now has assertions holding it in place, which matters because it is the route a harness uses to point the game at a relay it controls — **D-48** |
| **D-46** | **`getRelayUrl(pageOrigin)` took a parameter it never read.** It was in the signature and in the JSDoc as *"Override for testing (e.g., 'https://webgame-cuubz.thehomelabguy.com')"*, and no line of the body referenced it. Every call site passed nothing | low — a documented override that silently does not exist, in the function that decides which server the multiplayer layer talks to. **D-45 is what it cost:** the test file's private copy implemented the parameter as though it worked, and that is most of what the copy asserted | **PR 16** | Deleted with the move to `src/multiplayer/RelayUrl.js`; the `?relayUrl=` query parameter — the override that does work — is asserted in `test_relayUrl.js` now. Flagged by PR 15's PR 16 inventory; PR 16 established that removing it changes no call site |
| **D-37** | **`gameState.persistence` was always `undefined`.** `startGame()` had `gameState.persistence = characterManager ? characterManager.storage : null; // For periodic saving`, but `characterManager` was a `BrowserCharacterManager`, which names the field **`this.persistence`**. The ternary's truthy branch yielded `undefined` every time. It was `game.persistence` before PR 12 folded it onto `GameState`; PR 12 moved it verbatim rather than reading it, which is why the PR that touched the line did not catch it | low — latent. **Nothing read it.** The periodic save the comment referred to went through `characterManager.persistence.saveCharacter(selected)` directly, so saving worked; the field was a dead handle with a misleading name | **PR 14** | Fixed by removing the cause rather than the symptom. The `storage` / `persistence` split existed only because two classes did the same job, and PR 14 deleted one of them (**decision 26**); `CharacterManager` uses `storage`, so the line is correct as a side effect. **Both call sites changed, which is what this row demanded:** `savePlayerState()` now writes **through** `gameState.persistence` instead of reaching into the manager for a second handle. That half matters more than the rename — the field went dead *because* nothing read it, and a field nothing reads goes wrong again the next time someone moves it. Found by PR 13's PR 14 investigation |
| **D-38** | **The two `WorldManager`s disagreed on the maximum world-name length: 32 in the shipped browser class, 16 in the tested one**, and `test/test_worldManager.js:114` asserted `MAX_NAME_LENGTH === 16`. The tested `WorldManager` imported `MAX_NAME_LENGTH`/`MIN_NAME_LENGTH` **from `CharacterManager.js`** — a *character*-name limit reused for worlds | low at the time — the two classes had never both been live, so no player hit either limit through the wrong one. **It was a trap for PR 14 specifically:** reconciling onto the tested class without noticing would have silently tightened world names 32 → 16 and made a 20-character world unrenameable | **PR 14** | **Ruled 32, and worlds own the constant now** — `MAX_WORLD_NAME_LENGTH` in `WorldManager.js`, with the `CharacterManager` import and the re-export both gone. **Decision 24.** Three pieces of evidence and none is a preference: `index.html` gives `#world-name` and `#host-world-name` `maxlength="32"`; the class that has actually been running used 32; and **the test file asserted both numbers at once** — `MAX_NAME_LENGTH is 16` with "17 char name invalid (over max)" in one suite, and "one over max" applied to a **33**-character name in another. Borrowing a character constant for worlds was the defect, so keeping the number and fixing only the coupling would have left the trap in place. Four assertions across two test files were rewritten into what the fix makes true, including a **new** one that a 32-character world name is *accepted* — the case that was actually broken |
| **D-39** | **World previews advertised two biomes that do not exist.** `BrowserWorldManager.createWorld` built each world's `biomeMap.dominantBiomes` from its own eight-name list — `Plains Forest Desert Tundra Mountains Ocean Lava Corrupt` — and **Lava and Corrupt are not biomes this game has.** `BiomeSystem.js` defines ten (`Deep Ocean, Ocean, Beach, Plains, Forest, Badlands, Tundra, Desert, Mountains, Frozen Peaks`) and the tested `WorldManager.BIOME_NAMES` is exactly those ten. The world-slot preview string, its tooltip and its colour swatch were all drawn from the wrong list | low — cosmetic, no data risk. A player saw a world described as "Lava, Corrupt" and then walked into plains | **PR 14** | Found while repointing `createWorldSlotElement` onto `WorldManager.getWorldPreview`. Fixed by the reconcile itself — the tested class generates from the real ten — plus rekeying `createWorldSlotElement`'s `biomeColors` table onto those names, with the four new colours taken from `BiomeSystem.js`'s own `color:` fields rather than invented. **Worlds created before this PR keep the `biomeMap` already in their config**; nothing rewrites stored world data to correct a label, so only new worlds get the right list. Stated here rather than left to be discovered |
| **D-40** | **World deletion's chunk cleanup failed silently, and ran after the point of no return.** `BrowserWorldManager.deleteWorld` wrapped the IndexedDB chunk/manifest delete in `try { … } catch (err) { /* Silently ignore cleanup errors on world deletion. */ }`, so a failure reported `{success:true}` and **re-opened D-18 with no console trace** — the world's chunks left on disk with the only id that could find them already deleted. It also removed the localStorage config **first**, so a tab that died between the two halves orphaned the chunks permanently | low — needs a failing IndexedDB transaction, or a crash inside a two-statement window, to trigger. But the failure it hides is exactly the one D-18 exists to prevent, and it hides it completely | **PR 14** | Found while moving the block into `PersistenceManager.deleteWorld()` (**decision 25**) — which is the argument for the move: it had been read past in PR 6c, PR 6d and PR 13's comparison as a landmark, and lifting it out is what forced someone to read it as code. The `catch` now `console.warn`s with the world id. It still does **not** throw, because `WorldManager.deleteWorld` turns a throw into `{success:false}` and keeps the world in its list, which would leave the UI showing a world whose config was already gone. Order inverted: chunks first, so the crash window costs a regenerated world instead of an unreachable ~14 MB |
| **D-31** | **Escape never closed the inventory.** `src/main.js`'s pause handler read `if (typeof inventoryOpen !== 'undefined' && inventoryOpen)`, but `inventoryOpen` was a `let` declared inside `startGame()`'s `setTimeout` closure — one of the ~184 locals `refactor.md` §1.6 is about — and the handler is in a different scope. The name was never in scope there, so the guard was **permanently false**: pressing Escape with the inventory open paused the game and left the crafting screen sitting on top of the pause menu | medium — player-visible, in every session, since whenever the handler was written | **PR 12** | The dead block was **deleted in PR 11** with a comment naming this row, because the `inventoryOpen = false` inside it would have thrown under module strict mode if the guard had ever been true. PR 12 restored the behaviour the only way that was ever available: `inventoryOpen` is a declared field on `GameState`, `setupPauseMenu` takes the `GameState` as its argument, and the handler reads `state.inventoryOpen` — **no `typeof` guard and no `window` global.** If `state` were ever wrong this now throws instead of going quiet, which is the difference between the fix and the bug. Found by PR 11's `no-undef`, which is the rule §6 PR 11 calls "the payoff" |
| **D-34** | **`game.frameCount` was set to `0` once and never incremented**, so **every `frameCount % N === 0` throttle in the render loop was permanently true** (`0 % N === 0`) and every `frameCount < 10` rate limit never expired. Six paths intended to be throttled ran on **every frame**: multiplayer `sendMove` (comment says "~20Hz"; was ~60Hz), the armour HUD (`% 10`), `PlayerListHUD` position updates (`% 30`), the host→client `TIME_SYNC` broadcast (`% 30 ≈ 0.5s`; was 60/sec) plus its `% 300` console.log, the `[CHUNK_STREAM]` position dump (`% 60`), and `updateHotbarUI` (`% 5` — which removes and recreates nine `<canvas>` elements per call). The two mob `try/catch` handlers meant to warn ten times warned forever | medium — 3× the intended multiplayer uplink, 60× the intended time-sync rate, per-frame DOM churn in the hotbar, unbounded console spam in a hosted session. Visible as frame cost; invisible as a defect, because everything still *worked* | **PR 12** | `state.frameCount++` at the end of the render loop. Found by PR 12 while folding the ad-hoc `game.*` props onto `GameState`, which is what §7 PR 12 asks for — hoisting a counter nothing increments and calling the result "identical" would have enshrined it. **This is a deliberate behaviour change inside a phase whose deliverable is "identical game", and the argument is that all six sites state their intended rate in a comment** ("~20Hz", "every 30 frames ≈ 0.5s", "every ~0.5s"), so the identical game is the throttled one — decision 22. Counting at the **end** of the frame keeps frame 0 doing a full pass, so the first hotbar render, `TIME_SYNC` and `sendMove` still happen immediately rather than 5, 30 and 3 frames in. **The multiplayer half has no automated coverage** (`saveLoad.js` is single-context), so the rate change on `sendMove` / `TIME_SYNC` / `PlayerListHUD` is reasoned, not measured — that is the residual risk. Both e2e hosts: 152 → 166 assertions, 0 failures, across the change |
| **D-35** | **Nothing enforced "exactly one `window.*` assignment in `src/`" between PR 11 and PR 12.** `scripts/check-globals.js` asserted it by path; **PR 11 deleted the script** on the ground that `no-undef` subsumes it, and `no-undef` does not — `window` is `readonly` in `eslint.config.mjs`, but assigning to a *property* of a readonly global is not an error under any ESLint rule. Decision 19 spotted the HTML half of this gap and kept three assertions for it; the `window`-assignment half was missed, and `PR11_HANDOFF.md` §2 and the landmine list both describe an allowlist in `eslint.config.mjs` that does not exist | low — latent. No second assignment existed; the cost is that a future one would land silently, which is how `refactor.md` §2's shared global scope accumulated in the first place | **PR 12** | Fixed in `test/test_globalCollisions.js`, beside the `index.html` assertions decision 19 put there for the same reason. It walks `src/**/*.js`, strips comments, and asserts the set of files containing a `window.x =` assignment is exactly `['src/testBridge.js']`, with the allowlist named as a constant so widening it is a deliberate edit. **Proved non-vacuous rather than assumed:** adding `window.__probe = 1` to `src/core/GameState.js` turns the run red with *expected src/testBridge.js, got src/core/GameState.js, src/testBridge.js*. Found by PR 12 while deciding whether `src/testBridge.js` could be deleted |
| **D-36** | **`refactor.md` §8.4's fifteen-step `Game.init()` sketch did not match the order `startGame()` actually runs in** — in a block whose own instruction to PR 17 is *"preserve the existing ordering exactly — it is load-order sensitive"*. It listed multiplayer 9 / first-person hand 10 / mob system 11, where the code runs mobs → hand → multiplayer, and inventory 12 before block interaction 13, where the code does the reverse | low — documentation, but pointed at the one PR (17) whose accept criterion is not reordering load-sensitive init, and reordering *is* what following it literally would have done. **The mob system is constructed before the inventory exists and handed it two steps later**; a PR that "corrected" the order to match the doc would have broken auto-loot with no failing test | **PR 13** | Fixed in PR 13. `main.js`'s fifteen step banners are numbered **from the code** and §8.4 was rewritten to match, with a note saying the banners are the authority if the two ever diverge again. Found by PR 13 while placing the banners — which is an argument for the banners: nobody had read the two orderings side by side before something forced it |
| **D-32** | **`sumBase` and `sumAmp` were assigned without a declaration** in `BiomeSystem.js`'s `sampleBiomeParams`. In a classic script that is sloppy mode and the line silently creates two globals — which is why it worked for the entire life of the project. **An ES module is strict mode by definition**, so from the moment PR 9 landed the same line was a `ReferenceError`, and `sampleBiomeParams` is reached from `BiomeSystem.getBiomeAtWorldPos`, which `src/main.js` calls **twice per frame** | medium — silent, player-visible degradation. Both call sites (`main.js:4280`, `main.js:4397`) wrap it in `try { … } catch(e) {}`, so biome particle effects fell back to plains forever and biome-specific mob spawning got `undefined` for every chunk, with no console error and no failing assertion. Live for two commits; never deployed | **PR 11** | `let sumBase = 0, sumAmp = 0;`. **Introduced by PR 9 and caught by PR 11's `no-undef` two commits later** — the clearest possible answer to whether the rule was worth turning on, and the reason the two PRs belong in the same session. **Proved, not reasoned about:** the pre-fix line reconstructed into a temporary module and imported as real ESM throws `ReferenceError: sumBase is not defined`; the fixed one returns `{"id":"plains","name":"Plains","isCold":false}`. All 152 e2e assertions passed *over* this bug in PR 9, which is worth remembering about what a green harness does and does not prove |
| **D-22** | `.github/workflows/ci.yml` used `on: push` with no branch filter **plus** `on: pull_request`, so a same-repo PR branch ran CI twice on every push | low — wasted runner minutes | **PR 11** | `push: branches: [main]`. Observed rather than predicted from the moment PR #1 opened. PR 7 and PR 9 each edited `ci.yml` and deliberately left this alone, because narrowing it while `refactor/phase-0` was the only branch being worked on would have removed the push-triggered feedback loop the plan depends on. The loop survives: a push to a branch with an open PR still triggers `pull_request`. Only the duplicate stops |
| **D-29** | Five planning documents cited `js/…` paths that PR 9 deleted — `IMPLEMENTATION_PLAN.md` (25), `multiplayer.md` (17), `MOB_PLAN.md` (11), `performance.md` (10), `CRAFTING_PLAN.md` (5) | low — confusion, and a reader following a path to a file that does not exist | **PR 11** | Mechanical path sweep to the `src/` destinations. `DEPLOY.md` and `README.md` were done in PR 9 because they describe the live system. The `PR*_HANDOFF.md` files are deliberately **not** swept: they are dated records of what was true when they were written, and rewriting them would be falsifying a log |
| **D-4** | **`sync.sh` carried `--exclude='dist'`, so the first deploy after PR 9 would have uploaded everything except the application** and served a black page | **critical** | **PR 10** | `npm run build` runs first and the script **refuses to continue** unless `dist/index.html` exists *and* contains a module script tag — both checked locally, before the host is touched. `dist/*` is the payload. A post-deploy check on the host then confirms `index.html` references a `.js` and that the file is on disk, because "the deploy printed Sync complete" was never evidence of anything. `refactor.md` §1.4 called this the single biggest risk in the refactor |
| **D-2** | `sync.sh` never restarted the relay. No `systemctl` / `service` / `kill` / `pm2` anywhere in the repo, so every `server/` change was written to disk and silently ignored by the process running from memory | high | **PR 10** | The unit is installed into `/etc/systemd/system/` **only if it differs**, `daemon-reload`ed if so, then `systemctl restart cuubz-relay` with a check that it came back active. `sudo -n` so a missing passwordless sudo fails immediately instead of hanging on a prompt with no TTY — and that failure **warns** with the two commands to run by hand rather than aborting, because the static site is already deployed and correct at that point |
| **D-3** | No rollback of any kind: extract in place, no backup, no release directories, no symlink, and the uploaded archive deleted immediately after | high | **PR 10** | The web root (minus `textures/`) is tarred to `/home/dadmin/cuubz-deploy/backups/webroot-<stamp>.tar.gz` **before anything is deleted**; the last five are kept. `DEPLOY.md` §6.5 is the restore procedure and is explicit about the three things it still does not cover. **No atomic release-directory swap** — that needs the web server's document root pointed at a symlink, and what serves `/var/www/html` has never been verified (`DEPLOY.md` §3.1); guessing at nginx config from a machine that cannot reach the host is how a site goes down for real |
| **D-5** | `tar xzf` never deletes, so every file ever deployed stayed on the host forever — including, after PR 9, the entire dead pre-PR-9 `js/` tree next to the live code | high | **PR 10** | The managed paths are `rm -rf`'d before extraction, so a deploy converges on what the repo contains. **`textures/` is deliberately excluded from that list** — a blanket `rm -rf ${REMOTE_DIR}/*` would take 118 MB with it and be unrecoverable if the path were ever wrong |
| **D-6** | `chmod` fanned out over all of `/var/www/html` — 3,370 texture files every deploy — and, because it ran in an `&&` chain **after** extraction, aborted the script with the new files already live if one file was owned by another user | medium | **PR 10** | Scoped to what was just extracted with `textures/` pruned, and `\|\| true`. The `\|\| true` is not hiding a gate: extraction has already succeeded by then, so a permission the deployer cannot change is something to be told about, not something to fail a completed deploy over. That ordering inversion *is* the bug |
| **D-7** | The source archive was staged **inside** the public web root and removed only on success | medium — source disclosure | **PR 10** | Staged in `/home/dadmin/cuubz-deploy/incoming/`, outside the web root, removed after extraction |
| **D-8** | `server/index.js` routed `uncaughtException` into `process.exit(0)`. `cuubz-relay.service` says `Restart=on-failure`, and **exit 0 is not a failure** — so after any unhandled error the relay shut itself down cleanly and systemd left it down permanently. `unhandledRejection` was worse: it logged and did nothing, leaving the process alive in whatever state the rejection left it | medium — relay stays down | **PR 10** | One `shutdown(reason, code)`: SIGINT/SIGTERM exit 0, `uncaughtException` and `unhandledRejection` exit 1. **Ruling: a non-zero exit code, not `Restart=always`** (decision 3) — both bring the relay back, but `Restart=always` would also restart a deliberate `systemctl stop`, and the bug is that a crash is not being *reported* as one. Also added a 5 s shutdown watchdog: `server.close()` only fires once every connection ends and a relay's connections are long-lived WebSockets, so a crash could otherwise leave the process alive holding port 8765 with its sessions already disposed — the same outcome by a different route |
| **D-9** | `node_modules` excluded with no remote `npm ci`, so adding a dependency to `server/package.json` deployed the code that needs it and not the dependency; the relay threw `Cannot find module` on the next restart | medium | **PR 10** | `npm ci --omit=dev` in `/var/www/html/server` on every deploy (`npm install --omit=dev` if there is no lockfile) |
| **D-10** | `ExecStart` hardcoded `/home/dadmin/.local/node-v22.22.0-linux-x64/bin/node` — breaks by path on any node upgrade (`status=203/EXEC`, retried forever), and CI validated 22.23.x against it | medium | **PR 10** | `ExecStart=/usr/bin/env node index.js` with `/home/dadmin/.local/node/bin` first on the unit's `PATH`; `sync.sh` refreshes that symlink to the newest `node-v*-linux-x64` under `~/.local` on every deploy. A node upgrade becomes "unpack the tarball, run ./sync.sh". **Ruling: resolve node from the environment** (decision 4). This is the **highest-risk unverified change in PR 10** — it decides which interpreter runs production — and `DEPLOY.md` §5.2 lists its three failure modes and the one-line fallback |
| **D-11** | `textures/` (118 MB, 3,370 files) re-uploaded on every deploy | low — transfer cost | **PR 10** | Its own artifact: skipped by default, uploaded on `--textures`, and uploaded automatically if `textures/blocks/manifest.json` is missing on the host (a first deploy, or a restore onto a clean box). The split matches what `test/e2e/staticServer.js` serves locally, so the topology is rehearsed by every `npm run test:e2e` |
| **D-13** | The whole repo — `test/`, `scripts/`, every planning `.md`, `.claude/` — shipped to the public web root | low — information disclosure | **PR 10** | The payload is `dist/*` + `server/` + `cuubz-relay.service`. Nothing else. Note the **already-deployed** copies do not disappear on their own; D-5's `rm -rf` of the managed paths is what removes them, on the first PR 10 deploy |
| **D-24** | **`npm run build` exited 0 and produced a `dist/` that could not run.** Vite does not bundle a classic `<script src>`: `index.html`'s 65 tags produced one *"can't be bundled without `type="module"`"* warning each, neither the scripts nor `textures/` were copied, and `dist/index.html` shipped 65 references to files that were not there | medium — a build that succeeds and produces a broken site invites someone to deploy it | **PR 9** | Exactly as predicted when PR 7 logged it: converting the 65 files to ES modules is what made them bundleable. `npm run build` now emits `dist/index.html`, one 1.7 MB JS bundle, one CSS asset and the two Web Worker sources as separate hashed assets, and **`npm run test:e2e` runs the full 152-assertion harness against that output** — `test/e2e/staticServer.js` serves `dist/` rather than the working tree, so "the build output works" is a gate rather than an inference. The **textures half of this row is not closed and was never PR 9's**: `publicDir` is deliberately `false` (118 MB × every build — `refactor.md` §1.8), so `dist/` has no textures and the harness falls back to the repo root for them, mirroring the topology **PR 10** has to build on the deploy host |
| **D-23** | **`index.html`'s 65 `?v=` cache-bust strings were maintained by hand and nothing checked them.** 28 of the 65 went stale across all of Phase 0 (PR 2, 3, 4, 6b, 6c), so a deploy would have served returning players their cached pre-Phase-0 JavaScript — including a `chunkmanager.js` with no H-1 migration in it | medium — a shipped fix silently does not reach returning players | **PR 9** | Fixed by removing the convention, which is what PR 7 said would close it. There is one `<script>` tag now and no `?v=` on it; Vite emits content-hashed filenames for the bundle, the CSS and both worker sources, so a changed byte changes a filename and a stale cache cannot serve it. `scripts/check-globals.js` fails the build if a classic `<script src>` ever reappears in `index.html`, which is the only way the hand-maintained convention could come back. The mitigation PR 6d applied (bumping all 28) was what kept a Phase 0 deploy correct in the meantime. **One `?v=` remains** — `css/style.css?v=20260727-1` — and it is harmless: Vite bundles and hashes the stylesheet from the `<link>` tag at build, so the query never reaches a deployed URL. PR 27 removes the literal |
| **D-26** | **`SurvivalSystem` ↔ `DamageSystem` was a real import cycle that ES modules turn from harmless into fatal.** `survival.js` declared `DAMAGE_SOURCES` and called `calculateFallDamage`; `damageSystem.js` read `DAMAGE_SOURCES` as a computed key **at module top level** (`ENVIRONMENTAL_DAMAGE_RATES`). As classic scripts it worked because `index.html` loaded survival first into a shared global scope; under `require()` it worked because damageSystem carried a shim that pulled the table off `require('./survival')` first. With real ES modules, whichever side is evaluated first pulls in the other and the other reads a `const` in its temporal dead zone | **high if shipped** — `ReferenceError: Cannot access 'DAMAGE_SOURCES' before initialization`, at load, in the browser. `mobIntegration.js` imports from the survival side, so the game would have hit it on the first page load | **PR 9** | Fixed by extracting the table to `src/game/data/DamageSources.js`, a leaf module both files import — import *order* is not something a module graph lets you control, so reordering was not an option. Both files re-export it, so every existing import site and every test is unchanged. `refactor.md` §4.1 already puts data tables under `src/game/data/`. Found and fixed in the same PR: `npm test` went red on it immediately (`Cannot read properties of undefined (reading 'LAVA')`), which is the value of converting the tests in the same commit rather than after |
| **H-2** | **`onupgradeneeded` enumerated every object store, `deleteObjectStore`d all of them and recreated them empty**, under a comment reading "handles schema changes cleanly". Incrementing `DB_VERSION` — a one-character change — destroyed every saved world on every player's device, with no migration and no warning | **critical if triggered** | **PR 6d** | Replaced by a version ladder: `ChunkManager.SCHEMA_STEPS[v]` brings a database from `v-1` to `v`, an upgrade runs every step in `(oldVersion, newVersion]`, steps only ever create (`_ensureStore` / `_ensureIndex` are create-if-absent), and a version with no registered step **throws**, aborting the versionchange transaction and leaving the database at its old version with its data intact. `deleteObjectStore` no longer appears anywhere in `js/`. **Proved, not reasoned about:** `npm run test:e2e` seeds a version-2 database with three real encoded chunks and a real manifest, increments to version 3 through the shipped handler, and asserts every record survives field for field — same keys, `worldName`s, byte lengths, offset-16 checksums and `savedAt`. `test/test_chunkStorage.js` §17 runs the same increment in CI. The `DEPLOY.md` §2.1 ⛔ is now a procedure |
| **H-3** | `js/main.js:545` opened IndexedDB with **no version argument**; on a device where the DB did not exist yet this created `cuubz-worlds` at version 1 with no object stores, and the following `db.transaction([...])` threw `NotFoundError` into a silent `catch {}` | low — self-healed via the same handler as H-2 | **PR 6d** | Fixed by deleting the second opener rather than patching it. `ChunkManager.openDatabase()` is now the only opener in the codebase; it always names `DB_VERSION` and carries the schema ladder, and returns a fresh un-memoized connection the caller owns. The repair pass at the end of every upgrade re-creates missing stores, so an H-3 database created by an older build still heals — asserted directly (`test_chunkStorage.js` §20) rather than left to the delete-everything side effect that used to do it by accident |
| **H-1** | **Chunk primary keys were not world-scoped, so worlds cross-contaminated.** The `chunks` store keyed records on `` `${cx},${cz}` `` alone, so chunk (0,0) was ONE shared record across all three world slots. Measured: one visit to a second world destroyed **1,073 of the first world's 1,184 saved chunks**, and re-entering the first world served the second world's spawn chunk byte for byte | **high — live data corruption** | **PR 6c** | Store key is now `` `${worldName}:${cx},${cz}` `` at all seven chunk-store sites, plus a runtime migration at `DB_VERSION 2` (H-2 makes an upgrade-handler migration impossible). The logical key `ChunkManager.key` is unchanged — 17 call sites and the manifest format depend on it. Re-measured after the fix: world A keeps **1,184 of 1,184** and the store holds **2,393 = 1,184 + 1,209, the sum**. `DEPLOY.md` §2.4. **Already-destroyed data is not recoverable**; affected chunks regenerate from the seed |
| **D-15** | `chunkBinaryCodec.js` sized the buffer as `HEADER_SIZE + blockRuns.length * 4`, but `blockRuns` is a flat `Uint16Array` of `[id, count, …]`, so a run is *two* entries. **Every stored chunk was exactly 2× the size it needed, half zero padding** — 24,156 bytes allocated / 12,088 used, ≈14 MB of zeroes per world. `estimateSize` had the same error and over-reported 2× | medium — 50% of IndexedDB footprint and 50% of every 5 s flush's write volume, wasted | **PR 6c** | Both sites now `blockRuns.length * 2`. Shipped in the same migration as H-1 so players' bytes are rewritten once, not twice. Backward compatible: `decode()` never consults buffer length, it stops after `blockRunCount` runs. Found by PR 6b |
| **D-17** | `deleteChunk` called `store.delete(key)` **twice** — two separate `IDBRequest`s, one per handler — so every call issued two delete operations | low — idempotent, hence unnoticed | **PR 6c** | One request, both handlers. Asserted by operation count in `test/test_chunkStorage.js` |
| **D-18** | Deleting a world removed its manifest but **left every chunk record behind**, under a comment reading *"chunks remain orphaned but harmless — they're keyed by chunk coordinates"*. The premise was H-1: the records were not orphaned, they were **shared** with whatever world next generated the same coordinates | low pre-6c (the records were being overwritten anyway); a growing leak post-6c | **PR 6c** | Found while scoping PR 6c. World-scoped keys are what make a world's chunks both identifiable and safe to remove, so `js/main.js` now deletes them as a key range. Records already orphaned by a pre-6c deletion stay orphaned — they have no `worldName` prefix to match |
| **D-19** | The `beforeunload` chunk flush wrote chunk records **without updating the manifest**, so a chunk saved on tab close kept the checksum the manifest had recorded for its previous bytes | low pre-6c (nothing read those checksums); would have been **high** as soon as something did | **PR 6c** | Found while designing PR 6c's load-time integrity check, which is the first reader of those checksums — verifying them without fixing this would have deterministically discarded whatever a player built immediately before closing the tab. Both stores now written in one transaction, and a stale entry found on load is repaired rather than treated as corruption |
| **D-14** | `js/main.js:4562` called `game.playerSync.reset()`, which does not exist on `PlayerSyncManager`. **Every "Exit to Menu" threw**, skipping six cleanup steps and `showScreen('mainMenu')` — a blank page, F5 the only way out | high — the quit path was broken in every session, solo included | PR 6b | Call deleted; `clearAll()` was already the whole teardown. `DEPLOY.md` §7.2 |
| **D-16** | `#pause-pause-time` was a checkbox labelled "Pause Time of Day", `checked` by default, while `main.js:4693` sets `checked = !skybox.timePaused` — so checked meant time was **running** | low — confusing control, no data risk | PR 6b | Relabelled to "Day/Night Cycle" on the owner's decision. Inverting the logic would have started every existing player's cycle paused |
| **D-1** | `sync.sh` did not exclude `.env`, so a local env file would ship into a world-readable public web root | credential leak (latent — no `.env` exists) | PR 6 | `sync.sh:31` |

**Also fixed, before this ledger existed.** PR 3 fixed 8 global-scope collisions plus a
ninth consumer it uncovered (`js/input/interaction.js` calling an undeclared `_log`,
which would have made breaking or placing a block throw once the three colliding
definitions were renamed). PR 4 fixed seven more, including block drops resolved from a
pre-renumbering ID table (andesite dropped cobblestone), a duplicate block ID (`115`
claimed by two blocks), and a test that would have hung `npm test` the moment it went
green. Those thirteen predate the ID scheme and are not renumbered into it — the record
is `refactor.md` §5 PR 3 and PR 4. **Everything found from PR 6c onward gets an ID here.**
| **D-20** | Four relay tests called `http.listen()` on a **fixed port with no `'error'` handler** — 18765, 18770, 18780, 18790. An occupied port died on an unhandled `EADDRINUSE` and CI went red naming neither the suite nor the port | low | **PR 31** | Fixed in all **five** socket suites, not the four the row named: `multiplayerStress` already bound `listen(0)` and was used as the pattern, but it had no `'error'` handler either, so it was the row's remaining half. Every listener now binds port 0, reads `address().port` back, and fails the suite by name on `'error'`. **Proved by occupying all four old ports and re-running: 48/92/32/62, exit 0** — the old shape reproduced verbatim dies on `Unhandled 'error' event`. Found by PR 5's audit |
| **D-28** | **`test/helpers/esmRequire.js` gave CommonJS tests a view of `src/`'s ES modules that differed from the browser's on import cycles.** A regex transform, exports as getters | low — latent | **PR 31** | **Closed by deletion.** Vitest loads ESM natively, so the hook and both its install sites are gone and the divergence class it created (**D-73**) goes with it. Nothing in `src/` had a cycle, so nothing depended on the difference. Seven `src/` comments still name the hook as a constraint on how imports may be written — those are stale now and travel with **D-82**. Found by PR 9 |
| **D-47** | **`test/test_sessionUI.js` was 730 lines that `require`d nothing from `src/`** — 155 assertions against inline reimplementations of `SessionManager`, connection status, session-list and player-list rendering, host-form validation and tab switching, beside a hand-rolled `MockElement` DOM | low — 155 assertions of green that could not go red when the shipped code broke | **PR 31** | **Rewritten against the real modules: 155 → 171**, at `test/unit/multiplayer/sessionUI.test.js`, 398 lines, driving the real `SessionManager`, `SessionHosting`, `ConnectionHUD`, `PlayerListOverlay` and `LobbyScreen` against a `jsdom` fixture built from the **shipped templates** so an id rename breaks it. **Proved non-vacuous by 34 mutations of the real `src/` files, every one of which turned the suite red**; `src/` restored and verified clean after each. Three groups were dropped as duplicates of `playerListHUD.test.js` / `worldManager.test.js` and two as having no real counterpart at all — the count rose anyway because parametrised real coverage is denser than the literals it replaced. It surfaced **D-85**. Found by PR 16 |
| **D-48** | **No automated test in this repo had ever driven a multiplayer path in a browser.** `saveLoad.js` is single-context and never clicks `#btn-host` or `#btn-join`; the relay handshake, `JOIN_ACCEPTED` over a real socket and `startGame()` on a joining client were unexercised | medium — the gap that let **D-43** survive five green e2e runs and four PRs | **PR 31** | **Closed by `test/e2e/multiplayer.js` — `npm run test:e2e:mp`, 70 assertions, 0 failures, ~72 s.** A real `server/index.js` child on 8765 gated on its own `[RELAY] Listening` line (never a sleep), two `BrowserContext`s at `?relayUrl=ws://127.0.0.1:8765`, the full host and guest click paths. The two assertions that matter: the guest's `chunkManager.worldSeed` equals the seed the **host typed**, and the guest's `cuubz_last_session` carries the host's **mode** — D-43 in a browser for the first time, and the session runs in `creative` precisely because `'survival'` is D-43's hard-coded value. **Decision 65:** a THIRD entry point, not assertions added to `saveLoad.js`, so the 189/189 equality that proves `dist/` and the dev server are the same game is untouched. It found **D-84** on its first green run. Not in CI — `ubuntu-latest` has no Edge |
| **D-73** | **`esmRequire.js` only exported the FIRST declarator of a multi-declarator `export const A = …, B = …;`**, and `const { X } = require(…)` snapshotted a getter where ESM has a live binding | low — latent, D-28's second and third instances | **PR 31** | **Closed with the hook.** Checked before deleting rather than assumed: `BlockRegistry.js`'s `export let MAX_BLOCK_ID` is the only mutable export in `src/` and no test imports it, and every `import * as NS` target was resolved against every `NS.prop` and destructured read in the migrated suite — zero real divergences. Found by PR 23 |
| **D-79** | **Deleting `esmRequire.js` broke both e2e hosts unless `saveLoad.js` was converted in the same commit** — the hook had two install sites, `run_tests.sh:71` and `saveLoad.js:91`, and `refactor.md` §11 addressed only the runner | medium — neither `npm test` nor CI would have caught it (CI has no Edge) | **PR 31** | Fixed in the same commit, both sites. `saveLoad.js` needed the hook for exactly one line — `ChunkBinaryCodec` — which is now an `await import()`; the file stays CommonJS in every other respect. **Verified by running the pair: 189/0 on `dist` and 189/0 on `vite`, equal.** The second half of the row landed with it: `eslint.config.mjs` block 3 was split so `test/**` lints as `sourceType: 'module'` and `test/e2e/**` stays `'commonjs'`. Found by PR 26's PR 31 inventory |
| **D-80** | **The pre-refactor v2 IndexedDB dump PR 32's deliverable required does not exist anywhere in the repo**, and producing one means running the pre-refactor game in a browser and exporting IndexedDB | medium as a plan defect | **PR 31** | **Closed by ruling — decision 64: a synthetic v2 seed meets the criterion, and no dump is ever going to exist.** `test/integration/storageUpgrade.test.js` seeds a populated version-2 database through the **shipped ladder** with `fake-indexeddb`, chunk payloads produced by the real `ChunkBinaryCodec` so the seed cannot drift from the encoder, then opens at version 3 and asserts every chunk and manifest survives byte for byte and that `deleteObjectStore` is never called. It runs a **throwaway probe database** — `cuubz-storage-upgrade-probe`, asserted `!== DB_NAME` — so `cuubz-worlds` is never opened and `DB_VERSION` stays 2. **Proved non-vacuous six ways**, including restoring the pre-6d `deleteObjectStore` handler (3 records → 0) and flipping one stored byte. This is the H-2 mechanism inside `npm test` for the first time; it was previously only provable in the browser harness. Found by PR 26's PR 31 inventory |
| **D-83** | **The e2e harness left its `vite` child holding port 3100 when it died**, and the browser can die: one run stopped at assertion 96 with `Target page, context or browser has been closed` and left a `vite` LISTENING | medium — a stale listener on 3100 is the condition that has made a green run a lie eight times | **PR 31** | Both halves fixed in `test/e2e/viteServer.js`. **(a)** `assertPortFree()` runs **before** the spawn and refuses to start on an occupied port, loudly, with no reuse and no ephemeral fallback — a fixed port is what makes a leftover *visible*. **(b)** `killTree()`: on Windows `spawn` runs with `shell: true`, so the child is `cmd.exe` → `vite.cmd` → `node` and `proc.kill()` reaps the shell while the **grandchild keeps the port**. Reproduced exactly — spawned pid 5564, listening pid 9268, still listening after `kill()` — and `taskkill /T /F` takes the tree; `close()` then polls until the port is quiet and shouts if it never is. `saveLoad.js` gained a module-scoped idempotent `teardown()` that every exit path funnels through. **Verified by the real pair: 3100 free immediately after both runs.** The browser-death half is not explained and is not claimed to be. Found by PR 26 |
| **D-77** | **No mob had been rendered since PR 9.** `mobIntegration.js:72` guarded `MobRenderer`'s construction on `typeof THREE !== 'undefined'` with **THREE not imported in that file**, so the guard was constant **false** and `setRenderer()` was never called; `mobRenderer.js:38`'s `addMob(mob) { if (typeof THREE === 'undefined') return; }` was the mirror image, returning on its first statement every call | medium — a shipped subsystem silently inert in the browser, invisible to `no-undef` because `typeof` is the one construct it exempts | **PR 33** | **Fixed by deleting both conditions, in its own commit with the e2e pair as the gate**, per the owner's instruction — not folded into PR 33's ~30-site mechanical sweep. Importing THREE was rejected as the fix: it converts a constant-false dead guard into a constant-true one, the same defect wearing the other polarity. `mobRenderer.js` names THREE nowhere outside JSDoc, so no import was needed. **The whole newly-live path was executed in Node against real three 0.134 before the guards came out** — all five definitions built (9–16 meshes each), every `AI_STATES` value driven, disposal confirmed by three's `'dispose'` events — and **nothing in the reachable path throws**. `test/unit/game/mobRendering.test.js` is 103 assertions and the guards are pinned **by their effects, not by a source-text grep**, which could not tell a live guard from a sentence describing a dead one: **restoring either guard turns the file red (3 and 9 tests)**, one of 18 mutations run against the three source files, 17 of which were caught. Both e2e hosts stayed at **189/0** with zero console errors. Eight further defects the trace surfaced are **D-88**, and the observability gap around them is **D-89**; both are PR 34's. Found by PR 26's PR 33 inventory |
| **D-27** | **The genuine `typeof X !== 'undefined'` cross-module guards were constant**, reading as protection while providing none — 64 code occurrences across 31 files, of which **32** were the imported-binding case | low | **PR 33** | **30 removed, and the classification is the finding: 14 collapsed to their body; 6 had a constant-FALSE condition, so the guarded body was DEAD CODE and the fall-through was the only live branch** (`PlayerSync:670`, `SkyGeometry:84`/`:158`, `ChunkMeshBuilder:685`, `InventoryBlockItems:107`, and `mobModelBuilder:17`, whose dead body called `new THREE.Group()`); 10 kept a real second condition. **`SkyGeometry.js`'s `!this.renderer || !this.renderer.scene` — PR 9's fix — is still there**; only the `typeof THREE ||` disjunct went. **No test went red**, so no guard was load-bearing. 32 occurrences deliberately remain: 28 host-builtin feature detections, 3 `globalScope` in `workerGeneration.js` (the dual-mode IIFE parameter, one of them D-57's fix) and one instance-property probe. Removing them **puts all 30 sites under `no-undef` for the first time** — `typeof` is that rule's one exemption, which is how D-77 hid. The two D-77 sites landed separately. Found by PR 9 |
| **D-30** | `vite.config.js` kept `minify: false`, shipping ~1.9 MB unminified | low | **PR 33** | Flipped, **with `sourcemap` in the same edit** because flipping `minify` alone means the first real deploy is debugged from a minified bundle with no map. Browser JS+CSS **2,004,583 → 1,073,402 raw (−46.5%)**, **440,032 → 266,607 gzip (−39.4%)**; both `?url` workers byte-identical, as expected of assets. `minify: 'esbuild'` **hard-fails on this toolchain** — Vite 8 here is the rolldown build, `esbuild` is not installed, and naming it kills `vite build` **after `emptyOutDir` has wiped `dist/`**; `true` selects the bundled minifier. Re-verified before flipping: 0 `constructor.name`, 0 `extends Error`, 0 `Function.prototype.toString`, all `.name` reads data properties. **The gate is ONE host** — `test:e2e:vite` never builds — and that host reported **189/0** on the minified bundle. `sourcemap` is `'hidden'` and `sync.sh` excludes `*.map`: **decision 67**. Found by PR 9 |
| **D-62** | **The e2e seed assertion was nondeterministic** — `expected 424242, got 3335459929` against a byte-identical tree — because `WorldScreen.js` pre-fills `#world-seed` with a random uint32 and a **blank** field yields one from the same expression, making a `page.fill` that does not take **byte-indistinguishable from success** | medium — a flaky red trains the reader to re-run until green | **PR 31** (detector), **PR 33** (cure) | **Both halves are in.** PR 31's `fillChecked()` wraps all five `page.fill` sites and fails naming the value it actually read — proved in real headless Edge against a page that re-randomises on `input`, printing the exact value from the original failure. PR 33's cure is in `submitCreate`, **not** in `parseSeed`: `parseSeed('') → {ok:true, seed:undefined}` is a truthful answer to "what integer is in this string?" and `createWorld(name)`'s optional-seed contract is directly asserted, so the *policy* — a create form may not accept a blank seed — belongs to the form. Verified no live path creates a world without an explicit seed: the multiplayer temp world is pushed onto `worldManager.worlds` directly and never goes through `createWorld`. Observed by PR 18 |
| **D-71** | **The `window`-writer guard could not see the second global writer PR 23 activated** — it matched the literal token `window.<name> =`, and `workerGeneration.js:1060` assigns `globalScope._voxelgenGenerateChunk` where `globalScope === window` | low | **PR 33** | Fixed as the row specified, in two parts, and **neither is "widen the regex"**: aliases are resolved **per file** (the literals, plus any name bound by the IIFE tail or by `const X = window|self|globalThis`, **minus anything the file shadows**), and the allowlist moved from files to **(file, property) pairs** — six of them — so `workerGeneration.js` is exempt for `onmessage`/`onerror`/`_voxelgenGenerateChunk` and nothing else. Renamed accordingly. **The alias resolver immediately found what the row predicted:** `RegionTracker.js:32`'s `const self = this;` is a shadow, not a global, and the first resolver reported it — hence the subtraction step. It also found that `meshWorker.js`'s `self.onmessage`/`self.onerror` were invisible too, not just `globalScope`. Proved three ways: a new `window.foo =` in a non-allowlisted file goes red, a new `globalScope.bar =` in `workerGeneration.js` goes red, the sanctioned writes stay green. Found by PR 23 |
| **D-72** | **The shared data tables were exported unfrozen**, and PR 23 turned four copies into one reference — `BlockCategories.js`'s freeze is shallow, so `SPECIAL_MESH_TYPES`'s objects and `BLOCK_COLOR_MULTIPLIERS`'s arrays were shared across every `ChunkMeshBuilder` where the old code built fresh ones | low — latent | **PR 33** | Deep-frozen: `NAMED_ITEMS`, `MAX_STACKS`, `ITEM_CATEGORIES`, `SPECIAL_MESH_TYPES`, `BLOCK_COLOR_MULTIPLIERS`. ES modules are strict, so a silent mutation is now a loud `TypeError` — proved by 8 probes including the deep ones (`NAMED_ITEMS.coal.maxStack = 1`, `SPECIAL_MESH_TYPES[0][1].height = 9`, `BLOCK_COLOR_MULTIPLIERS[0][1][0] = 0`). **Nothing went red**, and that was checked rather than assumed: every write shape was grepped across `src/` including the two `BLOCK_REGISTRY` `color` arrays the freeze reaches transitively. Found by PR 23 |
| **D-74** | **~250 lines of provably dead fluid-face code in `ChunkMeshBuilder.js`, plus a UV fallback the two mesh paths answered differently** — `1.0/16` in the builder, `1.0/6` in the worker | low | **PR 33** | The dead block and its 13-line commented call site are gone; zero call sites, including dynamic dispatch (`chunk.getWaterLevel` exists nowhere). That left the file at 500, **100 over the ceiling**, so it was split by field-crossing count under **decision 44**: `ChunkMeshBillboards.js` (160, 0–1 crossings) and `ChunkMeshGeometry.js` (104, **0** crossings — they held all 15 `THREE.` references, so `ChunkMeshBuilder.js` **no longer imports three at all**). **759 → 340**, all three under 400. **The UV question was not settled with a one-character edit:** the fallback now ships in the worker's `msg.tables` payload, so both paths resolve to `TextureAtlas.uvTileSize()` (0.0614793…) and **cannot disagree by construction**; the old `1.0/6` was **2.71×** the true tile. `HORIZONTAL_FACES`'s import went, its export stayed. **The regression guard reading only `ChunkMeshBuilder.js` went vacuous the moment the file was split** — the adversarial pass caught it and it reads all three files now, comments stripped. Found by PR 23 |
| **D-76** | **Nothing capped `slot.count` at the item's `getMaxStack`**, so an over-full stack survived round-tripping and made `space` negative downstream; and `splitStack` lacked `swapSlots`'s `from === to` guard, so a same-slot split decremented and incremented the same object and fired two notifications while reporting success | medium — silent item-count corruption on the one UI surface with no automated coverage | **PR 33** | **Decided: clamp on read, and make the clamp loud.** Refusing to load costs a whole world to one bad number; leaving it lets the corruption spread through every save; clamping is lossy, which is exactly why it is not silent — `_capCount` warns with the call site, the item, both counts and how many items were destroyed. Wired into `setSlot` and both `deserialize` paths; only the **upper** bound is new, `Math.max(1, …)` stays. **Proved it cannot fire on good data:** `getMaxStack` and `InventorySync.getMaxStackSize` are body-identical and `addItem`/`addToSlot`/`splitStack` have always respected the same ceiling. `InventorySystem.js` landed at 392 — `_capCount` was moved out to keep it under 400. Found by PR 23 |
| **D-78** | **A rejected host request produced no client feedback at all.** `server/matchmaking.js:141` sends `HOST_REJECTED`; it was in neither the client's `MESSAGE_TYPES`, nor its routing list, nor `SessionManager` — while `JOIN_REJECTED` had all three | medium — player-visible: the host form sat in "connecting" forever | **PR 33** | Fixed as part of `shared/protocol.js`, plus the half the row did not know about: **`JOIN_REJECTED` never reset the connection HUD either**, so mirroring it exactly would have left the host reading "Host failed: …" beside an indicator still saying "Connecting…" — most of the original complaint. **Both** handlers now reset the status first. The mechanism is recorded too: `Client.js` held the matchmaking routing list **three** times verbatim and a fourth, subtly different copy in `_setupMatchmakingHandlers`; that is how a message type gets missed, and it is one exported frozen `MATCHMAKING_EVENTS` now. Found by PR 26's PR 33 inventory |
| **D-81** | **Three CSS cascade-order defects the split made legible, none caused by it** — a mobile equipment block placed before the base rules it overrides, a `min-width: 48px` beating a 36px track at ≤600px, and `.host-select-row` declared twice in one file | low — pre-existing and mobile-only | **PR 33** | **Three different fixes, each chosen so the equivalence proof PR 26 built is not forfeited.** (1) The block **moved between files** (tail of `crafting.css` → tail of `equipment.css`) and **no `@import` line moved**; across all 31 files its six selectors are declared in exactly two places, so the only relative order that changed is the one that was always meant to change — re-resolving **61,576 (element, property) triples** with the two files swapped gives **0 differences**. (2) A **specificity** fix (`#crafting-inv-grid .inventory-slot`, 1,1,0 vs 0,1,0), immune to a future manifest reshuffle, keeping the 48px touch floor everywhere else. (3) The **earlier** duplicate deleted — the one already losing every property, so the change is provably render-neutral. `cssCascade.test.js` is 20 assertions and **13 of them fail when replayed against the pre-PR stylesheet**. Found by PR 26 |
| **D-82** | **Stale-documentation census — eight statements that assert something no longer true**, the worst being the rollback procedure telling the reader to run a script deleted in PR 11 | low — no runtime effect, **except the rollback command**, which is the one procedure followed under pressure | **PR 33** | **All fixed, and the census grew to about twenty-one.** The rollback procedure had **three more** of its own, found only by running `./sync.sh --dry-run`: `DEPLOY.md:477`, `:919` and the `:943` "Step 2 caveat" all cite a `PROJECT_NAME` derived from the local directory basename, and **the current `sync.sh` has no such variable** — it stages via `mktemp` outside the web root (D-7's fix), so the caveat is a false warning about a leftover file that cannot exist. Also swept: seven `src/` files naming the deleted `esmRequire` hook as a live constraint, five more `test/test_*.js` citations at paths PR 31 deleted, `RelayUrl.js:15` (true of `multiplayer.js` now, still false of `saveLoad.js`), two stray post-import `'use strict'`s, and `vite.config.js`'s closing claim that `sync.sh` still excludes `dist/`. Assembled by PR 26 |
| **D-84** | **The host's Max Players setting never left the browser.** `Client.js:941` destructured `{ name, seed, mode }` and dropped `maxPlayers`; `server/index.js:140` hard-coded 4 | medium — player-visible | **PR 33** | Carried end to end and **clamped server-side** in `shared/protocol.js` — the client is not a trusted source for a session cap. Exercised across the whole input space: `undefined/null/''/'banana'/NaN/Infinity → 4`, `0/1/-5/true → 2`, `'3'/3.7/[3] → 3`, `99 → 4`; an old client sends no field and gets 4 exactly as before. **Verified in a browser, which is where it was found:** `test/e2e/multiplayer.js` sets the slider to 3 and the guest's browse row now reads **`1/3`** where it read `1/4`. `server/index.js`'s `onHostRequest` is the one path **no integration suite executed** — all five build their own relay wiring — which is why this survived; `test/integration/relayMaxPlayers.test.js` spawns the real `server/index.js` and is the first assertion in the repo that `server/` runs at all as an ES module. Found by PR 31 |
| **D-85** | **`health: null` rendered a player as 0% and red.** `SessionManager.js` and `PlayerListOverlay.js` both guarded only `undefined`, and `Math.max(0, Math.min(100, null))` is `0` | low — latent; `server/session.js` never sends the field | **PR 33** | `!= null` in both, defaulting to 100; a real `0` is still `0`. Proved by reverting each guard separately. Found by PR 31's D-47 rewrite, where the **deleted vacuous copy** asserted `null → 100 / green` — the fake test and the shipped code disagreed, which is D-47's complaint in miniature |
| **D-87** | **`test/e2e/viteServer.js` carried a literal ESC (`0x1B`) byte inside a regex literal** — invisible in `Read`, in `git diff` and in most editors, so the ANSI strip read as broken | low — the shipped code was correct; the cost was two independent reproductions losing time to it | **PR 33** | Replaced with `\x1b`. Because the code was already right, this is a **behaviour-identity** proof rather than a break-it proof: both spellings were run over a 12-case corpus — a real colourised Vite banner, `ESC[0m`, `ESC[m`, 256-colour, and four near-misses including a bare `[31m` with no ESC and the four literal characters `\x1b` — with identical `replace` and `matchAll` output on every case, and `READY_RE` still extracting the URL. Raw `0x1B` bytes remaining in the file: **0**. Found by PR 31 |
| **D-25** | **Twelve of the 65 files `index.html` used to load were referenced by nothing** — fetched, parsed and evaluated on every page load and then left sitting there. The entire audio subsystem, survival meters, environmental damage, quests and boss fights were all in that state | medium — thousands of lines of unwired *features* presented as shipped code | ~~PR 20~~ (six), ~~PR 23~~ (`Noise.js`), **PR 34** (the five) | **Closed. Decision 68: all five deferred gameplay subsystems are DELETED** — `SurvivalSystem` (1,159), `Boss` (1,135), `DamageSystem` (627), `QuestMarker` (605), `QuestSystem` (262), **3,788 lines of `src/` and 1,645 assertions across 8 test files**, plus parts of 2 more. Not one had ever executed outside `test/`. Wiring any of them is a *feature project*: `Boss.js` has **no rendering at all** and no boss HUD element exists, `SurvivalSystem.generateHUDHTML()` emits a **different** overlay from the five `.meter-fill` elements in the markup that nothing writes to, the quest layer has **four incompatible `questProgress` shapes and zero production callers**, and `DamageSystem.update()` hard-returns without `linkSurvivalSystem()`. Their tests were **defending defects** — `test_damageSystem` asserted `LAVA_ID = 15` against a registry where lava is 47 (**D-64**), and `Boss.phaseTransitionTimer`'s `NaN` deadlock had 369 green assertions over it. Deleting is the reversible option: everything is in git and at the pushed `pre-refactor-baseline` tag, and a feature PR can restore any of it **with a design**. It is the line **decision 42** already drew for six siblings. Found by PR 9 |
| **D-69** | **`SurvivalSystem` carried the integration surface of a class that no longer existed** — `_spawnManager`, `setSpawnManager()`, a `@param {SpawnManager}` JSDoc and an unreachable `_finishSleeping` branch, for the module PR 20 deleted | low — a documented integration point with no counterpart | **PR 34** | Closed with the file (**D-25**). `CharacterManager`'s *"Spawn Point Helpers (integration with SpawnManager)"* banner went too — and the check behind it is the part worth recording: **there was never an integration.** Those methods only ever touched `character.spawnPoints[worldId]`, which is live and reached from `savePlayerState.js:46` and `initPlayer.js:133`, so the banner was describing a coupling that had never existed rather than one that had been removed. Found by PR 20's adversarial pass |
| **D-75** | **PR 23 split residue — nine dead or duplicated fragments the cuts made visible** | low — every one inert | **PR 34** | **Seven deleted, one collapsed, and one was NOT inert.** Deleted: the duplicate `clientMode` assign, `createWorkerPool`, `generateFullWorld`'s two unread progress locals, `Inventory.canPlaceBlock` + `_getBlockProperties`, `Skybox.skyDome`, `makeGlowTexture`'s unread `radius`. **`createWorkerPool` was worth more than a tidy: it duplicated `init()`'s sequence *minus* the `?v=Date.now()` cache-bust, so wiring it in would have reintroduced D-23.** `_unloadMesh`/`_disposeOldMeshes` collapsed onto one name — the two 13-line bodies were proved byte-identical modulo two identifier names by normalising both against `git show HEAD:`, and `_disposeOldMeshes` did have a live caller, so this is a repoint rather than a deletion. **`ChunkConstants.js`'s `CHUNK_W`/`CHUNK_D` are LIVE and were left alone** — eight production importers plus `test/e2e/saveLoad.js:692-693` reading them by name through the bridge. The row called them a duplicate, which is true, but the fix is a nine-file rename with an e2e dependency, not an inert-fragment deletion. **None of this had any unit coverage at all** — the mesh pipeline was reachable only through `npm run test:e2e` — so `chunkStorage.test.js` gained sections 24 and 25 (139 → 167), asserting the observable effect rather than which method name produced it; neutering `_unloadMesh` turns 16 of them red. Found across PR 23's passes |
| **D-88** | **Eight defects in the mob rendering path, all found by reading code that had never executed** | medium as a class | **PR 34** (three), **PR 35** (the rest) | **The three with one correct answer each are fixed.** (b) `_animateAttack`/`_animateHurt` computed `progress` from the animator's **monotonic lifetime** accumulator, so any mob alive longer than ~0.3 s always saw `progress === 1` — **no attack animation had ever played and the hurt flash had never appeared**; both now use a per-animation start time, the shape `_animateDeath` already had. **That fix opened D-92**, a high-severity material leak, which the adversarial pass caught before the PR landed. (e) `deathAnim.js:68`'s `0.3 * dt || 0.016` precedence bug — the `dt`-present case was byte-identical in value, only the fallback changed. (a) `_buildCapsule` called `merged.mergeBufferGeometries(...)`, which is not a `BufferGeometry` method in any three version: **deleted rather than fixed**, because fixing it correctly needs the repo's first import from three's `examples/jsm` subtree — one `threePin.test.js` does not cover — added to serve **zero** call sites, which is the trade decision 68 settles the other way. **The five that remain are a different kind of question** — double-applied fog, a shadow pass that discards mob geometry on the block atlas's alpha, ~960 unmerged meshes at `mobCap`, the missing `Game.stop()` teardown, and a dead fog fast path — and they need a GPU to judge, so they travel with **PR 35**. Found by PR 33's D-77 trace |
| **D-92** | **The mob hurt flash leaked its material write, and PR 34's own fix for D-88 is what opened it.** `hurtReaction` writes `material.emissive`/`emissiveIntensity` and restores them only in its `progress >= 1` branch. Once **D-88 (b)** made `progress` a real per-animation ramp instead of a constant 1, that branch became unreachable: `mob.update()` clears HURT the instant `hurtTimer <= 0` and `mobManager.update()` runs every `mob.update()` **before** `renderer.update()`, so the animator never sees the exit frame. Measured across all five definitions: every mob's `emissiveIntensity` went 1 → 0 permanently after ONE hit, `corrupt_wisp` lost its designed `0x8b00ff`, and `stone_golem` at dt = 1/60 was left **glowing red forever** | **high** as introduced — player-visible on the subsystem PR 33 had just switched on, and frame-rate-dependent, so it would not reproduce reliably | **PR 34** | **Fixed in the same PR, before it landed.** Found by PR 34's adversarial pass — the eighth consecutive PR in which that pass found something five green gates did not, and the third in which what it found was a regression the PR had just introduced. The restore moved from `progress >= 1` to a **state-exit hook**, because `hurtReaction` is the only animation that writes to `material` rather than to parts (`_resetToInitialPose()` already covers parts every frame, which is why nothing else leaked). **The reason the new test missed it is the finding worth keeping:** it assigned `mob.aiState` directly and never called `mob.update()`, so nothing in the suite drove a mob *out* of a state |
| **D-53** | **`Game.update()` was a second `requestAnimationFrame` loop.** The Phase-0 `js/game.js` class carried an `update(timestamp)` that computed its own `delta`, had every system call commented out, and ended with `requestAnimationFrame((t) => this.update(t))`. **No call site has ever existed** | low — never called, so never a live defect. It became a trap the moment PR 17 made this class the one that runs the real init: `game.update()` is exactly the method someone reaches for when wiring a system, and calling it once starts a second rAF loop that runs forever alongside `renderLoop` | **PR 17** | **Fixed in PR 17 — deleted.** `PR16_HANDOFF.md` §4.4 flagged it in advance: *"if PR 17 ever calls `Game.update()`, there are two rAF loops. That is a bug waiting to be created."* Deleting it is the answer to that; the real loop is `renderLoop(state)` and PR 18 moves it to `src/engine/loop/RenderLoop.js`. Nothing else on the Phase-0 class was dropped — `running` / `paused` / `mode` / `lastTime` / `delta`, `player`, `blockPalette`, `start` / `stop` / `setMode` / `isCreative` / `isSurvival` / `canPlaceBlock` are all still there and `test_creativeMode.js` still passes against them |

---

## Open decisions, and which rows wait on them

These are the owner's calls, not the implementer's. Each is cited from the row that
waits on it, so a decision is never made twice or lost.

**All of them are settled.** Decisions 1–6 were answered by the owner on 2026-07-29,
7–10 on 2026-07-30 before PR 9 started, 11–15 were made *inside* PR 9, 16–19 inside
PR 11, 21–22 inside PR 12, 23 inside PR 13, 24–26 inside PR 14, 27 inside PR 15,
28–31 inside PR 16, **33–34 inside PR 17**, **35–41 inside PR 18**, **42–43 inside PR 20**, **44–51 inside PR 23**, **52–59 inside PR 26** and **60–66 inside PR 31** (32 is the owner's),
all under the
owner's standing instruction to decide
rather than stop. **Decision 20 is the owner's, 2026-07-30, and it overrides earlier
guidance in three documents** — read it before planning any deploy work. They are recorded below as
rulings, not as questions. Do not re-open them; the row that waits on each cites this
table, so a re-litigation would be silent.

| # | Decision | Ruling | Rows waiting |
|---|---|---|---|
| 1 | Push `pre-refactor-baseline`, open the PR, fast-forward `main` | **Push the tag. Open the PR** (`--base main --head refactor/phase-0`) once PR 6d lands, and **leave it open — the owner merges.** Do *not* fast-forward `main` from a session. Both done in PR 6d | — (a PR 1 acceptance criterion, and `DEPLOY.md` §6.2's rollback has no target without it) |
| 2 | Tick any of the seven Phase 0 gate checkboxes in `refactor.md` §5 | **Tick what has been verified by running it, and nothing else.** "Manual save/load test passes" is ticked with a footnote naming exactly what is automated (eleven of fourteen steps) and what waits on PR 12–13. Done in PR 6d | — |
| 3 | D-8: `Restart=always`, or a non-zero exit code | **Non-zero exit code**, so `Restart=on-failure` fires as the unit already intends. `Restart=always` would also restart a deliberate shutdown | **D-8** |
| 4 | D-10: how the node version gets pinned | **Resolve `node` from the environment** rather than pinning a patch version by absolute path. CI validates 22.23.x while the unit names 22.22.0; a pin that drifts from CI is worse than no pin | **D-10** |
| 5 | D-21: `SurvivalSystem`'s spawn `y` | **Match `SpawnManager`: `SEA_LEVEL + 4` = 68.** `y = 20` is 44 blocks underground | **D-21** |
| 6 | H-2: rewrite `onupgradeneeded` to migrate instead of deleting | **Rewrite it as a version ladder, and do not bump `DB_VERSION` while doing so.** Shipping a mechanism and exercising it are separate risks: the ladder is proved by an increment against a *seeded probe* database in both the browser harness and CI, so no player's database is moved off version 2 by the PR that makes moving it safe. Done in PR 6d | **H-2**, **H-3** — both closed |
| 7 | How the e2e harness reaches `ChunkManager`, `CHUNK_MAGIC` etc. once they are module-scoped | **A test-only bridge**: `src/testBridge.js` assigns them to one `window.__cuubz` namespace, imported first from `src/index.js`. Dynamic `import()` inside `page.evaluate` only works against the dev server, which would make the harness vite-only and contradict decision 13; hard-coding the constants turns invariant assertions into tautologies. Phase 2 puts a real `Game` on `window` anyway, so the bridge collapses into that — **its removal is slotted in `refactor.md` §7 PR 12**. Owner, 2026-07-30. Done in PR 9 | — |
| 8 | What happens to `scripts/check-globals.js` when `index.html` has one script tag | **Do not let it survive as a step that scans one file and checks nothing**, and do not retire it early — PR 11 deletes the script and its CI step together, in the commit that turns on `no-undef`. Owner, 2026-07-30. PR 9 repointed it (decision 11) | **D-27** |
| 9 | `workerGeneration.js`'s triple contract — keep the dual-mode `globalScope` IIFE and the main-thread inline fallback, or delete the fallback | **Keep both.** Losing the fallback means a browser that fails to spawn a worker gets no terrain at all, and PR 9 is supposed to be mechanical. `refactor.md` §1.3 requires this to be stated explicitly rather than left ambiguous, and the PR 9 outcome states it. Owner, 2026-07-30. Done in PR 9 | — |
| 10 | Whether PR 9 may land without PR 10 | **Yes, but the fact goes in the FIRST LINE of the handoff**, because until PR 10 lands the next deploy ships a site with no JavaScript. Owner, 2026-07-30 | **D-4** |
| 11 | What `check-globals.js` should check instead of duplicate globals | **The module boundary itself**: exactly one `<script type="module">` and zero classic `<script src>` in `index.html`; no `window.*` assignment in `src/` outside the allowlisted `src/testBridge.js`; no `module.exports` / `typeof module` / `require(` in `src/`; every `src/**.js` reachable by import from `src/index.js`. Same job — "nothing is quietly sharing the global scope" — asked of the structure that now exists. Made in PR 9, under decision 8's constraint that it must not go vacuous and must not be retired before PR 11 | **D-27** |
| 12 | How CommonJS tests require ES modules, given that `require()` rejects `import`/`export` syntax before any shim runs | **A `require` hook** (`test/helpers/esmRequire.js`) installed by `test/run_tests.sh` via `node -r`. `"type": "module"` at the repo root would convert `server/`, all 56 test files and both `scripts/` as collateral; rewriting 35 test files onto `await import()` is PR 31's job and means making every one of them async. `refactor.md` §6 PR 9 step 3 ("keep the shim where a passing test needs it") **cannot be followed as written** and this replaces it. Made in PR 9 | **D-28** |
| 13 | What `test/e2e/staticServer.js` serves, now that no static server can serve the working tree | **`dist/`, with a fallback to the repo root for `textures/`.** `index.html` loads one ES module whose graph contains bare specifiers, so the old "raw source" baseline is not servable by anything. Serving the built output makes `npm run test:e2e` prove the artifact PR 10 deploys actually runs, which closes **D-24**, and the texture fallback mirrors the deploy topology PR 10 has to create rather than working around it. `npm run test:e2e` now builds first. Made in PR 9 | **D-24** — closed |
| 14 | Whether the two Web Workers become ES module workers | **No.** Both stay classic scripts built from `fetch` + Blob; only the hard-coded `js/…` paths changed, to `?url` imports so Vite resolves them in dev and emits hashed assets on build. `refactor.md` §6 PR 9 suggests `new Worker(new URL(…), {type:'module'})`, but `meshWorker.js` has no imports and gains nothing from being a module, and `workerGeneration.js` **must** stay a classic IIFE because decision 9 keeps the main-thread inline fallback that evaluates the same file. The Blob indirection is also what makes the fallback reachable at all. Made in PR 9 | — |
| 15 | How `TextureAtlas.js` survives a base-URL change (`refactor.md` §1.8 offers a leading `/` or `import.meta.env.BASE_URL`) | **A leading `/`.** The deploy target serves from the web root (`/var/www/html`) and Vite's `base` is `/`, so the two are identical today; `import.meta.env` is a Vite-only global that would break the moment a Node test imports one of these files. If PR 10 ever needs a sub-path deploy, `BASE_URL` is the switch — that is one edit in two files and it is noted in `DEPLOY.md` §4.3. Made in PR 9 | — |
| 16 | D-27: whether PR 11 removes the 28 remaining `typeof X !== 'undefined'` guards, as its slot said | **No — moved to PR 33.** `no-undef` does not flag them; they are syntactically valid, so the lint PR has no mechanical leverage and it would be a hand-audit wearing a lint PR's hat. And each removal changes behaviour in exactly one environment — Node tests, where the guard is short-circuiting code that would now run, which is precisely how PR 9 found the `Skybox.init()` null dereference. PR 33 is the sweep-up PR, already owns removing the CommonJS shims, and lands after PR 31 has moved the suite to Vitest. Made in PR 11 | **D-27** |
| 17 | D-30: whether PR 11 flips `minify` now that `dist/` is what ships | **No — moved to PR 33.** PR 11 checked the thing that would make it unsafe (`Function.name` / `constructor.name` dependence: there is none) so this is not a safety call. It is that **the deploy path has still never been executed**, and the first real `./sync.sh` gets debugged from browser devtools on a LAN box where 1.3 MB costs nothing. Flip it after a deploy has happened; it is one line with both e2e hosts as the gate. Made in PR 11 | **D-30** |
| 18 | Whether PR 11 runs Prettier across the repo, as "lint + format" implies | **No. The tool ships; the reformat does not.** `npm run format` and `format:check` exist and neither is in CI. Running Prettier over 34,000 lines would touch every file and make `git blame` useless for the rest of the refactor, in the PR immediately before Phase 2 starts moving all of it anyway. Format what you touch; PR 26/27 is the natural point to reconsider. `.prettierignore` carries the reasoning. Made in PR 11 | — |
| 19 | What replaces the half of `check-globals.js` that `no-undef` does not cover | **Three assertions kept in `test/test_globalCollisions.js`**: `index.html` has exactly one `<script src>`, zero of them classic, and the one is `/src/index.js`. ESLint has no opinion about HTML, so a classic `<script src>` added back would hand whatever it loads a shared global scope again — silently, and invisibly to the linter. That is the one thing the deleted script did that its replacement cannot. Made in PR 11 | — |
| 20 | **When the rewrite gets deployed to `10.0.30.160`** | **Not until the entire rewrite is finished.** Not at the Phase 1 gate, not at the Phase 2 gate. PR 10's `sync.sh` stays unverified on purpose. **The owner's ruling, 2026-07-30, and it contradicts what three documents said before PR 12** — `DEPLOY.md` §4.3's operational rule read "do not run `./sync.sh` until PR 10 lands", which after PR 10 landed read as permission; `PR11_HANDOFF.md` §7 told the reader to deploy before writing any more code; and the Phase 1 gate's unticked deploy box read as blocked work. All three are corrected. **The consequence, stated plainly because it is accepted rather than overlooked: the delta between `refactor/phase-0` and anything that has ever run on the host grows with every PR, and the first real `./sync.sh` will be debugged against a codebase that has changed shape six times.** That is the owner's risk to carry, not an oversight. The deploy box stays unticked and is labelled **deliberately deferred**, not blocked | **D-12**, **D-30**, and the Phase 1 gate's sixth box |
| 21 | Whether PR 12 deletes `src/testBridge.js`, as §7 PR 12 and decision 7 both say it should | **No — the file stays; its removal moves to PR 33.** Made in PR 12, under the standing instruction to decide rather than stop. The bridge has two halves and only one of them collapses into a `Game` object. The **live** half does: `__cuubz.state` is the running `GameState` now, which is what unblocked the block-placement assertions. The **static** half does not — `ChunkManager` the *class*, `CHUNK_MAGIC`, `DB_VERSION`, `BLOCK_REGISTRY`, `HEADER_SIZE` are module-scoped bindings, not instance state, and no `Game` will ever carry them; the `DEPLOY.md` §2 invariant assertions read them directly *because* hard-coding them would make "the magic number did not change" a tautology (decision 7's own reasoning). Deleting the file therefore means moving 25 imports into `src/index.js` — the bootstrap §4.1 wants under 50 lines — or scattering `window.__cuubz.x =` across `src/`, which is the second sanctioned `window` assignment every document here forbids. **PR 12 changed the justification, not the need.** PR 33 owns removal and the condition is written in the file: it goes when something other than a `window` property can hand `page.evaluate` a module binding | **D-35** |
| 22 | D-34: whether PR 12 may fix `frameCount` when Phase 2's deliverable is "identical game" | **Yes, fix it in PR 12.** Made in PR 12. Three reasons. (a) §7 PR 12 explicitly owns folding `frameCount` onto `GameState`; hoisting a counter nothing increments, and calling the result identical, enshrines the bug in the object built to make the loop extractable. (b) **All six throttled sites state their intended rate in a comment** — "~20Hz", "every 30 frames ≈ 0.5s", "every ~0.5s" — so "identical" here means the game the code says it is, not the one the missing line produced. (c) The current behaviour sends 60 `TIME_SYNC` messages per second to every connected client and rebuilds nine hotbar `<canvas>` elements every frame; leaving that in to protect a definition of identity would be choosing the definition over the game. **The residual risk is stated in D-34 rather than waved off:** the multiplayer rate changes have no automated coverage, because `saveLoad.js` is single-context | **D-34** |
| 23 | Whether PR 13's "numbered private steps" are functions or banners | **Banner comments, not functions.** Made in PR 13, and the reason is a measurement rather than caution: the fifteen steps share **~160 init-only locals that PR 12 deliberately did not hoist.** PR 12 took the 21 `renderLoop` read, because those were what made the loop un-extractable; `loadingStatus`, `loadingProgress`, `selected`, `isJoiningClient`, `worldName`, `bestSpawnX/Y/Z`, `container`, `tileSize`, `renderDist` and the ~700-line knot of inventory/crafting/hotbar DOM closures are not among them. Extracting a step into a function today means threading those through parameters and back out through return values — **which is exactly the work PR 17 does properly**, by moving each onto `GameState` as it lifts the step onto `Game`. Doing it twice, badly first, is not cheaper, and a half-parameterised init is a worse stopping point than a flat one. What the banners buy now is real: `grep -n '══ Step' src/main.js` prints the init sequence in fifteen lines, and PR 17's cut lines are marked in the file rather than rediscovered | **D-36** |
| 24 | D-38: which world-name limit survives the reconcile, 32 or 16 | **32, and `WorldManager` owns `MAX_WORLD_NAME_LENGTH` outright** rather than importing a character limit. Made in PR 14. `index.html` already says `maxlength="32"` in two places, the shipped class used 32, and `test_worldManager.js` asserted 16 and 32 in different suites of the same file — so 16 was never a decision about worlds, it was `CharacterManager.MAX_NAME_LENGTH` arriving through an import. Tightening to 16 would have made an existing 20-character world unrenameable; keeping 16 and merely fixing the coupling would have left the same trap for the next reader. Four test assertions moved to the limit the fix makes true. **Also settled here:** `selectCharacter` becomes `async` and persists `lastPlayed`, adding one `localStorage` write per character selection — intended, because `selectWorld` already did it on both sides and the asymmetry was the bug | **D-38** |
| 25 | Where `BrowserWorldManager.deleteWorld`'s chunk cleanup lives after the reconcile | **`PersistenceManager.deleteWorld()`** (`src/engine/world/Persistence.js`). Made in PR 14. It cannot go in `src/game/entities/WorldManager.js` — Node tests import that file and it must stay environment-free — and leaving it in `main.js` would mean `main.js` still owning world CRUD, which is what the PR removes. `PersistenceManager` is the browser storage backend, already owns the localStorage half of the same delete, and is the object `WorldManager` is constructed with, so **`WorldManager.deleteWorld` did not change at all**: it already called `this.storage.deleteWorld(id)`, and a test's mock storage has no chunks to clean. The import cycle `PR13_HANDOFF.md` §4 said to check for does not exist — `ChunkManager.js` reaches `ChunkMeshBuilder`, `BiomeSystem`, `BlockRegistry`, `ChunkBinaryCodec`, `ChunkData` and `three`, none of which reaches `Persistence.js`. **Proved by measurement, not by reading:** with the cleanup disabled the e2e run reports *expected 0, got 1209* | **D-18** (regression), **D-40** |
| 26 | §3.4's reconcile-or-delete call: Option A or Option B | **Option A** — `main.js` uses the tested classes, both `Browser*` classes are deleted, browser-only behaviour is ported. Made in PR 14, confirming what PR 13's comparison concluded. The tested classes are near-supersets: they carry `setInventory`/`getInventory`/`setSpawnPoint`/`getSpawnPoint`/`serialize`/`deserialize`/`getQuestProgress`/`advanceQuest`/`addChunkReference`/`CHARACTER_COLORS`/`generateBiomeMap` on top of everything the browser copies did. Option B deletes the only coverage either class has ever had, in order to keep ~320 lines inside the file Phase 3 exists to empty. **Six divergences are recorded in `refactor.md` §8.1's outcome rather than merged silently**: the field-name split (D-37), `selectCharacter` becoming async (decision 24), the name limit (D-38, decision 24), the chunk cleanup (decision 25), `getBiomePreview` → `getWorldPreview`, and the `{success:false, error}` error shape | **D-37**, **D-38**, **D-39**, **D-40** |
| 27 | How the extracted UI reaches `main.js`'s manager state, given that all of it is null when the UI is constructed | **An object of live getters** (`uiDeps` in `main.js`), read through at call time. Made in PR 15. `characterManager`, `worldManager`, `perfSettings`, `sessionManager` and `gameState` are `let` bindings assigned inside `init()` and `startGame()`; a screen that captured one at construction would hold a permanent `null`. The alternative — rewriting ~110 references in `main.js` onto a context object — is a second large mechanical change in the same diff as a 700-line move, and **PR 17 and PR 19 delete those `let`s anyway** when they become fields on `Game` and `GameState`. When they do, `deps` becomes the `Game` instance and the getters go. **Also settled here:** the shared `#delete-char-modal` serves both characters and worlds, so its confirm/cancel handler lives in `UIManager` and dispatches to whichever screen the `dataset` names — neither screen can own it without reaching into the other, and the coupling is in the HTML until PR 26 | **D-41** |
| 28 | Where `renderPlayerList` / `hidePlayerList` go, given that §4.1's slot for "player list" is already occupied | **`src/ui/hud/PlayerListOverlay.js`, and explicitly not merged into `src/multiplayer/PlayerListHUD.js`.** Made in PR 16. §8.3 does not mention these two functions at all and §4.1 sends "player list" to `PlayerListHUD.js` — **which already exists, is imported by `main.js`, and is a different thing.** Both were read before deciding: `PlayerListHUD` is constructed inside `startGame()`, driven from the render loop every thirtieth frame, owns `#player-list`, and knows about world-space positions; these two are the `#player-list-overlay` element, redrawn wholesale from `SessionManager`'s `PLAYER_JOINED` / `PLAYER_LEFT` events, and know nothing about frames. Merging them would put two unrelated update cadences and two unrelated DOM subtrees behind one name — and the name would be the one already taken. The new file is named for the element id, so the collision cannot recur. §4.1 gains the entry. **Also settled here:** the former `hidePlayerList()` hid `#connection-hud` as well as the roster, which put a connection control inside a roster function; the halves are split between `PlayerListOverlay.hide()` and `ConnectionHUD.hide()` and **both call sites call both**, so the visible behaviour is unchanged | — |
| 29 | Whether the extraction recreates the `typeof MultiplayerClient !== 'undefined'` and `typeof HostManager !== 'undefined'` guards it moved | **No — they are dropped, and PR 33 still owns the sweep.** Made in PR 16. Both are D-27's pattern: with `X` an imported binding, `typeof X` can never be `'undefined'`, so the guard is constant-`true` and the `else` — *"MultiplayerClient not loaded — offline mode"* — is unreachable. Decision 16 moved the **sweep** to PR 33 because each removal changes behaviour in exactly one environment (Node tests, where the guard is short-circuiting code that would now run) and because the lint PR had no mechanical leverage. Neither applies to a guard being **rewritten into a brand-new file**: recreating it means deliberately authoring a provably-dead branch, and no Node test imports these two through this path. This is not PR 33's work done early — the other 59 stay, and the Phase 3 gate box still cannot close (see the gate). It is a refusal to copy dead code forward. The `this.client` null checks around the "offline simulation" branches are **kept** as they were, because they are guards on state rather than on a module binding | **D-27** |
| 30 | Whether the extracted session layer is one file or several | **Four: `SessionManager.js` (374), `SessionHosting.js` (119), `SessionRejoin.js` (135), `RelayUrl.js` (38).** Made in PR 16. §8.3 names one target and ~660 lines move; §8.2's accept criterion and §8.6's gate both say **no extracted file over 400 lines**, and one file would have been ~660. The three splits are seams rather than arbitrary cuts: `SessionHosting` is the only part of the layer that reads `index.html` form controls, which is what PR 26 will have to revisit; `SessionRejoin` is the only part that runs before there is a session; `RelayUrl` has no DOM and no imports, which is exactly what made **D-45**'s test fixable. **Dependencies run one way** — `SessionManager → SessionHosting`, `SessionRejoin → SessionManager`, both → `RelayUrl`/`StorageHelper` — and the obvious `SessionManager ↔ SessionRejoin` pair was avoided by having `SessionManager` reach the panel through `deps.updateRejoinPanel()`, which `main.js` already exposed. **D-26** is what an import cycle costs under real ES modules | — |
| 31 | `PR15_HANDOFF.md` §4.3's call: stand the relay up in the e2e harness, or say the session layer is unverified | **Neither as written. Cover the layer's *logic* in `npm test`, and give the browser gap an owner.** Made in PR 16. Option (b) leaves a live player-visible defect (D-43) with a fix nothing checks. Option (a) is a child-process relay, a second browser context and a second full `startGame()` — several minutes onto a seven-minute run, in a harness that **is not part of CI** (`ubuntu-latest` has no Edge), to cover a defect that lived in a pure function of three fields. What landed is `test/test_sessionRecord.js`: 51 assertions against the **real** `SessionManager` and `StorageHelper`, running on every push, proved non-vacuous by breaking each thing it checks, and including three *structural* assertions — one file spells the key, one file writes it, two files register `beforeunload` — in the idiom `test_globalCollisions.js` uses for the `window` allowlist, because no lint rule can see a stray `setItem`. **The browser half is not waved off:** it is **D-48**, owned by **PR 31**, which already owns relay test infrastructure and moves the suite to Vitest, and `saveLoad.js`'s `note()` is rewritten to say what changed rather than repeating what it said at PR 6b | **D-48** |
| 33 | Whether `startGame()` becomes one `src/core/Game.js`, as §8.4's heading says | **No — eleven files.** Made in PR 17. §8.4 says *"`startGame()` → `src/core/Game.js`"* and §8.2's accept criterion plus §8.6's gate both say **no extracted file over 400 lines**; `startGame` is **1,894**. This is decision 30's situation again and it is settled the same way — the ceiling wins and the cuts are seams, not slices. `Game.js` (399) is the class and the fifteen-step orchestration; `src/core/init/` holds nine step modules (59–232 lines) named for what they build; and step 13's ~700-line inventory/crafting/hotbar DOM knot goes to the destinations **§13 already named for it** — `src/ui/hud/Hotbar.js`, `src/ui/overlays/InventoryScreen.js` and `InventoryDrag.js` — plus `src/game/systems/DroppedItemsSystem.js` and `src/game/data/BlockColors.js` from **§4.1**. `PR16_HANDOFF.md` §4.3 offered leaving step 13 as a method on `Game`; that was rejected because it puts `Game.js` at 1,100 lines, and the PR 28/29 it proposed handing the DOM half to **was dropped in decision 32** — PR 26 inherited D-41, not this. **Every new file is under 400 and the largest is 327** | — |
| 34 | `PR16_HANDOFF.md` §4.4's call: does PR 17's `Game` absorb the Phase-0 `Game`, or displace it | **Absorb.** Made in PR 17. §8.4 says *"`js/game.js` is absorbed or deleted"* and deleting was never available: `BlockPalette` drives creative block cycling, `setMode()` is called from the pause menu and pushes creative physics onto the player, and `test_creativeMode.js` and `test_crafting.js` `require` the file. Displacing it — a second class beside the orchestrator — is what `startGame` was **already doing** (`new CuubzGame()` at step 8, with a `GameState` beside it) and is the thing this PR exists to stop. So one class: `running` / `paused` / `mode` / `lastTime` / `delta`, `player`, `blockPalette`, `start` / `stop` / `setMode` / `isCreative` / `isSurvival` / `canPlaceBlock` are **unchanged**, and `init(mode)` plus the fifteen steps are added. `state.game === game` and `game.state === state`; `GameState`'s four read-through getters are untouched, so nothing is duplicated. **Three things ride on it and are stated rather than left to be found:** `Game.update()` is deleted (**D-53** — a second rAF loop waiting to be started), `BlockPalette` moved to `src/core/BlockPalette.js` for the 400-line ceiling and is **re-exported** so `require('../src/core/Game.js').BlockPalette` still resolves, and `stop()` is a real method instead of step 14's monkey-patch — which is how **D-54** was found | **D-53**, **D-54** |
| 35 | How `SystemRunner` orders the frame, given that the multiplayer calls were interleaved at five separate points in `renderLoop` | **Derive the order from the code, and let `sendMove` stay where it physically is — inside the player step, not the network step.** Made in PR 18. §8.5 offers a `SYSTEM_ORDER` grouped by subject and then says, in as many words, *"derive it from the actual call order in the current `renderLoop` body, not from this list"* — and the two disagree: `sendMove` sits at `main.js:442-448`, between `player.update()` and the touch-look application, because it must send the **pre-look** rotation the frame already has. Grouping it with the other multiplayer calls at 577-650 would send a touch player's rotation one frame late, every frame. So the runner calls the network step **once**, at its own position, and `sendMove` lives in `PlayerStep.js`. The six steps are `input → player → view → combat → network → world`, and the three other load-bearing couplings are restated in the file that owns each: `mouse.update()` is the **last** statement of the combat step because the view and combat steps both read the just-clicked flags; the draw is in the **middle** of the world step, with the tooltip raycast and `updateRenderChunks` deliberately after it; and `state.frameCount++` is the last statement of the frame so frame 0 does a full pass (**D-34**). **Verified rather than asserted:** the adversarial pass normalised both sides and diffed the six step files against `main.js:386-846` statement by statement — not one statement moved | — |
| 36 | Where `camPos` lives, now that it is computed in one module and read two hundred lines later in another | **A declared `GameState` field, reassigned a *fresh* `THREE.Vector3` every frame.** Made in PR 18. `PR17_HANDOFF.md` §4.2 offered "reuse it per frame rather than reallocating, or thread a per-frame context". Reuse was rejected: `updateCamera`, `updateSkyPosition` and `biomeEffects.setCameraPosition` all receive it, and a mutated shared vector is a behaviour change for any consumer that retains the reference — unknowable without reading three files that PR 18 does not touch. A per-frame context object allocates one object per frame to avoid allocating one vector per frame. Assigning a fresh `Vector3` to `state.camPos` keeps the allocation count **exactly as it was** (one per frame, `main.js:494`) and makes the cross-module read explicit. **Decision 23 binds:** it is declared in the constructor, not grown by assignment | — |
| 37 | How the render loop and the pause menu reach `sessionManager`, which was a `main.js` module `let` read at nine points | **`state.session`, a declared `GameState` field, captured once at the top of `Game.init()` from `deps.sessionManager`.** Made in PR 18. The `let` was assigned by `initMenuNavigation`, by `rejoinSession` and by the auto-rejoin path — all three **before** any `GameState` exists, and none of them reassigns it during a session, so a one-time capture cannot go stale. Verified by the adversarial pass against all three writers: each assigns before its `startGame` call. The nine guard shapes are byte-preserved, **including the unguarded `state.session.client.playerId`** — preserving a missing guard is preserving behaviour, and it is logged separately rather than quietly fixed | — |
| 38 | **D-42**: whether the two live inline sites call `applyPerfSettings()` or it is deleted | **Deleted.** Made in PR 18. Neither site *can* call it — `initScene.js` step 5 has no chunk manager yet, and `initWorld.js` step 7 passes render distance through the `ChunkManager` constructor rather than a setter — so "wire it up" means reordering construction to serve a function with a dead third parameter. See the D-42 row | **D-42** — closed |
| 39 | **D-54**: whether `onExit` calling `game.stop()` is an acceptable behaviour change in the quit path | **Yes — make the call.** Made in PR 18. It is a real change (exiting to the menu now saves player position, inventory and equipment) but it is the change `DEPLOY.md` §7's timing table has claimed for three phases, so the alternative is editing the document to describe a teardown that never runs. It is also the only way **D-50**'s registry gets drained and the only way the 30 s interval is ever cleared. Wrapped in a `try/catch` so a failed save cannot break the exit path — which is **D-14**'s history, the PR 6b bug where one throw in `onExit` skipped six cleanup steps and left a blank page. The guard is on the *save*, not on the whole method: see the D-54 row for why that distinction is the whole ruling | **D-54** — closed |
| 40 | **D-50**: what owns a session's listener lifetime | **A teardown registry on `GameState`, drained by `Game.stop()`.** Made in PR 18. It is on `GameState` rather than `Game` because `state` is the object every init step already holds, so registering a remover is one line at the point of registration rather than a parameter threaded through nine modules; and `runTeardowns()` lives beside `addTeardown()` because the array is `GameState`'s. `Game.stop()` calls it, which keeps *session lifetime* on `Game` where decision 34 put it. **This also retired `Game._onStop`**, a hook PR 17 declared for exactly this job and never assigned — it had two references, both inside `Game.js`, and no assigner anywhere. See the D-50 row for the eight-versus-eleven correction | **D-50** — closed |
| 41 | Where the bootstrap lives, given that §4.1 wants `src/index.js` under 50 lines and ~310 lines are coming out of `main.js` | **`src/core/Bootstrap.js`, and the `< 50 lines` box does not close in PR 18.** Made in PR 18. `src/core/` already holds `Game.js` and `GameState.js`; a new top-level `src/app/` for one file buys nothing. `index.js` is **64 lines, of which 26 are D-25's ten side-effect imports and the comment explaining why they are deliberately in the graph** — without that block it is 37. **Those 26 lines are PR 20's, not PR 18's**, and deleting them to close a checkbox would silently drop ~6,000 lines out of reach of `npm run build` and of `no-undef`, which is the exact thing PR 9 refused to do. The box stays open and is labelled with its owner. The auto-rejoin block went to `src/multiplayer/AutoRejoin.js` rather than into the bootstrap — it is its own subject and keeps `Bootstrap.js` at 351 | — |
| 42 | **D-25**: wire or delete, for each of the twelve modules nothing references | **Six deleted, one reassigned, five deferred to a new slot — and the three-way split is the ruling.** Made in PR 20 off a twelve-module read-only triage banked in `PR20_HANDOFF.md` §4. **Deleted (2,592 lines of `src/`, 2,658 of `test/`)**: `AmbientAudio.js` and `SFX.js` (never instantiated; SFX's `blockIdToMaterial` is a pre-renumbering id table — **D-64**), `PerformanceOptimizer.js` (duplicates `PerformanceSettings` and `ChunkManager`'s own 2..16 clamp, and `estimateDeviceTier()` can only ever return MEDIUM because its detection block is gated on a `typeof THREE` that is never bound in the module), `Crosshair.js` (throws on its first hit — reads `hit.faceNormal.x` unguarded off the raycast branch that returns no `faceNormal`; the live crosshair is a DOM div), `pathfinding.js` (no importer, no test, and `isWalkable` never checks the cell it is testing), `SpawnManager.js` (`CharacterManager.spawnPoints` already owns per-character/per-world spawn and is live). **Reassigned**: `Noise.js` stays, owned by **PR 23** with **D-60** — `BiomeSystem` carries a verbatim copy of its Perlin block *twice*, and collapsing those onto it is what gives it a live consumer; deleting it now would take `test_noise.js`'s 38 assertions and PR 23 would need the module back. **Deferred to PR 34**: `SurvivalSystem`, `DamageSystem`, `QuestSystem`, `QuestMarker`, `Boss` — ~4,300 lines and ~1,000 assertions. **The line is that deleting a duplicate or a provably-broken module is triage, and deleting five feature subsystems is a product decision**; the standing process rule's own escape is a numbered slot, and "documented and unowned" is what it forbids. **One correction to this row's text, measured rather than assumed:** its severity cell says the modules are "dead weight in the bundle" — deleting 2,592 lines moved `dist/` by **2.78 kB (0.15%)**, and four of the six moved the hash not at all. Rollup has tree-shaken them since PR 9; the claim was true of classic `<script>` tags and stopped being true when they became modules. Do not expect a size win from PR 34 | **D-25** (six of twelve closed), **D-21** — closed, **D-69** |
| 43 | Whether PR 20 meets §9 PR 22's accept criterion, *"`renderLoop` calls only `SystemRunner.update(dt)` and `renderer.render()`"* | **No — the criterion is superseded, and meeting it literally would reorder the frame.** Made in PR 20. `renderer.render()` sits **deliberately in the middle of `WorldStep`**, with the hover-tooltip raycast and `chunkManager.updateRenderChunks` after it, because the tooltip reads the meshes the frame just drew and the chunk rebuild is meant to take the tail of the frame budget rather than delay the present. Collapsing the loop to `SystemRunner.update(dt); renderer.render();` moves the tooltip raycast, `updateRenderChunks` **and** `mobIntegration.update` from after the draw to before it — three measurable one-frame latency changes, in opposite directions, in a phase whose deliverable is an identical game. A second collision: `sendMove` must stay between `player.update()` and the touch-look (decision 35), while `playerSync.update(dt)` already runs a step later, so one system cannot occupy both slots. **What PR 20 did instead:** `src/game/systems/System.js` exists per §4.2 and the six step files remain the ordering spine, with the measured signature census in the class's header — only 3 of 11 live per-frame call sites already take `(dt)`; five take extra arguments and three have no `update` at all. Adoption is incremental, and the honest precondition for the sweep is that those signatures converge first. §9 PR 22's accept line is left in the document as a shape not taken, like §4.2's EventBus block | **D-25** |
| 44 | How a class whose methods share instance fields gets split, given the 400-line ceiling | **Prototype mixins.** Each method group is a plain object; the class file does `Object.assign(Class.prototype, …)` right after the declaration, so `this` is the same object, every call site is unchanged, and each body moves **verbatim**. Made in PR 23 for all three monoliths. Composition was rejected for `ChunkManager` specifically: its methods share **twelve** `this.` fields, and `flushDirty` reads `_flushQueue` + `memoryCache` + `_manifest` + `_db` + `_storeKey` together *because* **D-19** required the two stores not to disagree — a collaborator object is the split most likely to reintroduce it. Real modules were used only where the seam is genuinely zero-crossing. **The pattern's one new failure mode — a silent method overwrite by a later `Object.assign` — is closed by a load-time collision guard that names both files and throws**, proved non-vacuous in all three classes. **Verified, not asserted:** an AST differ compared every moved body against `git show HEAD:` — ChunkManager **53/53 byte-identical** apart from static-call renames and the two intended fixes, Inventory 48/49, Sky 39/46. Its price is **D-75**'s last item | — |
| 45 | Whether PR 23's split can be the eleven files §4.1 and §9 name | **No — twenty-three, and `ChunkCache.js` is not one of them.** Made in PR 23; decisions 30 and 33's situation for the fourth and fifth time, settled the same way — the ceiling wins and the cuts are seams chosen by **field-crossing count, not by topic**. `ChunkManager` 2,057 → 12 files, `InventorySystem` 981 → 7, `SkyRenderer` 1,007 → 4. **`ChunkCache.js` was deliberately not created:** §4.1 budgets 100 lines for "memoryCache Map" and `memoryCache` is read or written by 17 methods across every other target, so it would be a ~15-line `Map` wrapper four files each hold a reference to. **§9's `Hotbar.js` row was not followed either** — that filename is occupied by PR 17's icon renderer, and the three methods it names read five constructor-owned fields six other methods also use, so it is not a seam. Two cuts §4.1 does not name are the ones that make it work (`ChunkSchema.js`, `ChunkMeshCoordinator.js`); five more were forced by measurement, the strongest being `InventoryItemTypes.js`, whose four bodies contain **no `this` token at all**. `ChunkManager.js` lands at exactly **400** | — |
| 46 | **D-67**: which of `SkyRenderer`'s three disagreeing dawn/dusk schedules survives | **The sky-colour schedule — dawn 5 h → 7 h, dusk 17 h → 19 h — and all three consumers read one set of constants.** Made in PR 23. The sky is what the player actually sees, so fog, ambient and the phase label should agree with it rather than the reverse; `getSkyPhase()`'s hours already matched it exactly, so **only the fraction constants moved, and by at most 12 minutes of in-game time**. Expressed as `HOUR` constants in `DayNightCurves.js` with the fractions derived from them, so the two representations cannot drift again. `getSkyPhase`'s extra `dusk 19–20` label is label granularity, not a schedule, and is kept | **D-67** — closed |
| 47 | **D-51**: how the dropped-item colour table gets a source of truth, when `BLOCK_REGISTRY` has no colour field | **Derive it from the block's atlas tile — the alpha-weighted mean RGB — cached per id; the hand-written table is deleted.** Made in PR 23. The three options were hand-writing a `dropColor` on 193 entries (a content change with no test that can tell right from wrong, which is why PR 17 declined it), drawing the tile on the item, or deriving. **Only deriving is generated**, and `TextureAtlas` already exposes the `diffuseCanvas` and `tileMap` coordinates `Hotbar.renderItemIcon` samples the same way. `#888888` stays the fallback and is **never cached**. The two registry `color:` triples **multiply** the derived value rather than replacing it — `red_flower` and `yellow_flower` share one texture, so the derived mean alone would erase the only thing that field exists to say | **D-51** — closed |
| 48 | **D-68**: how far PR 23 goes into the mob spawn tables | **Fix the unmatchable names; defer "which biomes should have mobs" to PR 34.** Made in PR 23. A definition naming a biome `BiomeSystem` cannot produce is a **defect** — `corrupt_wolf` and `corrupt_wisp` have never spawned — so `'corrupt'` becomes `'badlands'`, the closest real analogue, and `stone_golem`'s unproducible `'deepslate_caves'` is dropped (its `'mountains'` entry already works; zero behaviour change verified over 500 draws). Deciding whether an **ocean** should have mobs is a content call of exactly the kind decision 42 removed from PR 20. **Five biomes still have none — `deep_ocean`, `ocean`, `beach`, `desert`, `frozen_peaks` — and the quest half (7 `markerBiome: 'corrupt'`, 5 `'lava'`) travels with PR 34.** What makes it stick is not the edit: `BIOME_NAME_TO_ID` is derived from `BIOME_DEFS` now, and `test_mobBiomes.js` fails if any mob names an id `BiomeSystem` cannot produce | **D-68** (names half) |
| 49 | **D-65**: whether `splitStack`'s no-op guard is `actualMove === 0` or `<= 0` | **`<= 0`.** Made in PR 23. A destination stack holding more than its `maxStack` — reachable, and now **D-76** — makes `space` negative, and a negative `actualMove` ran the transfer **backwards**: `fromSlot.count -= actualMove` *grew* the source. `<= 0` is the identical guard for the case the row names and closes that one too. The bounds half is `swapSlots`'s guard copied verbatim rather than newly written, because the row's complaint is that the two disagree | **D-65** — closed |
| 50 | **D-56**: what "passable" means, now that four inline id comparisons become one predicate | **`!BLOCK_PROPERTIES[id].solid` — the registry's own field, the one `Player._isSolidAt` already reads.** Made in PR 23. The row names three ids; writing those three as a set would have been a sixth hand-written table in the PR whose deliverable is deleting them. `BlockRegistry.js` computes `solid` as `block.solid !== undefined ? block.solid : block.category === 'solid'`, so mobs and the player now answer "is this solid?" from one place, and the four ices stay solid via their explicit `solid: true` despite `category: 'transparent'`. `mob.canSee` uses a separate `isAir` rather than `isPassable` deliberately — making leaves transparent to mob sight would be a gameplay change, and this way id 12 is the only delta | **D-56** — closed |
| 51 | Whether the **D-56** fix is acceptable when it leaves mobs sinking to the seabed | **Yes — land it and log the consequence.** Made in PR 23. Reverting to "water and lava are solid" means hand-writing the exception list decision 50 exists to delete, and the old behaviour was not correct either: mobs stood on the **surface** of oceans and lava lakes. What is actually missing is buoyancy, a swim path and drowning, none of which exists anywhere in `src/game/mobs/` — a gameplay feature, not a table fix. It is **D-70**, owned by **PR 34** alongside decision 48's five empty biomes. Stated here rather than left to be discovered, because it is a player-visible behaviour change in a PR whose other 22 files are structural | **D-70** |
| 52 | How the split CSS is loaded, given that §10 and §27 contradict each other | **One stylesheet, one `<link>`, an ordered `@import` manifest — §10's shape wins.** Made in PR 26. Adopting both proposals double-loads every rule, and adopting §27's *"import CSS from the module that uses it"* alone **breaks the cascade**: the 222-line responsive block and the 55-line crafting media block carry **no `!important`** and survive only on source position, while Vite emits per-module CSS in module-graph order. `index.css` is 30 `@import`s and nothing else, each annotated with the `style.css` line range it came from. **Proved rather than asserted:** 1,189 `(media, selector, property, value)` tuples compared against the original with every `var()` resolved — 0 residual differences — and the adversarial pass reproduced it independently and confirmed **0 rule-order inversions** and 0 token value drift across 368 substitutions | — |
| 53 | When the extracted templates mount, given §10's *"a template imported by its screen module"* | **All of them, eagerly, in one pass, as the first statement of `Bootstrap.start()`.** Made in PR 26. Mount-on-show is incompatible with the code as it stands: `UIManager`'s constructor resolves **19** elements eagerly and `src/` resolves **141 distinct ids** across 224 `getElementById` calls, so a screen mounting when shown hands every one of them `null`. **Verified by AST, not by reading:** every DOM lookup in all 146 files of `src/` sits inside a function or class body — **zero** at module-evaluation time — and `start()` has exactly one call site, so `mountTemplates()` cannot run twice and duplicate 157 ids. `index.html` is **28 lines**; the assembled DOM was proved identical to the original across **968 nodes**, the only differences being the 24 removed `style=` attributes and the classes replacing them | — |
| 54 | What happens to the four quarantined source-text tests | **Rewrite one, delete three; `QUARANTINE.md` reaches zero rows.** Made in PR 26. `test_pageLoad.js` 486 → **136 lines**, asserting the assembled DOM with its e2e id list **scraped from `saveLoad.js`** so the two cannot drift. The other three lose nothing: `test_responsiveHUD.js` was 38 `cssContent.includes(…)` string matches, one of them asserting a class no code writes to and a whole group asserting the **dead** `#crafting-grid`/`.crafting-slot` selectors — a test that pinned a bug rather than catching it; `test_mobileViewports.js` names nine devices and simulates none, its first-match extractor giving five of them the same 18 lines; `test_textureAssets.js` is 30 hand-written filenames against a directory of generated names, the same class as **D-64**. **The one thing genuinely lost is stated rather than waved off:** `test_textureAssets.js` also opened the 33 root PNGs and checked signature and 32×32 dimensions, which nothing else does | — |
| 55 | **D-41**: greyed-out button or error banner at the slot limit | **The greyed-out button, on the two modals.** Made in PR 26. Three of the five paths already did it and `#char-slot-info` already narrates *"3/3 characters (0 slots available)"*. The manager-level check stays — it is an invariant, not an affordance — so the banner remains reachable. Two sub-rulings settle with it: **null-manager polarity takes `WorldScreen`'s** (do not open when the manager is null), and all ten divergences between the five paths normalise to one implementation. There was **zero** coverage of any path before this | **D-41** — closed |
| 56 | **D-58**: does the pause menu own its listeners for the page or the session | **The session, with a complete cleanup.** Made in PR 26. `setupPauseMenu` already runs once per session and its cleanup already runs — it just removed 3 of 10. Naming the seven anonymous arrows and adding seven removers makes `Bootstrap.js:134`'s existing comment true, where the page-lifetime alternative means rewriting every handler to read through `deps.gameState`. The leak was worse than the row said and all three halves close together — see D-58 | **D-58** — closed |
| 57 | Whether the CSS split is §27's twenty-one files | **No — thirty-one.** Made in PR 26; decisions 30/33 for the sixth time. §27's list leaves **619 of 2,107 lines (29%) with no destination** — eleven whole sections, including the pause menu, the equipment panel and the 222-line responsive block — and names files that would be **empty** (`screen/character.css`, `screen/world.css`) or duplicated (`components/meters.css` *and* `hud/meters.css` are the same 43 lines). Every file is a **contiguous span of the original**, which is what makes the equivalence proof an ordered comparison rather than a set comparison. **§27's design tokens were partly invented and are corrected against measurement:** `--color-muted: #555` occurs **zero** times in the real CSS (the muted greys are four values) and `--font-mono: 'Courier New'` occurs zero times (it is bare `monospace`); the spacing scale is 4/8/**12**/16, not 4/8/16/24 | — |
| 58 | The orphan `.settings-panel` rule, which matches nothing | **Deleted, not resurrected.** Made in PR 26. It was authored for an inner panel element that has never existed, and materialising it is not a fix: `#settings-screen` is a full-viewport `.screen`, so `width: min(300px, 90vw)` would shrink the whole screen to a strip anchored left, and `.settings-group` at 312px would overflow the 268px content box a 300px panel leaves. Deleting an orphan rule is triage; designing a mobile settings panel is a UI decision with no test that can tell right from wrong | — |
| 59 | Whether decision 55's greyed-out button also applies to the three lobby `+ New` toggles | **No — those stay enabled and show the limit banner instead.** Made in PR 26, **after the adversarial pass found the first implementation had introduced a regression**: those buttons both open *and* close their inline form, and a disabled `<button>` dispatches no `click`, so at the limit a form that was already open became **undismissable except by Escape**. Decision 55 applies where there is no open state to trap. At the limit a closed lobby form does not open and sets the manager's own message; an open one always closes. **The message is not a second string literal** — `CharacterManager` and `WorldManager` now export the constant their own slot check returns, so the two cannot drift by construction. This is the fifth consecutive PR in which the adversarial pass found something five green gates did not | **D-41** |
| 60 | How 58 legacy test scripts become Vitest files, when each embeds its own `assert` helpers and its own report format | **Bodies move verbatim and un-reindented, one `it()` per file.** Made in PR 31. Each file becomes `it('<n>', () => legacy(async () => { …original body… }))` at column 0. Rewriting 24,000 lines into `describe`/`expect` was the alternative and it has **no gate that can tell a faithful rewrite from a weakened one** — the PR's deliverable is a runner swap, not a test rewrite. Re-indenting alone would silently alter every multi-line template literal in the suite. **The claim is checkable and was checked: every one of the 58 files reports exactly the assertion count its pre-migration original reported**, measured file-for-file against the legacy runner before it was deleted. The one file that is a rewrite rather than a migration is `sessionUI.test.js` (**D-47**), and it is excluded from that claim by name | **D-28**, **D-73** |
| 61 | `environment: 'jsdom'`, as `refactor.md` §11 specifies | **No — `'node'`, and jsdom is opt-in per file via `new JSDOM()` in the body.** Made in PR 31. `src/` carries **28 `typeof window` / `typeof document` / `typeof performance` feature-detection guards that are false under bare Node** (**D-27**, measured). jsdom flips every one of them: `Keyboard`, `Mouse` and `Touch` would register real listeners and `PlayerListHUD` would build DOM — **a larger behaviour change than the runner swap itself**, arriving in the same commit and invisible in the diff. The three files that need a DOM already build their own and keep doing so; `sessionUI.test.js` uses `new JSDOM()` in its body rather than the `@vitest-environment` pragma, because that sets `document` only and leaves `window`/`WebSocket`/`navigator` on their Node path | **D-27** |
| 62 | What to do about `process.exit`, which all 58 files call and which kills a Vitest worker | **Shim it, do not remove it — and treat "no verdict" as a failure.** Made in PR 31. `test/setup.js` records the code and throws; `test/helpers/legacy.js` turns the record into the test's verdict. **First exit wins**, because ten files wrap their body in `runTests().catch(err => { …; process.exit(1); })` and a success throw would otherwise be caught by the file's own error path and re-signalled as failure. The alternative — hand-editing 58 verdict blocks — is the highest-risk edit in the migration, 58 times, to remove a call one file can neutralise. **The load-bearing half is the inverse:** a body that finishes without ever signalling is reported as a FAILURE, which is what catches an async tail that was not awaited — a green run proving nothing, the failure mode this project has logged five times under other names. It fired three times during the migration and was right every time | — |
| 63 | Where the tests live, given that §32's `unit/{core,engine,game,ui}` has no home for a third of them | **`test/unit/{core,engine,game,ui,multiplayer,util,server,meta}/` plus `test/integration/`, and `__dirname` is an import.** Made in PR 31. §32's four directories leave `src/multiplayer/` (8 files), `src/util/`, `server/` and the four repo-invariant tests with nowhere to go; `meta` is where `globalCollisions`, `threePin` and `manifestGenerator` belong, because they assert facts about the repository rather than about a module. Files moved two or three directories deeper, so `test/helpers/paths.js` exports `TEST_DIR` and every migrated file imports it **as `__dirname`** — which means not one `path.join(__dirname, '..', …)` expression in any body had to be re-counted, and `__dirname` is deliberately NOT an ESLint global, so a stray one is a `no-undef` error rather than a silent `undefined` | — |
| 64 | **D-80**: what to do when the "pre-refactor v2 database dump" a deliverable requires does not exist and nobody has scheduled producing one | **A synthetic v2 seed meets the criterion, and no dump is ever going to exist.** Made in PR 31. Producing a real one means running the pre-refactor game in a browser and exporting IndexedDB — legal under decision 20 (it is local, not a deploy) but unscheduled through eleven PRs, and a captured dump is a fixture that rots. What matters is that the **ladder** runs against a **populated** version-2 database, and `test/integration/storageUpgrade.test.js` builds one through the shipped ladder with payloads from the real `ChunkBinaryCodec`, so the seed cannot drift from the encoder the way a checked-in dump would. It runs against a throwaway probe database and `DB_VERSION` stays 2 | **D-80** — closed |
| 65 | **D-48**: whether the multiplayer browser coverage goes into `saveLoad.js` or into a new file | **A third entry point — `test/e2e/multiplayer.js`, `npm run test:e2e:mp`.** Made in PR 31. `saveLoad.js`'s **189 assertions on both hosts, equal**, are the proof that the built bundle and the dev server are the same game; adding a two-context multiplayer section to it changes that number, puts a relay child inside the parity baseline, and makes the two hosts diverge (the vite host would have to spawn it too). A separate file costs one `package.json` line and ~72 s, keeps the baseline untouched, and can be run on its own by whoever next edits the session layer. It is not in CI — `ubuntu-latest` has no Edge — and it says so in its own header | **D-48** — closed |
| 66 | What replaces the assertion count, which `run_tests.sh` never produced | **`scripts/count-assertions.js`, and it fails rather than under-reporting.** Made in PR 31. The "6,532 assertions" figure quoted in six handoff documents was an ad-hoc shell loop rewritten from memory each time; the runner only ever printed how many *files* passed. The script runs Vitest non-silent and takes the last `N passed`-shaped line per file, which reproduces 6,532 exactly against the legacy runner — that equality is what makes it a measurement. **Two of its three guards exist because the adversarial pass broke it:** it exits non-zero if any file prints no count line (a new plain-Vitest file would otherwise contribute 0 in silence), and it stops reading at Vitest's own summary (which matches the count regex and was being charged to whichever file printed last) | — |
| 67 | Whether flipping `sourcemap` with `minify` (**D-30**) is safe when `sync.sh` ships `dist/*` | **`sourcemap: 'hidden'` AND `--exclude='*.map'` in the deploy archive — both, and `'hidden'` alone was not enough.** Made in PR 33 after its adversarial pass measured what would ship: `dist/assets/index-<hash>.js.map` carries `sourcesContent` for **148 files / 2,526,003 bytes** — the whole of `src/` plus `node_modules/three` — at a URL that is just the bundle's plus `.map`, chmod 644. That is **D-13**'s complaint (the repo reaching a web root) returning through the change meant to make the first deploy debuggable, and `sync.sh`'s own header claims it cannot happen. `'hidden'` only drops the `//# sourceMappingURL` comment; the file is still emitted, packed and served. The map stays in the local `dist/`, where `npm run test:e2e` uses it and where an operator loads it by hand against the same build — the hash in `index.html` names which one. **And the placement is load-bearing:** GNU tar applies `--exclude` only to members named after it, so both excludes had to move **before the first `-C`**; verified by building the archive both ways — 18 members with the map, 17 without | **D-30**, **D-13** |
| 68 | **D-25**: wire or delete, for each of the five deferred gameplay subsystems — the product call **decision 42** declined and created PR 34 to make | **Delete all five.** Made in PR 34, by the session, under the owner's standing instruction to decide rather than stop. Five reasons, in order. **(1)** Not one has ever executed outside `test/`; none is constructed anywhere in `src/` and the only edges in are data-table imports. **(2)** Wiring any of them is a *feature project, not a refactor step*: `Boss.js` has no rendering whatsoever and there is no boss HUD element, so wiring it means writing a boss renderer from scratch; `SurvivalSystem.generateHUDHTML()` emits a self-contained overlay that competes with the five `.meter-fill` elements already in the markup that nothing writes to, so wiring it is a UI decision between two designs; the quest layer has four incompatible `questProgress` shapes and zero production callers. **(3)** Their ~1,000 assertions were **defending defects** — `test_damageSystem` pinned `LAVA_ID = 15` against a registry where lava is 47, and a `NaN`-frozen unkillable boss sat under 369 green assertions. **(4)** Deleting is the **reversible** option: everything is in git and at the pushed `pre-refactor-baseline` tag, and a feature PR can restore any of it *with a design*, whereas keeping them puts 3,788 lines in every future change's blast radius — which is the complaint that put them on D-25. **(5)** It is the line decision 42 already drew when it deleted six siblings. **The cost is stated rather than waved off: this is the session deciding a product question the owner was not available for, and it removes four advertised features.** The five files are recoverable in one `git show` | **D-25** — closed, **D-69**, **D-68** (quest half) |
| 69 | **D-95**: the relay unit is `cuubz.service` on the host and `cuubz-relay.service` in the repo — which name wins | **The repo moves to `cuubz.service`; the host is not touched.** The owner's call, 2026-07-31, during the first deploy. `diff` between the two files was **empty** — byte-identical content, the filename the entire difference — so this was a naming choice rather than a drift to reconcile, and the direction that costs nothing wins: renaming the repo needs no root, no `daemon-reload` and no downtime, while renaming a running, enabled unit needs all three on a box with no `sudo` (**D-98**). **Three near-misses deliberately left alone**, because they only look like the unit name: the relay hostname `cuubz-relay.thehomelabguy.com`, `SyslogIdentifier=cuubz-relay` (renaming the journal tag would break continuity with every log line written before today), and `cuubz-relay[<pid>]` inside quoted journal excerpts. `refactor.md` and `PR34_HANDOFF.md` keep the old name for the same reason — they record what a past PR did. **The step 7 guard was inverted rather than deleted**: it now refuses if a `cuubz-relay.service` is found, since that is the pre-rename unit declaring the same port, and the old name stays in the pre-extract `rm` list so stale copies are cleared from any host deployed before today | **D-95** — closed |
| 70 | **D-98**: `sudo` is not installed on 10.0.30.160, so `sync.sh` cannot restart the relay — install it, or accept manual restarts | **Leave the host root-only and restart by hand.** The owner's call, 2026-07-31. The alternative was installing `sudo` plus a NOPASSWD rule scoped to `systemctl daemon-reload`, `systemctl restart cuubz` and the unit-file `cp` — which is what step 7 was written against and would make `./sync.sh` a genuine one-command deploy — and it was declined: a security posture change on a production host is not a bug fix, and it was not the thing being fixed. **So D-98 stays an open row as a standing operational condition, not a defect awaiting a patch.** What changed instead is that nothing pretends otherwise: step 7 distinguishes "sudo is not installed" (exit 92) from "sudo is installed but not passwordless" (exit 90), because the old single line sent the reader off to run four `sudo` commands that cannot exist on this box, and it prints the `ssh -t "su -c '…'"` form that does work. **The mitigation that makes this survivable is worth stating, because it is what actually recovered the relay on the day:** `Restart=on-failure` + `RestartSec=5` means a relay that is *down* picks up newly deployed code within five seconds with no root at all — the manual restart is only needed when the relay is *healthy*, and so has no reason to restart itself | **D-98** — accepted, stays open |
| 32 | Whether the remaining seventeen PRs are the right shape, after sixteen had landed | **No — collapsed to seven.** The owner's call, 2026-07-30, after PR 16. 18 absorbs 19; 20 absorbs 22 and **drops 21**; 23 absorbs 24 and 25; 26 absorbs 27, 28 and 29; 31 absorbs 32; 33 absorbs 30. **Numbers are not reassigned** — this file, source comments and six handoff documents cite PR numbers, so a renumber would silently break every citation; absorbed PRs keep their headings in `refactor.md`, marked **ABSORBED**, and the owner cells here were updated in the same commit. **The `EventBus` (PR 21) is dropped outright:** its stated benefit was cosmetic and its own section flags that `forEach` is registration-ordered and every ordering dependency would have to be re-asserted in new tests — which is trading explicit ordering for implicit ordering in a codebase where load order already produced D-36. PR 28/29's `Button`/`Modal`/`Slider` framework goes with it; **D-41** was the real content and moves to PR 26. **The gates were not cut** — both e2e hosts, the three per-commit gates, a row here for every bug, and non-vacuity proofs all stay, because H-1, D-32, D-34 and D-43 were each found under a fully green harness by structural change. **What was cut is the prose:** at the time of the collapse the repo carried 6,128 lines of planning documents against a 34,782-line `src/`. Outcome sections are a paragraph and a gate table now; handoffs are written when a session ends, not per PR | **D-21**, **D-41**, **D-42**, **D-33**, **D-47** |

---

## Where the numbers come from

Nothing in this file is inferred from source text alone. The measurements attached to
H-1 and D-15 were produced by `npm run test:e2e` (`test/e2e/saveLoad.js`) driving a real
browser and reading IndexedDB directly — see `DEPLOY.md` §9 for what that run does and
does not establish, and `refactor.md` §5 PR 6b for why it exists. Rows citing `sync.sh`
and the systemd unit are read from those files; `DEPLOY.md` §9 marks explicitly which of
them are **unverified** because nothing in this project has ever been run against the
deploy host.
